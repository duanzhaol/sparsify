{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_two_stage_soft_codebook_residual"},{"key":"EXPANSION_FACTOR","value":"2"},{"key":"K","value":"128"},{"key":"OPTIMIZER","value":"adam"},{"key":"LR","value":"3.0e-3"}],"expected_win":"lower_fvu","experiment_tier":"full","family_name":"lowrank_two_stage_soft_codebook_residual","family_stage":"mainline","hypothesis":"最终重构公式保持为 x_hat = U(Vx) + Σ_m pi_m c_m + Σ_i alpha_i^(1) b_i + Σ_j alpha_j^(2) b_j。low-rank trunk、soft codebook、两阶段 residual 都可静态导出为固定向量库上的有限加权和，因此可直接导出到 LUTurbo/Lottable 的“查表 + 加权求和 + 选择性补偿”推理链路；该 family 属于扩展兼容。当前主线在 EF=2 下已稳定满足 encoder 选择成本硬约束，且 signum 的 LR 轴已基本收敛：`LR=3.0e-3` 两轮复现优于 `2.4e-3`，而 `3.6e-3` 回退到 `FVU=0.058151...`，说明继续上探学习率的价值已下降。本轮只改一个主变量 `optimizer`，把 `OPTIMIZER` 从 `signum` 切到 `adam`，其余保持 `ARCHITECTURE=lowrank_two_stage_soft_codebook_residual, K=128, EXPANSION_FACTOR=2, LR=3.0e-3` 不变，回答唯一问题：在同样成本与部署形态下，adam 是否能带来更好的低成本 Pareto 点或更优曲线形状。","needs_sanity":false,"next_hypotheses":["如果 `adam` 在 `LR=3.0e-3` 下曲线健康且终点接近或优于 `signum`，则给 `adam` 一次单变量 LR 回调机会。","如果 `adam` 明显更差、发散或运行代价明显恶化，则关闭 optimizer 分支，保留 `signum + LR=3.0e-3` 为 EF=2 主线锚点。","在 optimizer 问题解决前，不改 K，也不改 TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM。"],"notes_to_memory":["兼容性判断：`lowrank_two_stage_soft_codebook_residual` 为扩展兼容；导出形式仍是 trunk 库 + codebook 库 + residual 字典库的有限加权和。","LR 轴结论：`LR=3.0e-3` 是当前更稳的 signum 主线候选；`LR=3.6e-3` 未带来改进，因此应停止继续上探。","本轮严格单变量，只改 `optimizer`；评估时继续同时检查曲线形状、`tokens_per_sec`、总耗时与 `final_total_tokens`，避免用更高训练代价换取不显著收益。"],"primary_variable":"optimizer","self_review":"这符合当前主线顺序：EF 已确认在预算内，clean baseline 已补齐，LR 轴也已通过 `3.0e-3` 与 `3.6e-3` 的结果基本收束，下一步应进入 optimizer 分支。保持 LR、EF、K 不变，只改 `optimizer`，归因清晰，并且遵守单变量原则。","summary":"停止继续上探 signum 学习率，转而在同一低成本主线配方上仅将 `OPTIMIZER` 从 `signum` 切到 `adam`，测试是否能在 EF=2 的可部署 regime 下得到更好的曲线或终点 FVU。","touched_files":[]}