{"command":"run","hypothesis":"After six non-improving rounds, the next valid move is a fundamentally different architecture: add explicit router-driven support selection to the strongest current low-K backbone (`lowrank_two_stage_soft_codebook_residual`) so the model can allocate its tight `K=8` budget more adaptively after soft-codebook coarse reconstruction.","summary":"Integrated a new family `routed_lowrank_two_stage_soft_codebook_residual` under `sparsify/`, added config validation and runtime dispatch, and passed the exact instantiate/forward/backward smoke test at `(architecture=routed_lowrank_two_stage_soft_codebook_residual, K=8, EF=12)`. This satisfies the severe-stagnation policy by pivoting to a new architecture rather than another minor hyperparameter tweak, while keeping the experiment anchored at the critical allowed low-K point.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"explore_unknown","family_name":"routed_lowrank_two_stage_soft_codebook_residual","family_stage":"prototype","self_review":"This is a real architecture pivot, not a parameter tweak: it changes support selection dynamics on both sparse stages while preserving the winning soft-codebook coarse path. Integration risk is low because the exact launch string passed smoke on CUDA. Scientific risk remains moderate: routing previously crashed in an older family and support-selection changes have often underperformed at low K, but this variant is better targeted because it starts from the current best low-K backbone instead of a weaker base.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"routed_lowrank_two_stage_soft_codebook_residual"},{"key":"K","value":"8"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["r91 introduced `routed_lowrank_two_stage_soft_codebook_residual`, a new prototype that adds explicit router-driven support selection to the current best soft-codebook two-stage low-K backbone.","Exact integration smoke passed for `routed_lowrank_two_stage_soft_codebook_residual` at `K=8`, `EF=12` on CUDA.","This round satisfies severe-stagnation policy by switching to a fundamentally different architecture instead of continuing LR/AUX tuning."],"next_hypotheses":["If `routed_lowrank_two_stage_soft_codebook_residual` wins at `K=8`, test the same family at `K=16` under the same recipe to see whether routing helps beyond the tightest low-K anchor.","If it is close but still behind at `K=8`, compare the same family at `K=16` before discarding it, because routing may help more once stage budgets are slightly less constrained.","If it clearly loses at `K=8`, conclude that extra routing on top of the soft-codebook backbone is not the missing ingredient and pivot next to a different structural family rather than more support-selection variants."],"primary_variable":"architecture"}

--- STDERR ---
OpenAI Codex v0.116.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: none
session id: 019d1896-8ca7-7ca1-95c7-f4081922f61d
--------
user
Continue the SAE research session. Round 91.
Return one JSON object matching the action schema. No markdown fences.

Policy guidance:
SEVERE STAGNATION: 6 rounds without improvement.
1. If only K=128 tested, MUST try K=64 or K=32
2. Otherwise try fundamentally different architecture
3. Stop minor tweaks — they haven't worked

K coverage: [1, 2, 4, 5, 6, 7, 8, 9, 10, 12, 16, 24, 32].

Structured update:
{
  "round": 91,
  "frontier": [
    "k=32 ef=12 fvu=0.08386963605880737 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @0821b7d",
    "k=24 ef=12 fvu=0.09216673672199249 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @c12369d",
    "k=16 ef=12 fvu=0.1046760231256485 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @22de855",
    "k=12 ef=12 fvu=0.1120903491973877 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @a743e7f",
    "k=10 ef=12 fvu=0.11728082597255707 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @fdc9df5",
    "k=9 ef=12 fvu=0.12033096700906754 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @f6cc90c",
    "k=8 ef=12 fvu=0.12280897796154022 arch=lowrank_soft_codebook_residual lr=3.2e-3 opt=signum @cbd5142",
    "k=7 ef=12 fvu=0.12780356407165527 arch=lowrank_two_stage_soft_codebook_residual lr=3.2e-3 opt=signum @8776e2a"
  ],
  "latest_results": [
    "r88_1774233565 lowrank_two_stage_soft_codebook_residual k8 discard fvu=0.12391594797372818 | Run one parameter-only exploitation experiment at `K=8` with",
    "r89_1774234012 lowrank_two_stage_soft_codebook_residual k8 discard fvu=0.12724432349205017 | Run one parameter-only exploitation experiment at `K=8` with",
    "r90_1774234384 lowrank_grouped_soft_codebook_residual k8 archive fvu=0.12297302484512329 | Added a new SAE family `lowrank_grouped_soft_codebook_residu"
  ],
  "latest_round_summary": [
    "r90 lowrank_grouped_soft_codebook_residual edit_sae_code -> archive fvu=0.12297302484512329 372s | At the critical low-K anchor `K=8`, combining the winning soft-codebook coars..."
  ],
  "agent_state": {
    "consecutive_crashes": 0,
    "consecutive_no_improve": 6,
    "rounds_since_new_family": 91
  },
  "pending_hints": [
    {
      "text": "K values are restricted to the following set: {4, 8, 16, 24, 32, 64, 96, 128}. Do NOT use any other K value. This applies to all experiments regardless of architecture or family.",
      "scope": "persistent",
      "status": "pending"
    }
  ],
  "next_hypotheses": [
    "If `lowrank_grouped_soft_codebook_residual` wins at `K=8`, test the same family at `K=16` under the same recipe to see whether the gain generalizes beyond the tight-budget anchor.",
    "If it is close but slightly behind at `K=8`, consider one confirmatory run with a larger `group_topk_size` before discarding the grouped-soft-codebook idea.",
    "If it clearly loses at `K=8`, conclude that added grouping hurts more than it helps even with the soft-codebook coarse path and pivot to a different architecture family rather than more support-selection variants."
  ]
}
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
mcp startup: no servers
codex
Round 91 is under severe stagnation, so I’m treating this as an architecture pivot rather than another recipe tweak. I’m checking which fundamentally different family can be added or activated under `sparsify/` while keeping the run at an allowed K anchor.
exec
/bin/bash -lc 'rg -n "asymmetric|soft_codebook|class LowRank.*SparseCoder|class .*Routed|router|bucket" sparsify/sparse_coder.py sparsify/config.py' in /root/sparsify-ascend succeeded in 0ms:
sparsify/config.py:197:            "bucketed_topk",
sparsify/config.py:210:            "bucketed_lowrank_residual",
sparsify/config.py:216:            "lowrank_soft_codebook_residual",
sparsify/config.py:217:            "lowrank_grouped_soft_codebook_residual",
sparsify/config.py:218:            "lowrank_two_stage_soft_codebook_residual",
sparsify/sparse_coder.py:298:    if architecture == "bucketed_topk":
sparsify/sparse_coder.py:326:    if architecture == "bucketed_lowrank_residual":
sparsify/sparse_coder.py:338:    if architecture == "lowrank_soft_codebook_residual":
sparsify/sparse_coder.py:340:    if architecture == "lowrank_grouped_soft_codebook_residual":
sparsify/sparse_coder.py:342:    if architecture == "lowrank_two_stage_soft_codebook_residual":
sparsify/sparse_coder.py:505:        self.bucket_scale = nn.Parameter(
sparsify/sparse_coder.py:508:        self.bucket_bias = nn.Parameter(
sparsify/sparse_coder.py:533:        gate = torch.sigmoid(self.bucket_scale * centered_norms + self.bucket_bias)
sparsify/sparse_coder.py:561:        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
sparsify/sparse_coder.py:562:        self.code_router.bias.data.zero_()
sparsify/sparse_coder.py:577:        logits = self.code_router(x)
sparsify/sparse_coder.py:584:        # path through the router so DDP does not see unused parameters.
sparsify/sparse_coder.py:664:        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
sparsify/sparse_coder.py:665:        self.code_router.bias.data.zero_()
sparsify/sparse_coder.py:680:        logits = self.code_router(x)
codex
The grouped soft-codebook probe was close but still dominated, so I’m moving to a stronger architecture change: explicit router-driven support selection on top of the best current soft-codebook two-stage backbone. I’m reading the routed two-stage implementation now and will wire the soft-codebook variant cleanly if it looks viable.
exec
/bin/bash -lc "sed -n '1246,1406p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
class RoutedLowRankTwoStageResidualSparseCoder(LowRankTwoStageResidualSparseCoder):
    """Two-stage low-rank residual SAE with explicit router-driven support selection."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
        self.stage1_router = nn.Linear(
            d_in, self.num_latents, device=device, dtype=dtype
        )
        self.stage2_router = nn.Linear(
            d_in, self.num_latents, device=device, dtype=dtype
        )

file update
M /root/sparsify-ascend/sparsify/config.py
@@ -218,2 +218,3 @@
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
M /root/sparsify-ascend/sparsify/sparse_coder.py
@@ -343,2 +343,4 @@
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
@@ -2127,2 +2129,169 @@
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
apply_patch(auto_approved=true) exited 0 in 8ms:
Success. Updated the following files:
M /root/sparsify-ascend/sparsify/config.py
M /root/sparsify-ascend/sparsify/sparse_coder.py
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index 7cb32aeacc7728672ef6a730d1c0e7a0f3f22616..2ad07a43f47dd413e1861ada31a686956bee1928
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -216,6 +216,7 @@
             "lowrank_soft_codebook_residual",
             "lowrank_grouped_soft_codebook_residual",
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 72527b5f3008ef85fc9bb755703f872707c542cc..6c30aa4dba2aa896169f6afb5b2981d5756b6475
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -341,6 +341,8 @@
         return LowRankGroupedSoftCodebookResidualSparseCoder
     if architecture == "lowrank_two_stage_soft_codebook_residual":
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -2125,6 +2127,173 @@
         )
 
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index 7cb32aeacc7728672ef6a730d1c0e7a0f3f22616..2ad07a43f47dd413e1861ada31a686956bee1928
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -216,6 +216,7 @@
             "lowrank_soft_codebook_residual",
             "lowrank_grouped_soft_codebook_residual",
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 72527b5f3008ef85fc9bb755703f872707c542cc..6c30aa4dba2aa896169f6afb5b2981d5756b6475
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -341,6 +341,8 @@
         return LowRankGroupedSoftCodebookResidualSparseCoder
     if architecture == "lowrank_two_stage_soft_codebook_residual":
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -2125,6 +2127,173 @@
         )
 
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
The new routed soft-codebook family is wired into config and dispatch. I’m running the exact integration smoke at the intended low-K target before I commit the next-round action.
exec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify import SparseCoder
from sparsify.config import SparseCoderConfig

device = 'cuda' if torch.cuda.is_available() else 'cpu'
try:
    import torch_npu
    if torch.npu.is_available():
        device = 'npu'
except ImportError:
    pass

arch = 'routed_lowrank_two_stage_soft_codebook_residual'
k = 8
ef = 12

cfg = SparseCoderConfig(architecture=arch, k=k, expansion_factor=ef)
sae = SparseCoder(1024, cfg, device=device, dtype=torch.float32)
x = torch.randn(4, 1024, device=device)
out = sae(x)
out.fvu.backward()
print('integration_smoke: OK', arch, k, ef, device)
PY" in /root/sparsify-ascendfile update:
diff --git a/sparsify/config.py b/sparsify/config.py
index 7cb32aeacc7728672ef6a730d1c0e7a0f3f22616..2ad07a43f47dd413e1861ada31a686956bee1928
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -216,6 +216,7 @@
             "lowrank_soft_codebook_residual",
             "lowrank_grouped_soft_codebook_residual",
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 72527b5f3008ef85fc9bb755703f872707c542cc..6c30aa4dba2aa896169f6afb5b2981d5756b6475
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -341,6 +341,8 @@
         return LowRankGroupedSoftCodebookResidualSparseCoder
     if architecture == "lowrank_two_stage_soft_codebook_residual":
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -2125,6 +2127,173 @@
         )
 
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

 succeeded in 4.53s:
Using fused decoder on CUDA.
integration_smoke: OK routed_lowrank_two_stage_soft_codebook_residual 8 12 cuda
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index 7cb32aeacc7728672ef6a730d1c0e7a0f3f22616..2ad07a43f47dd413e1861ada31a686956bee1928
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -216,6 +216,7 @@
             "lowrank_soft_codebook_residual",
             "lowrank_grouped_soft_codebook_residual",
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 72527b5f3008ef85fc9bb755703f872707c542cc..6c30aa4dba2aa896169f6afb5b2981d5756b6475
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -341,6 +341,8 @@
         return LowRankGroupedSoftCodebookResidualSparseCoder
     if architecture == "lowrank_two_stage_soft_codebook_residual":
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -2125,6 +2127,173 @@
         )
 
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
{"command":"run","hypothesis":"After six non-improving rounds, the next valid move is a fundamentally different architecture: add explicit router-driven support selection to the strongest current low-K backbone (`lowrank_two_stage_soft_codebook_residual`) so the model can allocate its tight `K=8` budget more adaptively after soft-codebook coarse reconstruction.","summary":"Integrated a new family `routed_lowrank_two_stage_soft_codebook_residual` under `sparsify/`, added config validation and runtime dispatch, and passed the exact instantiate/forward/backward smoke test at `(architecture=routed_lowrank_two_stage_soft_codebook_residual, K=8, EF=12)`. This satisfies the severe-stagnation policy by pivoting to a new architecture rather than another minor hyperparameter tweak, while keeping the experiment anchored at the critical allowed low-K point.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"explore_unknown","family_name":"routed_lowrank_two_stage_soft_codebook_residual","family_stage":"prototype","self_review":"This is a real architecture pivot, not a parameter tweak: it changes support selection dynamics on both sparse stages while preserving the winning soft-codebook coarse path. Integration risk is low because the exact launch string passed smoke on CUDA. Scientific risk remains moderate: routing previously crashed in an older family and support-selection changes have often underperformed at low K, but this variant is better targeted because it starts from the current best low-K backbone instead of a weaker base.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"routed_lowrank_two_stage_soft_codebook_residual"},{"key":"K","value":"8"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["r91 introduced `routed_lowrank_two_stage_soft_codebook_residual`, a new prototype that adds explicit router-driven support selection to the current best soft-codebook two-stage low-K backbone.","Exact integration smoke passed for `routed_lowrank_two_stage_soft_codebook_residual` at `K=8`, `EF=12` on CUDA.","This round satisfies severe-stagnation policy by switching to a fundamentally different architecture instead of continuing LR/AUX tuning."],"next_hypotheses":["If `routed_lowrank_two_stage_soft_codebook_residual` wins at `K=8`, test the same family at `K=16` under the same recipe to see whether routing helps beyond the tightest low-K anchor.","If it is close but still behind at `K=8`, compare the same family at `K=16` before discarding it, because routing may help more once stage budgets are slightly less constrained.","If it clearly loses at `K=8`, conclude that extra routing on top of the soft-codebook backbone is not the missing ingredient and pivot next to a different structural family rather than more support-selection variants."],"primary_variable":"architecture"}
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index 7cb32aeacc7728672ef6a730d1c0e7a0f3f22616..2ad07a43f47dd413e1861ada31a686956bee1928
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -216,6 +216,7 @@
             "lowrank_soft_codebook_residual",
             "lowrank_grouped_soft_codebook_residual",
             "lowrank_two_stage_soft_codebook_residual",
+            "routed_lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 72527b5f3008ef85fc9bb755703f872707c542cc..6c30aa4dba2aa896169f6afb5b2981d5756b6475
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -341,6 +341,8 @@
         return LowRankGroupedSoftCodebookResidualSparseCoder
     if architecture == "lowrank_two_stage_soft_codebook_residual":
         return LowRankTwoStageSoftCodebookResidualSparseCoder
+    if architecture == "routed_lowrank_two_stage_soft_codebook_residual":
+        return RoutedLowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -2125,6 +2127,173 @@
         )
 
 
+class RoutedLowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageSoftCodebookResidualSparseCoder
+):
+    """Soft-codebook two-stage residual SAE with explicit router-driven support selection."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.stage1_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+        self.stage2_router = nn.Linear(
+            d_in, self.num_latents, device=device, dtype=dtype
+        )
+
+        nn.init.kaiming_uniform_(self.stage1_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.stage2_router.weight, a=5**0.5)
+        self.stage1_router.bias.data.zero_()
+        self.stage2_router.bias.data.zero_()
+
+    def _route_stage(
+        self,
+        inputs: Tensor,
+        weight: Tensor,
+        bias: Tensor,
+        router: nn.Linear,
+        k: int,
+    ) -> tuple[Tensor, Tensor, Tensor, Tensor]:
+        pre_acts = F.linear(inputs, weight, bias)
+        acts = F.relu(pre_acts)
+        router_logits = router(inputs) / self.cfg.gated_temperature
+        router_gate = torch.sigmoid(router_logits)
+        routed_acts = acts * router_gate
+        scores = routed_acts + 0.1 * torch.tanh(router_logits)
+        _, top_indices = torch.topk(scores, k, dim=-1, sorted=False)
+        top_acts = routed_acts.gather(-1, top_indices)
+        return top_acts, top_indices, scores, routed_acts
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, _ = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_scores = torch.maximum(stage1_scores, stage2_scores)
+        return EncoderOutput(combined_acts, combined_indices, combined_scores)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_scores, stage1_routed = self._route_stage(
+            code_residual,
+            self.encoder.weight,
+            self.encoder.bias,
+            self.stage1_router,
+            self.stage1_k,
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_scores, _ = self._route_stage(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_router,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_scores, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            _, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_acts = stage1_routed.gather(-1, aux1_indices)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_input = code_residual - aux1_out
+            aux2_logits = F.linear(
+                aux2_input,
+                self.residual_encoder.weight,
+                self.residual_encoder.bias,
+            )
+            aux2_full = F.relu(aux2_logits)
+            aux2_router_logits = (
+                self.stage2_router(aux2_input) / self.cfg.gated_temperature
+            )
+            aux2_routed = aux2_full * torch.sigmoid(aux2_router_logits)
+            aux_stage2 = torch.where(
+                dead_mask[None],
+                aux2_routed + 0.1 * torch.tanh(aux2_router_logits),
+                -torch.inf,
+            )
+            _, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_acts = aux2_routed.gather(-1, aux2_indices)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

tokens used
115,156
