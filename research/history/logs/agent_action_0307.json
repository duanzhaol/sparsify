{"command":"run","hypothesis":"最终重构公式保持为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh b_i^sh + Σ_{j∈S_{e1(r1)}^τ(r1)} β_j c_{e1,j} + Σ_{m∈S_{e2(r1)}^τ(r2)} γ_m c_{e2,m}`，其中 `r1(x)=x-shared(x)`，`r2(x)=r1(x)-expert1_cleanup(r1)`，`(e1,e2)` 是同一次 product-key 路由在 `r1` 上选出的有序 top-2 静态 expert 子库。在线导出仍是“shared 静态库查表 + ordered PK 选静态 expert 子库 + 两段有限加权和 + 选择性补偿”，不涉及动态字典生成，因此兼容性判断为 `直接兼容`。相对 r305 本轮只改 `STAGE1_RATIO=0.125`，其余 `ARCHITECTURE=shared_cascade_product_key_expert_jumprelu, K=32, EF=1, NUM_EXPERTS=3072, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, OPTIMIZER=adam, LR=8e-4` 保持不变；主假设是：r306 的条件触发第二专家明显抬高 exceed，说明当前 low-K 主瓶颈不是“少开第二专家”，而是 routed cleanup 预算仍偏紧。因此在 current winning scaffold 上，把 shared/coarse 预算从 `8/24` 调成 `4/28`，在完全同成本下给两段 routed cleanup 更多 K，可能比继续扩 bank 或再开新 family 更直接压低 hard-token exceed。该点本地精确成本复核为 `selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`，与 r305 同成本并满足预算；若要击败 r305 的 exact `objective=0.267107`，需要把 `exceed_alpha_0.50` 压到约 `0.15993` 以下。","summary":"建议以 r305 为 reference，只改 `STAGE1_RATIO=0.125`。这是一次同成本、单变量、明确面向降低 exceed 的 knee 校准：不再减少 cleanup 深度，而是在当前 best cascade scaffold 上把更多 K 还给 routed residual cleanup，直接检验 hard-token tail 是否因此下降。","change_type":"param_only","expected_win":"lower_objective","family_name":"shared_cascade_product_key_expert_jumprelu","family_stage":"prototype","self_review":"这轮仍留在 incumbent scaffold 邻域，但不是低信息细扫。之所以允许做这一个 patch，是因为 r306 已给出非常具体的新证据：条件关闭第二专家会显著恶化 exceed，说明当前值得优先验证的不是再发散开新 family，而是把同成本预算从 coarse 端挪回 cleanup 端，看交换率是否立刻改善。主要风险是 stage1 过薄后 shared coarse 主干退化，导致 exceed 不降反升；若如此，就应把 `STAGE1_RATIO` 这条轴视为已完成 knee 裁决，而不是继续围绕它补点。","needs_sanity":false,"env_overrides":[{"key":"STAGE1_RATIO","value":"0.125"}],"touched_files":[],"notes_to_memory":["r305 的 persisted exact 结果为 objective=0.267107、cost=0.107178、exceed=0.159929；后续阈值判断应以这组 exact 值为准。","r306 `shared_conditional_cascade_product_key_expert_jumprelu` 在同成本档回退到 objective=0.291644、exceed=0.184467，说明当前 low-K 区间里减少第二段 cleanup 会明显伤害 tail-error。","本地 override allowlist 校验已通过：`validate_env_overrides([{key:'STAGE1_RATIO', value:'0.125'}], fallback_architecture='shared_cascade_product_key_expert_jumprelu') == []`。","本地 exact-string smoke 已通过：`shared_cascade_product_key_expert_jumprelu` 在 `K=32, EF=1, NUM_EXPERTS=3072, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.125` 下可 instantiate、forward、backward。","本地成本复核：`selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`；与 r305 处于相同成本档。","本地 `validate_action(...)` 复核已通过：以 `reference_round=305` 且仅改 `STAGE1_RATIO=0.125` 的 param_only 提案不会被当前 policy 拦截。"],"next_hypotheses":["如果该点在同成本下把 exceed 压到 `<=0.15993` 并赢过 r305，则下一轮保留该 scaffold，只做一次最小容量校准，例如回到 `NUM_EXPERTS=2048` 或保留 3072，以判断更大 cleanup 预算与 bank 粒度的组合是否真有协同。","如果该点 exceed 有所下降但 objective 仍未明显优于 r305，则记录为“cleanup 预算增加有帮助但不足以形成 regime shift”；后续应优先切向新的 scaffold-level architecture probe，而不是继续扫 split。","如果该点与 r305 持平或更差，则可更明确地结束 `STAGE1_RATIO` 这条轴在当前 cascade scaffold 上的探索，下一轮默认优先换结构槽位。"],"reference_round":305}