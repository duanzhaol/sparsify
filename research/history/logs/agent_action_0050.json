{"change_type":"edit_sae_code","command":"run","family_name":"lowrank_expert_topk","family_stage":"stabilize","reference_round":42,"env_overrides":[{"key":"TRUNK_RANK","value":"128"}],"expected_win":"explore_unknown","needs_sanity":true,"hypothesis":"最终重构公式保持为 x_hat = Σ_{r=1..R} β_r(x) u_r + Σ_{i∈S_{e(x-t(x))}(x-t(x))} α_{e,i}(x) b_{e,i}，其中 t(x)=Σ_{r=1..R} β_r(x) u_r。也就是先用静态 low-rank trunk 向量库 U={u_r} 重构平滑主干，再对残差用轻量 router 选择单个静态 expert 子库 B_e={b_{e,i}} 并做局部 sparse 选择；因此它仍可直接导出到 LUTurbo/Lottable 的“trunk 查表 + expert 子库查表 + 有限加权和”推理链路，兼容性判定为扩展兼容。Round 48 和 Round 49 都表明 blocker 仍然是运行时构造路径，而不是结构质量：policy behavioral diff 通过 `from sparsify import SparseCoder` 直接实例化时，`cfg.architecture=lowrank_expert_topk` 仍未生效，比较结果依旧变成 topk vs topk。本轮主任务是继续修复 `sparsify/sparse_coder.py` 中的直接构造分发，让 `SparseCoder(1024, SparseCoderConfig(architecture='lowrank_expert_topk', ...))` 真正返回对应 family，然后按 r42 仅改 `TRUNK_RANK=128` 的原计划重跑，拿到这条 `lowrank + expert` 结构在约 0.44x total_cost 下的首个有效点。","summary":"当前最高优先级仍是消除重复 blocker，而不是继续在 `lowrank_expert_residual` 上打磨低成本邻域点。r48、r49 连续两轮都显示 `lowrank_expert_topk` 在 behavioral diff 阶段被误拦，且 round summary 里 `touched_files=[]`、`patch_path=null`，说明修复并未真正落地；因此 Round 50 应继续做最小 repair，把 `SparseCoder(..., cfg)` 的分发修通，再重跑相对 r42 仅改 `TRUNK_RANK=128` 的低成本 probe。","self_review":"满足当前阶段的运行时优先级：先消除 blocker，再做结构比较。代码改动范围仍只限 `sparsify/`，不改 evaluation semantics，也不改 AutoResearch/launcher；实验配置相对 r42 仍只改一个 env 参数 `TRUNK_RANK=128`。这次 repair 需要给出可验证的完成标准：1. 直接构造 `SparseCoder(..., cfg)` 时按 `cfg.architecture` 分发；2. 用完全相同的 `architecture=lowrank_expert_topk, K=192, EF=1` 做 instantiate/forward/backward smoke；3. behavioral diff 不再把它判成 topk。风险是修复后该 family 的真实效果可能仍不强；但那时才是有效结构结论，而不是观测链路伪影。","touched_files":["sparsify/sparse_coder.py"],"notes_to_memory":["r48 和 r49 之后可以确认：当前 blocker 仍是 `SparseCoder(..., cfg)` 直接构造路径没有正确尊重 `cfg.architecture`，而不是 `lowrank_expert_topk` 本身的结构质量。","`lowrank_expert_topk` 的 LUTurbo 形式仍是静态 trunk 库 + 单 expert 静态子库上的有限加权和，属于扩展兼容 family。","按当前成本估算，`lowrank_expert_topk K=192 EF=1 TRUNK_RANK=128` 的 total_cost 约为 0.44x；修复 blocker 后应优先获得这个点的真实 FVU。"],"next_hypotheses":["如果 repair 后 `TRUNK_RANK=128` 的真实结果在约 0.44x 成本下明显优于当前 `<0.5x` anchor，则下一轮保持 family 不变，只改 `TRUNK_RANK=64`，测试 lowrank+expert 是否还能继续向左延伸。","如果 repair 后该点仍明显弱于 `jumprelu` 或 `lowrank_expert_residual` 的低成本点，则应归档 `lowrank_expert_topk`，把预算转回其他高优先级结构方向。","如果 repair 后出现新的失败模式，则下一轮继续只处理新的 blocker，不把结果误判为架构质量结论。"]}