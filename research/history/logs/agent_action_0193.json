{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"factorized_expert_topk"}],"expected_win":"lower_fvu","experiment_tier":"proxy","family_name":"factorized_expert_topk","family_stage":"prototype","hypothesis":"候选 family 设为 `factorized_expert_topk`。最终重构公式为 `x_hat = Σ_{e∈E(x)} Σ_{i∈S_e(h(x))} α_{e,i}(h(x)) b_{e,i}`，其中 `h(x)=ReLU(Bx)` 是共享低秩 bottleneck，`{b_{e,i}}` 是各 expert 的静态子库原子；在线侧可导出为“低秩 scorer/router 选 expert 与局部 support，再在静态 expert 子库上查表并做有限加权和”，静态库不随样本动态生成，因此兼容性判断为 `直接兼容`。唯一主假设：相对 r190 仅切换到 `ARCHITECTURE=factorized_expert_topk`，在保持 `K=96`、`ACTIVE_EXPERTS=1`、`LATENTS_PER_EXPERT=192` 的 very-short-path recipe 下，用更轻的 expert-internal low-rank small head 替代当前 `lowrank_expert_topk` 的 trunk+expert 路径，可以在约 `0.17x total_cost` 内把 FVU 压到显著低于 r190 的 `0.2322`，从而判断 small-head routed MoE-like 线是否值得继续。","needs_sanity":false,"next_hypotheses":["如果这轮在约 0.17x 下明显优于 r190，下一轮优先继续留在该 family，只改 `FACTORIZED_HIDDEN_DIM` 做最小容量归因。","如果这轮仍明显弱于 r190 或低成本 anchors，则可基本结束当前 target 上的 expert-internal low-rank small-head 方向，把预算转回别的 architecture-level probe。"],"notes_to_memory":["本地成本自检：在 r190 exact recipe 上切到 `factorized_expert_topk` 后，selection≈0.08x、deployment≈0.09x、combined≈0.17x，处于 `<0.25x` 主战场且远低于 1.5×h×n 硬约束。","历史信号上，`shared_routed_factorized_expert_topk` 基本连续负证据，而 `factorized_expert_topk` 至少在 r184 给过一次 keep；因此在继续 low-cost architecture probe 时，优先回到更短 active path 的 small-head 线更合理。"],"reference_round":190,"self_review":"这轮满足单变量原则：相对 r190 只改 `ARCHITECTURE`。没有新增 tunable 参数，也没有代码改动，因此不需要触碰 `research/AutoResearch/` 或 `scripts/`。选择 `factorized_expert_topk` 而不是继续试 `shared_lowrank_*` / `two_stage_*`，是因为这些方向已被当前策略明确下调优先级；选择它而不是 `shared_routed_factorized_expert_topk`，是因为后者在当前 target 上的低成本负信号更一致，而本轮更需要回答的是“更轻的 expert-specific small head 本身是否有效”。主要风险是该 family 的旧轮次先验偏弱；但当前这次是以 r190 exact small-K recipe 做 fresh matched probe，信息增量仍然高。","summary":"建议以 r190 为 reference，只改 `ARCHITECTURE=factorized_expert_topk`。这是一次落在 `<0.25x` 主战场内、且比 r190 更靠左的 matched architecture probe，用当前 `K=96` 小支撑配方直接检验 expert-internal low-rank small-head 是否能形成新的低成本结构段。","touched_files":[]}