{"change_type":"param_only","command":"run","env_overrides":[{"key":"K","value":"192"}],"expected_win":"lower_fvu","experiment_tier":"proxy","family_name":"topk","family_stage":"mainline","hypothesis":"最终重构公式固定为 x_hat = Σ_{i∈S(x)} α_i(x) b_i。`topk` 的在线侧只输出稀疏支持集 S(x) 与对应系数 α_i(x)，离线侧字典库 B={b_i} 保持静态，因此可直接导出到 LUTurbo/Lottable 的静态向量查表加权和推理链路，兼容性判定为直接兼容。本轮相对 r13 仅改 `K: 160 -> 192`，回答一个问题：在 `EF=1` 的同一 encoder 成本槽位下，把 deployment 预算从约 0.16x 提到约 0.19x 后，`topk` 在 <0.5x 总成本带内是否仍有清晰 Pareto 改善，且训练曲线后段是否继续稳定下行而不是开始明显变平。","needs_sanity":false,"next_hypotheses":["如果 `K=192` 仍有明确改善，则说明当前 `topk, EF=1` 在 <0.5x 成本带内 deployment-side frontier 仍未走平，后续可再做一次边界探针到 `K=256` 以确认 0.5x 附近的斜率。","如果 `K=192` 收益已明显减弱或曲线提前饱和，则说明当前主线 family 在该结构槽位上的 deployment 增益开始递减，后续应停止继续抬 `K`，转向其他兼容 family 或高优先级未充分验证的轻量 expert 子库 / MoE-like 方向。"],"notes_to_memory":["`topk` 属于直接兼容 family；导出形式保持为 `x_hat = Σ_{i∈S(x)} α_i(x) b_i`，满足 LUTurbo/Lottable 的静态库有限加权和约束。","Round 14 继续只改 `K`，避免引入 optimizer/LR/family 混杂，优先确认 deployment 成本轴在当前 <0.5x 成本带内的边际收益是否开始变平。","按当前 proxy 线性近似，`K=192` 的 deployment 成本约 0.19x，总成本约 0.44x（0.25x + 0.19x），显著低于 1.5x 硬约束，也仍位于 <0.5x 低成本带内。"],"reference_round":13,"self_review":"满足单轮单参数变更约束；相对 r13 只修改了 `K`。选择 `K=192` 而不是直接跳到 `K=256`，是为了继续保持 deployment 轴归因清晰，并先确认 <0.5x 成本带内的斜率是否已经开始变平。","summary":"继续主线 `topk`。其最终重构公式为 `x_hat = Σ_{i∈S(x)} α_i(x) b_i`，可直接导出为 LUTurbo/Lottable 的静态字典查表加权和，因此属于直接兼容。Round 14 以 r13 为参考，只把 `K` 从 160 提到 192，在不改变 encoder 成本的前提下继续测 deployment 预算增加后的边际收益，并重点观察训练曲线是否开始饱和。","touched_files":[]}