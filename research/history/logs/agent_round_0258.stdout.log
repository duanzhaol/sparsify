{"command":"run","hypothesis":"候选 family 设为 `shared_product_key_expert_jumprelu`。最终重构公式为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh(x) u_i + Σ_{e∈E_pk(r_1(x))} Σ_{j∈S_e^τ(r_1(x))} γ_{e,j}(x) b_{e,j}`，其中 `r_1(x)=x-Σ_{i∈S_sh(x)} α_i^sh(x) u_i`，`{u_i}` 是 always-on shared coarse 静态向量库，`{b_{e,j}}` 是各 routed expert 的静态子库原子，`E_pk(r_1(x))` 由 left/right product-key router 给出组合式 expert addressing，expert 内再用 JumpReLU 形成局部 support。导出到 LUTurbo/Lottable 的在线链路仍是“shared coarse 子库查表/加权和 -> residual 上做 product-key expert 选择 -> expert 内 JumpReLU sparse support -> 静态库查表加权和 -> 选择性补偿”，静态库不随样本动态生成，因此兼容性判断为扩展兼容。它与现有 `shared_two_stage_residual_expert` 的精确差异不是换名，而是把第二阶段的 flat router 改成了 product-key compositional routing；与 `product_key_expert_jumprelu` 的精确差异则是新增了 always-on shared coarse stage，让 routed micro-experts 只做 residual cleanup。相对 r235 只把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新 family，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4`、`NUM_EXPERTS=128`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=192`、`STAGE1_RATIO=0.25` 与其余主 recipe 不变，直接回答当前 prompt 里最高优先级但尚未被现有实现正面覆盖的问题：`shared/coarse + PK-routed micro-experts` 这种不对称分工，是否能比 plain routed / shared+flat-router / router-only 结构更慢地恶化 small-K exceed。按本地 exact 成本复核，该 matched probe 的 selection_accesses=613376、deployment_accesses=163840、combined_accesses=777216、exact total_cost_ratio=0.185302734375，仍远低于 1.5×h×n 硬预算；若要打赢 r235 在当前 controller 口径下的 exact objective=0.358553，则需要把 `exceed_alpha_0.50` 压到 `0.173250265625` 以下。门槛不低，但它是当前最贴近“shared/coarse 主干 + compositional tiny cleanup experts”主问题、且具有明确新信息增量的 matched architecture probe。","summary":"已实际新增并注册 `shared_product_key_expert_jumprelu`。建议以 r235 为 reference，只改 `ARCHITECTURE=shared_product_key_expert_jumprelu`，做一轮新的 matched architecture probe，直接判断“shared coarse library + product-key routed micro-experts”这个结构槽位本身是否值得继续。","change_type":"edit_sae_code","expected_win":"explore_unknown","family_name":"shared_product_key_expert_jumprelu","family_stage":"prototype","self_review":"这轮满足 Round 258 的核心约束：只做 1 个 architecture probe，并且相对 reference 只改 `ARCHITECTURE`。之所以不继续回到 `product_key_expert_jumprelu` 或 `expert_jumprelu` 上补参数点，是因为 round257 已经说明 `K=48` 这种同 family deploy-knee 延伸没有把 objective 拉过 r251；而 Round 258 明确要求切回结构槽位判断。之所以不选 `shared_two_stage_residual_expert`、`shared_lowrank_*`、`factorized_expert_topk` 这些已有 family，是因为它们在当前 target 的相近 matched lane 上都已经给过较清晰负信号；继续重复它们只会得到低信息增量。新 family 的价值在于它正好补上当前实现缺失的组合：`shared coarse stage` 先吃平滑主干，第二阶段不是 flat router，而是 `product-key` compositional routing 选 very-short-path micro-experts 做 residual cleanup。这比简单重测 `shared_two_stage_residual_expert` 更能回答 prompt 里关于 `product-key / compositional dictionary + shared/residual cleanup` 的核心问题。主要风险也很清楚：exact cost `0.1853x` 高于 r235 的 `0.1641x`，所以如果它不能把 exceed 至少压到约 `0.1733` 以下，就仍然不够构成正收益；但这正是本轮 probe 要干净回答的结构性问题。","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"shared_product_key_expert_jumprelu"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py","research/AutoResearch/override_registry.py"],"notes_to_memory":["已实际新增并注册 `shared_product_key_expert_jumprelu`，真实落盘文件只有 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`；没有新增 tunable env key，也没有改 `scripts/autoresearch_test.sh`。","新 family 的结构语义是 `shared coarse sparse stage + product-key routed JumpReLU residual experts`：这不是 `shared_two_stage_residual_expert` 的别名，因为第二阶段从 flat full router 换成了 compositional product-key expert addressing；也不是 `product_key_expert_jumprelu` 的别名，因为它显式增加了 always-on shared coarse branch。","本地 exact architecture smoke 已通过：`SparseCoderConfig(architecture='shared_product_key_expert_jumprelu', k=32, expansion_factor=1, num_experts=128, active_experts=2, latents_per_expert=192, stage1_ratio=0.25)` 在 CPU 上可成功 instantiate、forward、backward。","本地 override allowlist 自检已通过：`validate_env_overrides([{key:'ARCHITECTURE', value:'shared_product_key_expert_jumprelu'}], fallback_architecture='expert_jumprelu')` 返回空列表；连同 `STAGE1_RATIO/NUM_EXPERTS/ACTIVE_EXPERTS/LATENTS_PER_EXPERT` 的组合校验也返回空列表。","本地 exact 成本复核：`shared_product_key_expert_jumprelu K=32 EF=1 NUM_EXPERTS=128 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=192 STAGE1_RATIO=0.25` 的 selection_accesses=613376、deployment_accesses=163840、combined_accesses=777216、exact total_cost_ratio=0.185302734375；打赢 r235 exact objective=0.358553 所需阈值为 `exceed_alpha_0.50 < 0.173250265625`。"],"next_hypotheses":["如果 `shared_product_key_expert_jumprelu` 在 matched lane 上把 exceed 明显压低并接近或打赢 r235，则下一轮应继续留在该 family，只改 `STAGE1_RATIO`，判断 shared/coarse 与 PK residual cleanup 的预算分工是不是主控制旋钮。","如果它的 FVU 改善但 objective 仍不够，说明“shared coarse + compositional router”本身还不足以关闭当前 small-K exceed 缺口；后续若继续追这条线，应优先想办法进一步缩短 shared stage 或细化 micro-expert 容量，而不是回到 flat-router shared family 重复试验。","如果它在 objective 上仍明显输给 r235，尤其是 exceed 仍停在 `0.18+` 区间，则可更明确地下调 `shared/coarse + routed cleanup` 这条现有 family 族在当前 target 上的优先级；之后应把结构预算转向别的 genuinely new slot，而不是继续在 shared residual 变体里细扫。"],"reference_round":235}

--- STDERR ---
OpenAI Codex v0.117.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 019d47cf-f23e-7ef3-b0f3-e300794d8d23
--------
user
继续 LUTurbo 研究会话。当前轮次：Round 258。

返回一个符合 action schema 的 JSON 对象，不要使用 markdown 代码块。

务必显式包含全部 schema 字段；resume 模式下也不要省略 `needs_sanity`、`reference_round`、`notes_to_memory`、`next_hypotheses` 等字段。

提醒：每轮只改一个 env 参数；不要声明 primary_variable；param_only 默认应显式给出 reference_round。只有当前 target 冷启动且暂无 reference_round 时，才允许显式返回 reference_round=null，相对 target_profile_baseline 只改 1 个 env 参数。默认只改 sparsify/；只有在新增 tunable 参数或修复参数接线时，才允许改 research/AutoResearch/ 与 scripts/autoresearch_test.sh 的必要文件。如果返回 `edit_sae_code` / `edit_perf_code`，JSON 必须对应已经实际落盘的代码修改；不要只写计划。如果新增 env key，先本地确认 allowlist/wiring 已接通，再返回该 key。当前结果判定与 leaderboard 更新逻辑在 research/AutoResearch/controller.py，不在旧的 research/controller.py。当前单目标是 objective_score = total_cost_ratio + exceed_alpha_0.50；FVU 只作诊断。必须显式返回全部 schema 字段，即使为空也不要省略。最终返回 JSON。

LUTurbo 兼容性硬约束：
1. 提案前必须先写出最终重构公式，并说明它如何导出到 LUTurbo/Lottable 推理链路。
2. 每个 family 必须先判断兼容性：直接兼容 / 扩展兼容 / 不兼容。
3. 明确标记为不兼容的 family，不得继续占据当前 objective leaderboard，也不得继续作为主线推进。
4. 当前 leaderboard 只代表兼容 family 的 objective 排名；不兼容 family 只能作为历史参考，不能驱动后续决策。
当前 target：训练 hookpoint 为 layers.[3].self_attn.q_proj。
成本 proxy：train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output。
5. 成本硬约束：total_cost (encoder + deployment) 不得超过 1.5×h×n（原始 matmul 的 1.5 倍）。
   超过此阈值的配置将被 policy 拦截。
   降低 encoder 成本：减小 EXPANSION_FACTOR / TRUNK_RANK / NUM_CODES、使用低秩 scorer、探索非全字典选择机制。
   降低部署成本：减小 K / TRUNK_RANK / NUM_CODES；部署侧按静态库条目同时计入输入原子访问与输出查表结果。
   可通过 TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM 等 env_overrides 调节。
6. 当前单目标为 objective_score = total_cost_ratio + exceed_alpha_0.50；FVU 只作诊断与 tie-break，不再是主优化轴。

策略引导：
Round 258 策略模式：架构探针
原因：主线已经连续 5 轮没有改进，可以插入 1 个 matched architecture probe。
当前 objective anchor family：expert_jumprelu
当前 anchor 摘要：ARCHITECTURE=expert_jumprelu K=32 EXPANSION_FACTOR=1 OPTIMIZER=adam LR=8e-4
说明：这里只给 matched baseline / 归因校准用的紧凑 anchor。若本轮要做新的结构 probe，不要把它当默认 continuation plan；如需 param_only patch，完整 reference_round 配置见后文。
本轮要求：
1. 只允许做 1 个 matched architecture probe。
2. 保持主线的 K、OPTIMIZER、LR、EXPANSION_FACTOR 与主要 recipe 不变，只改变 architecture 本身。
3. probe 应优先用于可能显著降低 objective_score 的结构槽位，而不是继续在同一 family 上补局部插值点。
4. 这个 probe 只回答一个问题：该结构槽位本身值不值得继续。

兼容性注册表：直接兼容 12 个，扩展兼容 24 个，不兼容 3 个。

Agent 状态：round=257, consecutive_crashes=0, consecutive_no_improve=5, rounds_since_new_family=16

当前 target profile：
  training_hookpoint=layers.[3].self_attn.q_proj
  cost_proxy=train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output
  original_matmul=1024x4096 (4194304 accesses)

当前研究阶段：单目标 `objective_score = total_cost_ratio + exceed_alpha_0.50` 优先。
  当前要回答的是“谁能把 objective 真正降下来”，而不是“谁的 FVU 最低”或“谁只把 cost 压得更左”。
  当前距离 `objective<0.22` 仍差约 0.134；这已经不是抠几个千分点能解决的问题，需要显著改变当前 cost/exceed tradeoff 的结构或容量突破。
  当前已验证 incumbent 是 `expert_jumprelu` (K=32)，objective≈0.354491 = cost 0.16x + exceed 0.194491。
  如果最近 2 轮没有把 objective 明显拉低，下一轮默认优先换结构槽位，而不是继续做同 family 的局部插值。
  对 0.005~0.015 级别的小改进不要过度满足；在 `0.22` 目标下，这类改进通常只够校准，不够构成主线胜利。

当前高优先级方向：
  1. 新的 matched-objective architecture probe；优先回答新的结构槽位值不值得继续，而不是继续沿同一 family 做线性插值。
  2. 当前可优先关注 `total_cost_ratio≈0.14~0.20` 这一带；若新结构想占用更高 cost，必须能非常明确地换回更低 exceed。
  3. `shared bottleneck + expert-specific head`、expert 内部 low-rank、`shared low-rank trunk + routed residual experts`、以及更轻的 scorer / router。
  4. 不对称分工的 sparse MoE-like 结构：shared/coarse 分支吃平滑主干，routed experts 只做 residual cleanup，active path 尽量短。
  5. product-key / compositional routing 与 shared/residual cleanup 的组合；目标不是再省一点 router，而是让 hard tokens 的 exceed 更慢增长。
  6. variable-active path by difficulty：普通 token 保持极短路径，困难 token 才追加少量 expert / residual 分支。
  7. 能明显降低 exceed_alpha_0.50 的结构，即使 total_cost_ratio 有适度上升，只要 objective 更低且仍满足预算。
  8. 能在较小 K 下仍把 exceed 控住的结构，而不是只做线性降 K。
  9. 当前优先方向应同时包括：一是继续压榨 incumbent 主线附近真正高价值的 knee / 容量点；二是只在能明确解释为何会改善 small-K tradeoff 时才做新结构 probe。

当前应下调优先级的方向：
  1. 只改善 FVU、却没有改善 objective_score 的局部 recipe 微调。
  2. 连续在同一 family 上补 `K` / `LATENTS_PER_EXPERT` / rank / lr 插值点，但 objective 没有扩展的细扫。
  3. 任何明显抬高 total_cost_ratio、却没有换回更低 exceed 的结构探针。
  4. 只把 shared / routed 分支换个轻微变体、但没有改变容量与 active path 关系的低信息增量实验。
  5. 在当前目标差距仍很大时，还继续围绕 incumbent 做保守小步扫描。
  6. 把 reference_round 当成默认延续路线，而不是只把它当 patch anchor / 对照基线。
  7. 任何无法导出为静态子库有限加权和的 MoE / dynamic-dictionary 方向。

论文与结构灵感（当前优先消化）：
最值得吸收的 6 个方向：
1. `DeepSeekMoE`：shared experts + 更细粒度 expert 分片。
   来源：ACL 2024, DeepSeekMoE
   https://aclanthology.org/2024.acl-long.70/
   关键点：
   - 不是只做普通 top-k experts，而是同时把 experts 切得更细，并单独保留 shared experts 负责公共知识。
   - 可转成：`1 个 always-on shared/coarse 分支 + 很多很小的 routed micro-experts`。
   - 重点不是让单个 expert 更大，而是让 expert 更碎、更专门化；每个 token 仍只激活很短路径，但总容量显著变大。
   - 这和当前想要的“不对称分工”最贴近。

2. `Expert Choice Routing`：不是 token 选 expert，而是 expert 选 token。
   来源：Google Research Blog, Expert Choice Routing
   https://research.google/blog/mixture-of-experts-with-expert-choice-routing/?hl=pt_BR
   关键点：
   - 更均衡的 load balancing。
   - 每个 token 可以有可变数量的 experts。
   - 难 token 应允许更多 experts，简单 token 只用 1-2 个。
   - 对当前目标特别 relevant：可以把“小 K 基础路径 + 按难度追加少量 expert / 补偿路径”作为核心思路。
   - 这比固定所有 token 都用同样 `ACTIVE_EXPERTS` 更贴近真实目标。

3. `Product Key Memory`：两级 / 乘积式索引的大容量 memory。
   来源：NeurIPS 2019, Large Memory Layers with Product Keys
   https://papers.nips.cc/paper/9061-large-memory-layers-with-product-keys
   关键点：
   - 用 product keys 做超大 memory。
   - 容量很大，但访问成本仍然很低。
   - 不一定非要 flat top-k 直接从一大坨 latent 里选。
   - 可以做：`coarse codebook A × coarse codebook B -> 候选子库 -> 小 K 精选`。
   - 本质是组合式 dictionary / compositional memory，非常适合“小 K 但想保留大容量”。

4. `Hash Layers`：去掉 learned router，改成 hashing / balanced routing。
   来源：Hash Layers for Large Sparse Models
   https://huggingface.co/papers/2106.04426
   关键点：
   - 不需要额外 routing 参数。
   - 不需要复杂 load balancing loss。
   - 仍然能和 learned routing 竞争。
   - 若 router/scorer 成本不低或训练不稳定，可以试：`hash/bucket -> bucket 内局部 scorer/top-k`。
   - 甚至可以试固定分桶 + 轻量 re-score。

5. `Omni-SMoLA`：shared backbone + 轻量 low-rank experts 做 residual specialization。
   来源：Google Research, Omni-SMoLA
   https://research.google/pubs/omni-smola-boosting-generalist-multimodal-models-with-soft-mixture-of-low-rank-experts/
   关键点：
   - 不复制完整 experts。
   - 用很多轻量 low-rank experts 残差式地补 backbone。
   - 可转成：`shared low-rank trunk + many tiny low-rank expert residuals`。
   - 每个 expert 不是完整 `d_in -> latents`，而是 rank 很小的 residual adapter。

6. Activation sparsity / Top-K thresholding 本身可能带来更稳的性质。
   来源：Google Research, On Emergence of Activation Sparsity in Trained Transformers
   https://research.google/pubs/on-emergence-of-activation-sparsity-in-trained-transformers/
   关键点：
   - transformer 里的 activation sparsity 是自然出现的。
   - 更稀疏的 Top-K thresholding 可能带来更好的鲁棒性 / 校准。
   - 小 K 不一定只是“被迫压成本”，也可能是结构先验的一部分。
   - 关键是要有足够好的 coarse/shared/trunk 去兜住主干，让小 K 只负责 tail。

当前最值得优先尝试的 4 个具体结构想法：
1. `shared coarse library + routed micro-experts`
   - 灵感来自 DeepSeekMoE。
   - shared 分支吃公共部分，routed experts 切得很细，每次只开 1-2 个。
2. `variable-active-experts by difficulty`
   - 灵感来自 Expert Choice。
   - 不是所有 token 都固定 2 experts，而是简单 token 1 个，难 token 2-4 个。
3. `product-key / compositional dictionary`
   - 灵感来自 PKM。
   - 先粗定位，再小 K 精选，换容量而不线性增加 deploy lookup。
4. `shared low-rank trunk + tiny low-rank expert residuals`
   - 灵感来自 Omni-SMoLA。
   - 不是 full expert，而是很多 rank 很小的 expert adapters。

一个重要判断：
- 这些灵感里，和当前目标最匹配的，不是“对称多专家一起干活”，而是：
- `shared / coarse / trunk` 负责主干。
- `tiny routed experts` 负责难点 / residual / tail cleanup。
- `variable active path` 只在困难样本上打开。
- 这和当前 `objective = cost + exceed` 很一致，因为它本质上就是在最小化：
- 普通 token 的基础成本。
- 困难 token 的额外补偿需求。

别名与复用提醒：
- 提出 literature-inspired probe 前，先检查它是不是已经被现有 family 近似覆盖，只是名字不同。
- 例如：`shared coarse + residual experts` 常接近 `shared_two_stage_residual_expert`。
- `shared low-rank trunk + routed experts` 常接近 `shared_lowrank_routed_expert_topk` 或其 residual 变体。
- `cheap trunk + routed experts` 常接近 `lowrank_expert_topk`。
- `shared expert + routed experts` 常接近 `shared_routed_expert_topk`。
- 如果只是重命名或轻微同构变体，不要当成 brand-new architecture；必须明确写出与现有实现的精确差异。

补充一个基线教训：
- `Switch Transformer` 的启发是：很多时候简单路由先赢。
  https://huggingface.co/papers/2101.03961
- 所以如果要加复杂结构，仍应尽量保持：
- `very short active path`
- `very cheap router`
- `very clean decomposition`


训练代理 leaderboard（single objective: objective_score = total_cost_ratio + exceed_alpha_0.50；FVU 仅诊断）：
  r235 objective=0.3544909989833832 = cost 0.16x + exceed 0.19449099898338318 | fvu=0.1710990071296692 | arch=expert_jumprelu K=32 EF=1 | FEASIBLE
  r236 objective=0.36131738901138305 = cost 0.18x + exceed 0.18131738901138306 | fvu=0.1570778787136078 | arch=expert_jumprelu K=48 EF=1 | FEASIBLE

目标状态：
  当前 incumbent → expert_jumprelu (K=32, EF=1):
    objective=0.3544909989833832 = cost 0.16x + exceed 0.19449099898338318 | encoder=0.12x | deploy=0.04x | fvu=0.1710990071296692
  最低 exceed leader → expert_jumprelu (K=48, EF=1):
    objective=0.36131738901138305 = cost 0.18x + exceed 0.18131738901138306 | encoder=0.12x | deploy=0.06x | fvu=0.1570778787136078
  单目标：objective_score = total_cost_ratio + exceed_alpha_0.50
  降低 encoder 成本: 降 EF / TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM
  降低部署成本: 降 K / TRUNK_RANK / NUM_CODES
  降低 exceed_alpha_0.50: 优先减少 tail error / online compensation 需求，而不是只看最终 FVU

阶段目标：把 objective_score 压到 0.22 以下。
  当前 incumbent 约为 objective=0.354491 (cost=0.16x, exceed=0.194491)；距离目标仍差 0.134491。
  这意味着后续实验不能只满足于很小的局部改进；需要明确回答“这轮是否在逼近 0.22 所需的 tradeoff”。
  若想达到该目标，不同 cost 档位大致需要满足：
    - 若 cost≈0.14x，则 exceed 需压到 ≤0.08
    - 若 cost≈0.16x，则 exceed 需压到 ≤0.06
    - 若 cost≈0.18x，则 exceed 需压到 ≤0.04
    - 若 cost≈0.20x，则 exceed 需压到 ≤0.02
  因此：若新结构 cost 升到约 0.20x，却仍无法把 exceed 压到约 0.02 附近，则通常不够支撑 0.22 目标。
  只有那些明显缩小这一缺口的结构 probe，才值得获得后续 follow-up 预算。

当前成本 profile：hookpoint=layers.[3].self_attn.q_proj | proxy=1024x4096
基础 family 的 Encoder 侧成本速查表（非最终 budget，ratio 指 encoder-only）：
  架构                                        EF=1    EF=2    EF=3    EF=4    EF=6    EF=8    EF=12 
  topk                                      0.25x+   0.5x+    0.75x+   1.0x+    1.5x+    2.0x-    3.0x-  
  gated                                     0.5x+    1.0x+    1.5x+    2.0x-    3.0x-    4.0x-    6.0x-  
  factorized_topk                           0.25x+   0.38x+   0.5x+    0.62x+   0.88x+   1.12x+   1.62x- 
  lowrank_residual                          0.38x+   0.62x+   0.88x+   1.12x+   1.62x-   2.12x-   3.12x- 
  lowrank_soft_codebook_residual            0.41x+   0.66x+   0.91x+   1.16x+   1.66x-   2.16x-   3.16x- 
  lowrank_two_stage_soft_codebook_residual  0.66x+   1.16x+   1.66x-   2.16x-   3.16x-   4.16x-   6.16x- 
  注意：以上仅为 encoder 选择成本（不依赖 K）。最终 budget 以 total_cost = selection + deployment 为准。
  部署侧 sparse 路径额外贡献约：K=32 +4%，K=64 +8%，K=128 +16%（按 K×(d+n) 近似）。
  说明：本表只覆盖基础 family，不覆盖 expert / routed / shared+routed 家族。
  这些家族的 encoder 成本还受 NUM_EXPERTS / ACTIVE_EXPERTS / LATENTS_PER_EXPERT / FACTORIZED_HIDDEN_DIM 等影响，应优先参考 leaderboard 条目与 reference_round 完整配置。

最近几轮（兼容架构）：
  （最近没有兼容 family 的新结果）

操作提示：
  1. 当前主目标仍是 `objective_score = total_cost_ratio + exceed_alpha_0.50`。判断成败先看 objective，再拆看 cost 与 exceed；不要把 low-cost 或 low-FVU 单独当成成功。新的阶段目标已经更新为把 objective 压向 `0.22` 以下；以当前 best lane 粗看，这通常要求把 `cost≈0.12~0.14x` 同时配上 `exceed≈0.08~0.10`，明显不是 incumbent 邻域的小步微调能自然得到的。若多轮结果仍停留在 `0.28~0.33` 一带，应优先寻找能带来 regime shift 的结构，而不是继续做温和插值。
  2. 当前仍要重点回答“怎样在更小 `K` 下控制 `exceed_alpha_0.50`，并把 objective 真正压向 `0.22`”。如果某条复杂结构不能在 small-K 主战场里显著减缓 exceed 的退化，它就不值得保留。相比继续围绕 incumbent 做低信息细扫，更值得测试的是：更细粒度 experts、shared bottleneck + expert-specific head、expert 内部 low-rank、variable-active-experts by difficulty、以及 coarse-to-fine candidate selection 这类可能改变 small-K tradeoff 的结构。

最近可用 reference_round 的完整配置（仅作 patch anchor / 对照基线，不代表默认延续路线）：
  只有当你明确选择 param_only patch 时才需要这些完整配置；若本轮是新结构 probe，不必围绕这些点继续微调。
  r235 expert_jumprelu (decision=frontier, source=frontier_entry)
  active_experts=2
  architecture=expert_jumprelu
  auxk_alpha=0.03125
  batch_size=1
  cost_model_label=train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output
  d_in=1024
  dead_feature_threshold=10000000
  expansion_factor=1
  factorized_hidden_dim=256
  family_name=expert_jumprelu
  family_stage=prototype
  grad_acc_steps=8
  hookpoints=layers.[3].self_attn.q_proj
  k=32
  latents_per_expert=192
  lr=8e-4
  micro_acc_steps=1
  n_output=4096
  num_experts=128
  optimizer=adam
  original_matmul_accesses=4194304
  stage1_ratio=0.25
  target_profile={'profile_id': 'qwen3_0p6b_layer3_q_proj_fused_qkv_proxy_v1', 'training_hookpoint': 'layers.[3].self_attn.q_proj', 'd_in': 1024, 'n_output': 4096, 'elbow_threshold_path': 'thresholds/Qwen3-0.6B/thresholds_q.json', 'cost_model_label': 'train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output', 'original_matmul_accesses': 4194304}
  trunk_rank=16
  use_hadamard=False

当前背景文档（弱先验，不是结论）：
兼容性表已省略；具体 compatibility 以当前 runtime registry 与 hard rules 为准。

## 1. 文档用途

这份文档的作用不是告诉 Agent “当前最优主线是什么”，而是提供一份低偏置背景说明。

应这样使用它：

- 把它当成兼容性约束、工程不变量、和未决问题清单
- 不要把旧位置、旧轮次、旧 leaderboard 的 family 排名当成当前 target 的默认真理
- 不要把这里出现的方向理解成固定 opening order
- 当前 target 上的结论，必须依赖当前 target 自己的新结果

当前固定事实：

- 训练 hookpoint 固定为 `layers.[3].self_attn.q_proj`
- 成本 proxy 固定按 fused QKV 部署矩阵 `1024 x 4096` 统计
- 成本硬约束固定为 `total_cost <= 1.5 x (1024 x 4096)`
- 当前单目标固定为 `objective_score = total_cost_ratio + exceed_alpha_0.50`
- 其中 `total_cost_ratio = total_cost / (1024 x 4096)`
- `FVU` 继续保留，但只作诊断与 tie-break，不再是主优化轴

一句话：现在要优化的是“总部署代理成本 + 在线补偿需求”，不是单独的 low-cost，也不是单独的 low-FVU。

## 1.1 当前 target 的阅读方式

下面这些是当前 target 的阅读方式，不是结论：

- 比较两个候选时，先拆开看 `total_cost_ratio` 和 `exceed_alpha_0.50`，再看它们合成后的 `objective_score`
- 如果一个结构让 cost 略升，但 exceed 明显下降，只要 objective 更低且仍在预算内，就值得保留
- 如果一个结构只让 FVU 更低，但 objective 没降，就不应当作主要成功
- 不要把“更靠左”或“更低 FVU”误当成新的全局主线；真正的 incumbent 只由 objective 决定
- 连续多轮只在同一 family 上做 `K / LATENTS_PER_EXPERT / rank / lr` 小插值、而 objective 没有扩展时，应优先换结构槽位

## 3. 可信的继承项

下面这些可以从旧 run 继承，因为它们是工程不变量，不依赖旧位置的性能结论：

- 单变量原则必须继续执行；`param_only` 一次只改一个 env 参数
- 新增 tunable 参数必须打通 `sparsify/ + override_registry + config_resolution + runner + scripts/autoresearch_test.sh`
- 第一轮引入新参数后，必须检查 round config 与 checkpoint config 中该参数真的生效
- invalid round、silent fallback、schema mismatch、launcher 未透传，这些问题会直接污染结论
- `total_cost` 必须拆成 encoder 与 deployment 两部分看
- `objective_score` 必须拆成 `total_cost_ratio` 与 `exceed_alpha_0.50` 两部分看
- compatibility 与 performance 是两回事；结构兼容不代表一定值得继续

## 4. 不可信的旧结论

下面这些都不应直接继承到当前 target：

- 旧位置上的 family 排名
- 旧位置上的最优点或历史“objective 分布形状”
- “复杂 backbone 一定优于简单结构”这类总括判断
- “简单结构已经证伪”这类总括判断
- “当前主战场一定是某个固定 low-cost 区间”这类结论
- 对 whitening / codebook / lowrank / two-stage / routed / MoE-like 的好坏预判
- 某个 family 在旧位置失败，就默认它在新位置也失败
- 某个 family 在旧位置表现强，就默认它在新位置仍是 opening 主线
- 固定 opening order、固定主线 family、固定 deferred family 列表

一句话：旧 run 只能提供工程经验和搜索空间，不能提供当前 target 的性能真理。

## 5. 当前 target 上真正未知的事

这些问题都还没有在当前 target 上被证明：

- 哪类结构最能降低当前 `objective_score`
- 当前 incumbent 的主要瓶颈更常见地来自 cost，还是来自 exceed
- 允许适度增 cost 来换更低 exceed 的最佳交换率大致在哪
- `shared bottleneck + expert-specific head` 是否比现有 shared+routed recipe 更高效
- expert 内部 low-rank、或 `shared low-rank trunk + routed residual experts` 是否能给出更好的 objective
- 哪些结构能在更小 `K` 下仍控制住 `exceed_alpha_0.50`
- preprocessing / loss 切换会不会比结构切换更重要
- asymmetric MoE-like / routed 子库方案能否在保持导出约束的前提下形成更低 objective 的新点

Agent 应把这些当成未决问题，而不是已有答案。

## 6. 当前高优先级方向

当前优先级最高的方向不是“默认继续压某个固定成本区间”，而是明确围绕单目标做结构性探索：

- 新的 matched-objective architecture probe，而不是继续沿同一 family 做线性插值
- 不对称分工的 sparse MoE-like 结构：shared/coarse 分支吃平滑主干，routed experts 只做 residual cleanup，active path 尽量短
- `shared bottleneck + expert-specific head`
- expert 内部 low-rank
- `shared low-rank trunk + routed residual experts`
- 更轻的 scorer / router
- 能在较小 `K` 下仍把 `exceed_alpha_0.50` 控住的结构
- 宁可做中等风险但高信息增量的新原型，也不要把预算继续花在 incumbent 邻域的低信息细扫上

这些方向优先级高，不是因为它们已经被证明最好，而是因为：

- 它们更直接回答“怎样同时控制 cost 与 exceed”
- 它们更可能带来新的结构信息，而不是同一 recipe 上的局部插值
- 它们能测试“容量是否能增大，但 active path 仍保持很短”这个核心问题
- 当 objective 长时间不动时，它们比继续补 `K / rank / lr / LATENTS_PER_EXPERT` 插值更有信息价值

如果要实现新的原型，应优先满足：

- `ACTIVE_EXPERTS` 很小，例如 1 或 2
- router 足够轻，不要把省下来的选择成本重新吃掉
- 总激活路径仍短，不要因为多 expert 让总 `K` 无限制膨胀
- shared / coarse 分支若存在，应主要负责平滑主干，不要和 routed experts 对称地共同承担主表示
- 最终形式仍能写成若干静态子库上的有限加权和

## 7. 当前默认做法

如果当前 target 还没有足够本地证据，默认做法应当是：

- 先建立当前 target 自己的 objective anchors
- 再根据当前 target 自己的 leaderboard 形状决定下一步往哪里扩
- incumbent 只当 matched baseline / objective anchor，不要把它自动理解成默认 continuation plan
- 优先选择归因清晰、能补充新结构信息的实验；在工程可控的前提下，允许选择中等接线风险的新结构原型
- 如果最近 2 轮都属于同一 family 且只是局部参数插值，同时 objective 没有明显扩展，则下一轮默认应优先换结构槽位
- 继续留在同一 family 的前提是：你能明确指出还有哪个高价值、未验证、且足以改变 objective 结论的结构假设
- 如果一个方向只让 FVU 下降、却没有让 objective 下降，不应继续主导预算
- 如果一个方向让 cost 上升，但 exceed 明显下降且 objective 更低，则可以继续
- 不要让旧位置的主线故事替代当前 target 的新证据
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
codex
继续会话先核对 `round_0257`、`frontier.json` 和当前兼容家族成本，确认这轮架构探针该围绕哪个真实 reference 做 matched probe，而不是沿用过时 anchor。
exec
/bin/bash -lc "python - <<'PY'
import json
for p in ['research/history/round_summaries/round_0257.json','research/history/frontier.json']:
    print('\\n==',p)
    d=json.load(open(p))
    if p.endswith('0257.json'):
        print(json.dumps({
            'round': d['round'],
            'action': d['action'],
            'result': d['result'],
            'resolved_reference_env_config': d.get('resolved_reference_env_config'),
            'resolved_candidate_env_config': d.get('resolved_candidate_env_config'),
        }, ensure_ascii=False, indent=2)[:12000])
    else:
        print(json.dumps(d, ensure_ascii=False, indent=2)[:5000])
PY" in /root/sparsify-ascend
 succeeded in 0ms:

== research/history/round_summaries/round_0257.json
{
  "round": 257,
  "action": {
    "command": "run",
    "hypothesis": "最终重构公式保持为 x_hat = Σ_{e∈E_pk(x)} Σ_{i∈S_e^τ(x)} α_{e,i}(x) b_{e,i}。其中 {b_{e,i}} 是静态 expert 子库原子，E_pk(x) 由 left/right product-key router 给出：logit_e(x)=l_{a(e)}(x)+r_{b(e)}(x)，再选出极少量 active experts；expert 内继续用 JumpReLU 形成局部 support。导出到 LUTurbo/Lottable 的在线链路仍是“left/right product-key 打分 -> 选择少量静态 expert 子库 -> expert 内 sparse support -> 查表加权和 -> 选择性补偿”，静态库不随样本动态生成，因此该 family 继续判定为直接兼容。当前工作区里真实兼容 incumbent 是 r251 `product_key_expert_jumprelu K=32 NUM_EXPERTS=512 LATENTS_PER_EXPERT=96 ACTIVE_EXPERTS=2`，exact objective=0.290692=cost 0.097168x + exceed 0.193524；它的 objective 已明显由 exceed 主导而不是 cost 主导，且训练曲线 `0.240827 -> 0.196897 -> 0.175105` 仍显示 `curve_still_improving=true`。r252-r256 已依次给出 bank 下压、单 expert path、hash router、cheap flat router、shared low-rank coarse cleanup 的负信号，因此当前最高信息量的单变量动作不再是换一个更贵 architecture，而是直接回答当前 PK 主线是否已受 small-K deploy budget 限制。相对 r251 只把 `K` 从 `32` 提到 `48`，保持 `ARCHITECTURE=product_key_expert_jumprelu`、`NUM_EXPERTS=512`、`LATENTS_PER_EXPERT=96`、`ACTIVE_EXPERTS=2`、`EXPANSION_FACTOR=1`、`adam`、`LR=8e-4` 与其余 recipe 不变，做一次 deployment-capacity knee probe。该点本地精确成本为 selection_accesses=243712、deployment_accesses=245760、combined_accesses=489472、exact total_cost_ratio=0.11669921875；因此只需把 exceed_alpha_0.50 压到 0.173993 以下就能打赢 r251 的 exact objective。这比继续切到当前已知 exact 成本 ≥0.144x~0.186x 的其他 family 更现实，也更直接回答“当前 frontier 的瓶颈究竟是不是 small-K lookup capacity”。",
    "summary": "建议以 r251 为 reference，只改 `K=48`。这是对当前真实兼容主线 `product_key_expert_jumprelu` 的单变量 deployment-knee probe：当前 incumbent 明显 exceed-led，且 recent architecture probes 已基本证明问题不在于继续更换 router/coarse family；因此先验证在保持最强 PK recipe 不变时，适度增加 sparse lookup budget 能否把 exceed 压回到新的 objective 低点。",
    "change_type": "param_only",
    "expected_win": "lower_objective",
    "family_name": "product_key_expert_jumprelu",
    "family_stage": "mainline",
    "self_review": "这轮继续留在同一 family 有充分依据，不属于低信息量细扫。最近 4 轮无改进并不意味着必须机械地继续开新 family；更关键的是 recent new-family probes 已经把当前最像样的替代槽位裁过一遍，而且它们在当前 recipe 下的 exact 成本都明显高于 PK 主线，却没有换回足够低的 exceed。相对 r251 只改 `K`，归因非常干净：selection cost 完全不变，只测试 deploy lookup capacity 是否是当前 small-K tradeoff 的主瓶颈。主要风险是 exceed 改善不足 0.0195，导致 objective 仍不如 r251；但即使如此，这一轮也能高质量地回答“当前 winning family 还剩不剩 deploy-knee 空间”，其信息价值高于继续切向又一个更贵的 architecture closure probe。",
    "needs_sanity": false,
    "env_overrides": [
      {
        "key": "K",
        "value": "48"
      }
    ],
    "touched_files": [],
    "notes_to_memory": [
      "工作区真实兼容 frontier 仍以 `research/history/frontier.json` 为准：r251 `product_key_expert_jumprelu` 是当前 best compatible objective 点；prompt 里的旧 `expert_jumprelu` anchor 已过时，不应继续当默认主线。",
      "r251 的 exact 指标为 objective=0.290692、cost=0.097168x、selection=0.058105x、deploy=0.039062x、exceed=0.193524；当前 objective 主导项是 exceed，不是 cost。",
      "r252-r256 已连续给出负信号：`NUM_EXPERTS=256`、`ACTIVE_EXPERTS=1`、`hashed_expert_jumprelu`、`factorized_router_expert_topk`、`shared_lowrank_two_stage_residual_expert` 都没有把 objective 拉回 r251 附近，因此下一步最值得先回答的是 PK 主线是否受 `K=32` 的 deploy budget 限制。",
      "本地自检通过：`validate_env_overrides([{key:'K', value:'48'}], fallback_architecture='product_key_expert_jumprelu')` 返回空列表。",
      "本地精确成本复核：`product_key_expert_jumprelu K=48 NUM_EXPERTS=512 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=96` 的 selection_accesses=243712、deployment_accesses=245760、combined_accesses=489472、exact total_cost_ratio=0.11669921875；打赢 r251 exact objective 所需阈值为 `exceed_alpha_0.50 < 0.17399311210441587`。"
    ],
    "next_hypotheses": [
      "如果 `K=48` 明显降低 objective，下一轮继续留在 `product_key_expert_jumprelu`，只改 `K=64` 或回补一个 `K=40`/`K=56` 中间点，定位当前 PK 主线的 deploy knee，而不是再切回已给负信号的 router/coarse family。",
      "如果 `K=48` 只带来很小 exceed 改善、无法覆盖新增 deploy cost，则可更明确地判定当前 PK 主线已经不是 lookup-limited；后续应停止在同 family 上继续开 K 轴，转向真正新的结构原型，而不是再做温和插值。",
      "如果 `K=48` 让 FVU 改善但 objective 仍无竞争力，则把它记为诊断性信号：当前距离 `0.22` 的缺口不能靠单纯增加 sparse lookup budget 解决，后续需要新的容量组织方式。"
    ],
    "reference_round": 251
  },
  "result": {
    "decision": "archive",
    "status": "ok",
    "timestamp": "1775027344",
    "val_fvu": "0.16268384456634521",
    "exceed_alpha_0_50": "0.180935",
    "objective_score": "0.297634",
    "total_cost": "489472.000000",
    "total_cost_ratio": "0.116699",
    "selection_cost": "243712.000000",
    "selection_cost_ratio": "0.058105",
    "deployment_accesses": "245760.000000",
    "deployment_ratio": "0.058594",
    "observed_fvu": "0.162684",
    "k": "48",
    "architecture": "product_key_expert_jumprelu",
    "expansion_factor": "1",
    "run_health": "normal",
    "termination_reason": "completed",
    "tokens_per_sec": "55519.462912",
    "baseline_ratio": "0.499944",
    "checkpoint": "/root/sparsify-ascend/checkpoints/research_agent/round_0257/round0257_standard_1775026645_dp2_bs1_ga8_ef1_k48_20260401_145731",
    "experiment_id": "r257_1775027344",
    "head_commit": "8d33f69e34f2f2d2725eb6e2d1099766f1adbf2b",
    "description": "建议以 r251 为 reference，只改 `K=48`。这是对当前真实兼容主线 `product_key_expert_jumprelu` 的单变量 deployment-knee probe：当前 incumbent 明显 exceed-led，且 recent architecture probes 已基本证明问题不在于继续更换 router/coarse family；因此先验证在保持最强 PK recipe 不变时，适度增加 sparse lookup budget 能否把 exceed 压回到新的 objective 低点。",
    "self_review": "这轮继续留在同一 family 有充分依据，不属于低信息量细扫。最近 4 轮无改进并不意味着必须机械地继续开新 family；更关键的是 recent new-family probes 已经把当前最像样的替代槽位裁过一遍，而且它们在当前 recipe 下的 exact 成本都明显高于 PK 主线，却没有换回足够低的 exceed。相对 r251 只改 `K`，归因非常干净：selection cost 完全不变，只测试 deploy lookup capacity 是否是当前 small-K tradeoff 的主瓶颈。主要风险是 exceed 改善不足 0.0195，导致 objective 仍不如 r251；但即使如此，这一轮也能高质量地回答“当前 winning family 还剩不剩 deploy-knee 空间”，其信息价值高于继续切向又一个更贵的 architecture closure probe。",
    "log_path": "/root/sparsify-ascend/research/history/logs/round0257_standard_1775026645.log",
    "metrics_path": "/root/sparsify-ascend/checkpoints/research_agent/round_0257/round0257_standard_1775026645_dp2_bs1_ga8_ef1_k48_20260401_145731/metrics.jsonl",
    "curve_start_fvu": "0.224450",
    "curve_mid_fvu": "0.183076",
    "curve_end_fvu": "0.162684",
    "curve_late_slope": "-0.000000",
    "curve_still_improving": "true",
    "initial_target_tokens": "30000000",
    "final_target_tokens": "50000000",
    "final_total_tokens": "49979392",
    "resume_stage_count": "3",
    "continuation_stop_reason": "plateau",
    "continued_from_checkpoint": "true"
  },
  "resolved_reference_env_config": {
    "ARCHITECTURE": "product_key_expert_jumprelu",
    "EXPANSION_FACTOR": "1",
    "K": "32",
    "HOOKPOINTS": "layers.[3].self_attn.q_proj",
    "OPTIMIZER": "adam",
    "LR": "8e-4",
    "BATCH_SIZE": "1",
    "GRAD_ACC_STEPS": "8",
    "MICRO_ACC_STEPS": "1",
    "AUXK_ALPHA": "0.03125",
    "DEAD_FEATURE_THRESHOLD": "10000000",
    "USE_HADAMARD": "0",
    "COMPILE_MODEL": "1",
    "FACTORIZED_HIDDEN_DIM": "256",
    "TRUNK_RANK": "16",
    "STAGE1_RATIO": "0.25",
    "NUM_EXPERTS": "512",
    "LATENTS_PER_EXPERT": "96",
    "ACTIVE_EXPERTS": "2"
  },
  "resolved_candidate_env_config": {
    "ARCHITECTURE": "product_key_expert_jumprelu",
    "EXPANSION_FACTOR": "1",
    "K": "48",
    "HOOKPOINTS": "layers.[3].self_attn.q_proj",
    "OPTIMIZER": "adam",
    "LR": "8e-4",
    "BATCH_SIZE": "1",
    "GRAD_ACC_STEPS": "8",
    "MICRO_ACC_STEPS": "1",
    "AUXK_ALPHA": "0.03125",
    "DEAD_FEATURE_THRESHOLD": "10000000",
    "USE_HADAMARD": "0",
    "COMPILE_MODEL": "1",
    "FACTORIZED_HIDDEN_DIM": "256",
    "TRUNK_RANK": "16",
    "STAGE1_RATIO": "0.25",
    "NUM_EXPERTS": "512",
    "LATENTS_PER_EXPERT": "96",
    "ACTIVE_EXPERTS": "2"
  }
}

== research/history/frontier.json
{
  "r251": {
    "objective_score": 0.29352433085441587,
    "total_cost": 407552.0,
    "total_cost_ratio": 0.1,
    "selection_cost": 243712.0,
    "selection_cost_ratio": 0.06,
    "deployment_accesses": 163840.0,
    "deployment_ratio": 0.04,
    "exceed_alpha_0_50": 0.1935243308544159,
    "fvu": 0.175105020403862,
    "k": 32,
    "ef": 1,
    "architecture": "product_key_expert_jumprelu",
    "commit": "825191378b5f831d599abb72e22c06f646a9b4e2",
    "config": {
      "architecture": "product_key_expert_jumprelu",
      "expansion_factor": 1,
      "k": 32,
      "optimizer": "adam",
      "lr": "8e-4",
      "hookpoints": "layers.[3].self_attn.q_proj",
      "batch_size": 1,
      "grad_acc_steps": 8,
      "micro_acc_steps": 1,
      "auxk_alpha": 0.03125,
      "dead_feature_threshold": 10000000,
      "use_hadamard": false,
      "family_name": "product_key_expert_jumprelu",
      "family_stage": "mainline",
      "target_profile": {
        "profile_id": "qwen3_0p6b_layer3_q_proj_fused_qkv_proxy_v1",
        "training_hookpoint": "layers.[3].self_attn.q_proj",
        "d_in": 1024,
        "n_output": 4096,
        "elbow_threshold_path": "thresholds/Qwen3-0.6B/thresholds_q.json",
        "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
        "original_matmul_accesses": 4194304
      },
      "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
      "d_in": 1024,
      "n_output": 4096,
      "original_matmul_accesses": 4194304,
      "trunk_rank": 16,
      "stage1_ratio": 0.25,
      "factorized_hidden_dim": 256,
      "num_experts": 512,
      "active_experts": 2,
      "latents_per_expert": 96
    },
    "checkpoint": "/root/sparsify-ascend/checkpoints/research_agent/round_0251/round0251_standard_1775021981_dp2_bs1_ga8_ef1_k32_20260401_133947",
    "peak_memory_gb": null,
    "target_profile": {
      "profile_id": "qwen3_0p6b_layer3_q_proj_fused_qkv_proxy_v1",
      "training_hookpoint": "layers.[3].self_attn.q_proj",
      "d_in": 1024,
      "n_output": 4096,
      "elbow_threshold_path": "thresholds/Qwen3-0.6B/thresholds_q.json",
      "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
      "original_matmul_accesses": 4194304
    },
    "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
    "metric_version": "objective_v2_full_deploy"
  },
  "r252": {
    "objective_score": 0.29979407429695126,
    "total_cost": 393216.0,
    "total_cost_ratio": 0.09,
    "selection_cost": 229376.0,
    "selection_cost_ratio": 0.05,
    "deployment_accesses": 163840.0,
    "deployment_ratio": 0.04,
    "exceed_alpha_0_50": 0.2097940742969513,
    "fvu": 0.18915528059005737,
    "k": 32,
    "ef": 1,
    "architecture": "product_key_expert_jumprelu",
    "commit": "f383069ec06ba29f8ae18c543b65357b239ab397",
    "config": {
      "architecture": "product_key_expert_jumprelu",
      "expansion_factor": 1,
      "k": 32,
      "optimizer": "adam",
      "lr": "8e-4",
      "hookpoints": "layers.[3].self_attn.q_proj",
      "batch_size": 1,
      "grad_acc_steps": 8,
      "micro_acc_steps": 1,
      "auxk_alpha": 0.03125,
      "dead_feature_threshold": 10000000,
      "use_hadamard": false,
      "family_name": "product_key_expert_jumprelu",
      "family_stage": "mainline",
      "target_profile": {
        "profile_id": "qwen3_0p6b_layer3_q_proj_fused_qkv_proxy_v1",
        "training_hookpoint": "layers.[3].self_attn.q_proj",
        "d_in": 1024,
        "n_output": 4096,
        "elbow_threshold_path": "thresholds/Qwen3-0.6B/thresholds_q.json",
        "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
        "original_matmul_accesses": 4194304
      },
      "cost_model_label": "train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096 and counts deploy libraries as d_in+n_output",
      "d_in": 1024,
      "n_output": 4096,
      "original_matmul_accesses": 4194304,
      "trunk_rank": 16,
      "stage1_ratio": 0.25,
      "factorized_hidden_dim": 256,
      "num_experts": 256,
      "active_experts": 2,
      "latents_per_expert": 96
    },
    "checkpoint": "/root/sparsify-ascend/checkpoints/research_agent/round_0252/round0252_standard_1775022762_dp2_bs1_ga8_ef1_k32_20260401_135249",
    "peak_memory_gb": null,
    "target_profile": {
      "profile_id": "qwen3_0p6b_layer3_q_proj_fused_qkv_proxy_v1",
      "training_hookpoint": "layers.[3].self_attn.q_proj",
      "d_in": 1024,
      "n_output": 4096,
      "elbow_threshold_path": "thresholds/Qwen3-0.6B/thresholds_q.json",
      

exec
/bin/bash -lc "python - <<'PY'
import json,glob
for path in sorted(glob.glob('research/history/round_summaries/round_02*.json'))[-12:]:
    d=json.load(open(path))
    a=d.get('action',{})
    r=d.get('result',{})
    print(d['round'], a.get('family_name'), a.get('env_overrides'), '=>', r.get('decision'), r.get('objective_score'), r.get('total_cost_ratio'), r.get('exceed_alpha_0_50'), r.get('architecture'))
PY" in /root/sparsify-ascend
 succeeded in 0ms:
246 product_key_expert_jumprelu [{'key': 'NUM_EXPERTS', 'value': '512'}] => keep 0.318433 0.144043 0.174390 product_key_expert_jumprelu
247 product_key_expert_jumprelu [{'key': 'LATENTS_PER_EXPERT', 'value': '160'}] => keep 0.307648 0.128418 0.179230 product_key_expert_jumprelu
248 product_key_expert_jumprelu [{'key': 'NUM_EXPERTS', 'value': '1024'}] => keep 0.307962 0.132812 0.175150 product_key_expert_jumprelu
249 product_key_expert_jumprelu [{'key': 'LATENTS_PER_EXPERT', 'value': '128'}] => keep 0.294943 0.117188 0.177755 product_key_expert_jumprelu
250 product_key_expert_jumprelu [{'key': 'NUM_EXPERTS', 'value': '512'}] => keep 0.297123 0.112793 0.184330 product_key_expert_jumprelu
251 product_key_expert_jumprelu [{'key': 'LATENTS_PER_EXPERT', 'value': '96'}] => keep 0.290692 0.097168 0.193524 product_key_expert_jumprelu
252 product_key_expert_jumprelu [{'key': 'NUM_EXPERTS', 'value': '256'}] => keep 0.303544 0.093750 0.209794 product_key_expert_jumprelu
253 product_key_expert_jumprelu [{'key': 'ACTIVE_EXPERTS', 'value': '1'}] => archive 0.371793 0.073730 0.298063 product_key_expert_jumprelu
254 hashed_expert_jumprelu [{'key': 'ARCHITECTURE', 'value': 'hashed_expert_jumprelu'}] => discard 0.393051 0.090332 0.302719 hashed_expert_jumprelu
255 factorized_router_expert_topk [{'key': 'ARCHITECTURE', 'value': 'factorized_router_expert_topk'}] => discard 0.411181 0.179688 0.231494 factorized_router_expert_topk
256 shared_lowrank_two_stage_residual_expert [{'key': 'ARCHITECTURE', 'value': 'shared_lowrank_two_stage_residual_expert'}] => discard 0.408941 0.185547 0.223394 shared_lowrank_two_stage_residual_expert
257 product_key_expert_jumprelu [{'key': 'K', 'value': '48'}] => archive 0.297634 0.116699 0.180935 product_key_expert_jumprelu

exec
/bin/bash -lc "python - <<'PY'
from sparsify.sparse_coder import _get_sae_class
from sparsify.config import SparseCoderConfig
D=1024; N=4096
common=dict(k=32, expansion_factor=1, num_experts=128, active_experts=2, latents_per_expert=192, factorized_hidden_dim=256, trunk_rank=16, stage1_ratio=0.25)
archs=['expert_jumprelu','adaptive_active_expert_jumprelu','product_key_expert_jumprelu','hashed_expert_jumprelu','factorized_router_expert_topk','shared_routed_expert_topk','shared_two_stage_residual_expert','factorized_expert_topk','shared_routed_factorized_expert_topk','shared_routed_factorized_expert_residual','shared_lowrank_routed_expert_topk','shared_lowrank_two_stage_residual_expert','shared_lowrank_routed_expert_residual','lowrank_expert_topk','lowrank_expert_jumprelu','lowrank_expert_residual','two_stage_residual_expert']
for arch in archs:
    try:
        cfg=SparseCoderConfig(architecture=arch, **common)
        m=_get_sae_class(arch)(D,cfg,device='cpu')
        c=m.selection_cost_estimate(N)
        print(arch, c['combined_accesses'], round(c['combined_accesses']/(D*N),6), c['total_accesses'], c['deployment_accesses'])
    except Exception as e:
        print(arch,'ERROR',e)
PY" in /root/sparsify-ascend
 succeeded in 6083ms:
Using fused decoder on CUDA.
expert_jumprelu 688128 0.164062 524288 163840
adaptive_active_expert_jumprelu 688128 0.164062 524288 163840
product_key_expert_jumprelu 580608 0.138428 416768 163840
hashed_expert_jumprelu 571392 0.13623 407552 163840
factorized_router_expert_topk 630784 0.150391 466944 163840
shared_routed_expert_topk 884736 0.210938 720896 163840
shared_two_stage_residual_expert 884736 0.210938 720896 163840
factorized_expert_topk 557056 0.132812 393216 163840
shared_routed_factorized_expert_topk 606208 0.144531 442368 163840
shared_routed_factorized_expert_residual 1654784 0.394531 1490944 163840
shared_lowrank_routed_expert_topk 901120 0.214844 737280 163840
shared_lowrank_two_stage_residual_expert 901120 0.214844 737280 163840
shared_lowrank_routed_expert_residual 1949696 0.464844 1785856 163840
lowrank_expert_topk 802816 0.191406 557056 245760
lowrank_expert_jumprelu 802816 0.191406 557056 245760
lowrank_expert_residual 1851392 0.441406 1605632 245760
two_stage_residual_expert 1736704 0.414062 1572864 163840

codex
Round 257 已把 `K=48` 裁掉了：exceed 从 `0.1935` 降到 `0.1809`，但新增 deploy cost 更大，objective 仍劣于 r251。现在按 Round 258 规则只能做一次 matched architecture probe，我在用当前 `K=32/EF=1` anchor 对比各现有兼容 family 的真实成本门槛，选一个最像“结构槽位本身值不值得继续”的 probe。
exec
/bin/bash -lc "python - <<'PY'
import json,glob
for path in sorted(glob.glob('research/history/round_summaries/round_023[5-9].json'))+sorted(glob.glob('research/history/round_summaries/round_024[0-6].json')):
    d=json.load(open(path))
    a=d.get('action',{})
    r=d.get('result',{})
    print('\\nround',d['round'])
    print('family',a.get('family_name'),'overrides',a.get('env_overrides'),'ref',a.get('reference_round'))
    print('result',r.get('decision'),r.get('objective_score'),r.get('total_cost_ratio'),r.get('exceed_alpha_0_50'),r.get('architecture'))
PY" in /root/sparsify-ascend
 succeeded in 0ms:

round 235
family expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'expert_jumprelu'}] ref 212
result keep 0.358553 0.164062 0.194491 expert_jumprelu

round 236
family expert_jumprelu overrides [{'key': 'K', 'value': '48'}] ref 235
result keep 0.364911 0.183594 0.181317 expert_jumprelu

round 237
family expert_jumprelu overrides [{'key': 'JUMPRELU_INIT_THRESHOLD', 'value': '0.05'}] ref 235
result archive 0.359437 0.164062 0.195374 expert_jumprelu

round 238
family adaptive_active_expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'adaptive_active_expert_jumprelu'}] ref 235
result archive 0.360480 0.164062 0.196417 adaptive_active_expert_jumprelu

round 239
family lowrank_expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'lowrank_expert_jumprelu'}] ref 235
result archive 0.378804 0.191406 0.187398 lowrank_expert_jumprelu

round 240
family hashed_expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'hashed_expert_jumprelu'}] ref 235
result discard 0.434227 0.136230 0.297997 hashed_expert_jumprelu

round 241
family product_key_expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'product_key_expert_jumprelu'}] ref 235
result crash None 0.138428 None product_key_expert_jumprelu

round 242
family product_key_expert_jumprelu overrides [{'key': 'ARCHITECTURE', 'value': 'product_key_expert_jumprelu'}] ref 235
result keep 0.338660 0.138428 0.200232 product_key_expert_jumprelu

round 243
family product_key_expert_jumprelu overrides [{'key': 'K', 'value': '48'}] ref 242
result keep 0.344919 0.157959 0.186960 product_key_expert_jumprelu

round 244
family product_key_expert_jumprelu overrides [{'key': 'ACTIVE_EXPERTS', 'value': '1'}] ref 242
result keep 0.386909 0.091553 0.295356 product_key_expert_jumprelu

round 245
family product_key_expert_jumprelu overrides [{'key': 'NUM_EXPERTS', 'value': '256'}] ref 242
result keep 0.323396 0.140625 0.182771 product_key_expert_jumprelu

round 246
family product_key_expert_jumprelu overrides [{'key': 'NUM_EXPERTS', 'value': '512'}] ref 245
result keep 0.318433 0.144043 0.174390 product_key_expert_jumprelu

exec
/bin/bash -lc "python - <<'PY'
import json,glob
for path in sorted(glob.glob('research/history/round_summaries/round_023[8-9].json'))+sorted(glob.glob('research/history/round_summaries/round_024[0-1].json')):
    d=json.load(open(path))
    print('\\n== round',d['round'])
    print(d['action']['hypothesis'][:3000])
PY" in /root/sparsify-ascend
 succeeded in 0ms:

== round 238
最终重构公式设为 x_hat = Σ_{e∈E_var(x)} Σ_{i∈Ŝ_e(x)} α_{e,i}(x) b_{e,i}，其中 {b_{e,i}} 是各 expert 的静态子库原子，expert 内支持集 Ŝ_e(x) 仍由 JumpReLU 门控形成：positive_{e,i}(x)=ReLU(z_{e,i}(x))，gate_{e,i}(x)=σ((positive_{e,i}(x)-τ_{e,i})/β)，α_{e,i}(x)=positive_{e,i}(x)·gate_{e,i}(x)；但 active expert 集合 E_var(x) 不再固定为 2，而是由 router 置信度自适应决定：若 top1 在 top2 归一化份额中已足够占优，则只激活 1 个 expert，否则激活 2 个 expert。导出到 LUTurbo/Lottable 的在线链路仍是“router 选择少量静态 expert 子库 -> expert 内 JumpReLU-gated sparse support -> 查表加权求和 -> 选择性补偿”，静态库不随样本动态生成，因此新 family `adaptive_active_expert_jumprelu` 判定为直接兼容；`batch_topk`、`adaptive_budget_topk` 等 batch/sample-coupled family 仍属不兼容，不能进入当前兼容 leaderboard。本轮唯一主假设是：当前 `expert_jumprelu` 已在 K=32/K=48 两个 matched-cost 点略优于 `expert_topk`，说明改进点可能不是“更软支持”本身，而是应继续缩短容易 token 的 active path；因此相对 r235 只把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新实现的 `adaptive_active_expert_jumprelu`，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4`、`NUM_EXPERTS=128`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=192` 与其余主 recipe 不变，直接测试“按难度自适应 1-or-2 experts”是否能在不增加 worst-case cost 的前提下进一步压低 exceed_alpha_0.50。按本地 exact 成本复核，该 matched recipe 的 selection_accesses=524288、deployment_accesses=163840、combined_accesses=688128、exact total_cost_ratio=0.1640625，与当前 incumbent `expert_jumprelu@K32` 同成本，因此这轮是干净的 matched architecture probe，不混入新的成本轴。

== round 239
最终重构公式设为 x_hat = t(x) + Σ_{e∈E(x-t(x))} Σ_{i∈Ŝ_e(x-t(x))} α_{e,i}(x) b_{e,i}，其中 t(x)=Σ_r β_r(x)u_r 是静态 low-rank trunk 库 U={u_r} 的重构，{b_{e,i}} 是静态 expert 子库原子，E(·) 由轻量 router 选出的少量 experts，expert 内系数改为 JumpReLU 形式 α_{e,i}(x)=ReLU(z_{e,i})·σ((ReLU(z_{e,i})-τ_{e,i})/bandwidth)·p_e(x)。导出到 LUTurbo/Lottable 的链路仍是“trunk 查表/加权和 -> router 选少量静态 expert 子库 -> 子库内 JumpReLU-gated sparse support -> 有限加权和”，没有动态生成字典，因此 `lowrank_expert_jumprelu` 判断为扩展兼容；`batch_topk`、`adaptive_budget_topk` 等 batch/sample-coupled family 仍属不兼容，不能驱动当前 objective leaderboard。本轮主假设是：r238 已经说明 variable-active-path 在 same-cost K=32 lane 上没有收益，而当前又已连续两轮无改进，应切到新的结构槽位；相对 r235 仅把 `ARCHITECTURE` 切到新实现的 `lowrank_expert_jumprelu`，保持 `K=32, EF=1, TRUNK_RANK=16, NUM_EXPERTS=128, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=192, adam, LR=8e-4` 不变，直接测试“shared low-rank trunk + routed JumpReLU experts”是否能在约 total_cost_ratio=0.19140625 这一带用更强的主干/tail 分工换回明显更低的 exceed_alpha_0.50。

== round 240
最终重构公式设为 x_hat = Σ_{e∈E_hash(x)} Σ_{i∈Ŝ_e(x)} α_{e,i}(x) b_{e,i}，其中 {b_{e,i}} 是各 expert 的静态子库原子，E_hash(x) 由固定 sign-hash 路由从输入 x 直接选出少量 active experts，expert 内系数采用 JumpReLU 形式 α_{e,i}(x)=ReLU(z_{e,i})·σ((ReLU(z_{e,i})-τ_{e,i})/bandwidth)·p_e(x)。导出到 LUTurbo/Lottable 的链路仍是“固定哈希路由选择少量静态 expert 子库 -> expert 内 JumpReLU sparse support -> 静态库查表加权和 -> 选择性补偿”，静态库不随样本动态生成，因此新 family `hashed_expert_jumprelu` 判断为直接兼容；`batch_topk`、`adaptive_budget_topk` 等 batch/sample-coupled family 仍属不兼容，不能驱动当前 objective leaderboard。本轮主假设是：r238 与 r239 已说明 variable-active-path 和 low-rank trunk 都没有把 objective 真正拉下来，而当前 incumbent 的主要短板仍是 exceed 过高但又不能再容忍更重结构；因此相对 r235 仅把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新实现的 `hashed_expert_jumprelu`，保持 `K=32, EF=1, adam, LR=8e-4, NUM_EXPERTS=128, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=192` 与其余 recipe 不变，直接测试“去掉 learned router、保留 full expert heads + JumpReLU local supports”能否在 exact total_cost_ratio=0.13623046875 的更低成本下，把 exceed_alpha_0.50 控制在约 0.21826 以下，从而刷新当前 objective incumbent。

== round 241
最终重构公式设为 x_hat = Σ_{e∈E_pk(x)} Σ_{i∈S_e^τ(x)} α_{e,i}(x) b_{e,i}。其中每个 expert 子库 {b_{e,i}} 都是静态向量库，E_pk(x) 不是由 full router 或 fixed hash 直接给出，而是由两路 learned product-key 路由分数相加后在笛卡尔 expert 对上选出少量 active experts：logit_e(x)=l_{a(e)}(x)+r_{b(e)}(x)；expert 内部再用 JumpReLU 形成局部 support S_e^τ(x)。在线导出链路仍是“left/right product-key router 打分 -> 选择少量静态 expert 子库 -> expert 内 JumpReLU sparse support -> 查表加权求和 -> 选择性补偿”，不依赖动态生成字典，因此兼容性判断为直接兼容。`batch_topk`、`adaptive_budget_topk` 这类 batch/sample-coupled family 仍是不兼容，不能进入当前 objective leaderboard。本轮唯一主假设是：当前 incumbent `expert_jumprelu@K32` 的 objective 缺口仍主要卡在 selection cost，而 recent `hashed_expert_jumprelu` 与 `factorized_router_expert_topk` 的负信号说明“把 router 变轻”本身不能靠 fixed hash 或 generic low-rank hidden 粗暴完成；相对 r235 仅把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新实现的 `product_key_expert_jumprelu`，保持 `K=32`、`EXPANSION_FACTOR=1`、`NUM_EXPERTS=128`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=192`、`adam`、`LR=8e-4` 与其余 recipe 不变，直接测试 PKM-style coarse-to-fine compositional routing 是否能在保住 routed micro-expert 质量的同时，把 exact total_cost_ratio 压到 0.138427734375，并把 exceed_alpha_0.50 维持在约 0.2161 以下，从而刷新当前 incumbent 0.354491。这个 family 与已测 `factorized_router_expert_topk` 的精确差异是：它不是“低秩 hidden -> 全 expert logits”，而是“左右两组小 key 的可组合 expert addressing”，因此是在当前 target 上尚未被现有 family 覆盖的新结构槽位。

codex
我把 `expert_jumprelu@r235` 那条 matched lane 最近的 probe 也翻了一遍：`adaptive_active`、`lowrank_expert_jumprelu`、`hashed`、`product_key` 都已经在同配方下测过。接下来要避免重复旧槽位，优先选一个还没在这条 `K=32, 128 experts, 192 width` lane 上做过的、且能直接回答 “shared/coarse + routed cleanup 值不值得继续” 的家族。
exec
/bin/bash -lc "python - <<'PY'
from research.AutoResearch.override_registry import validate_env_overrides
for arch in ['shared_routed_expert_topk','shared_two_stage_residual_expert','shared_lowrank_routed_expert_topk','shared_lowrank_two_stage_residual_expert','factorized_expert_topk','shared_routed_factorized_expert_topk']:
    print(arch, validate_env_overrides([{'key':'ARCHITECTURE','value':arch}], fallback_architecture='expert_jumprelu'))
PY" in /root/sparsify-ascend
 succeeded in 0ms:
shared_routed_expert_topk []
shared_two_stage_residual_expert []
shared_lowrank_routed_expert_topk []
shared_lowrank_two_stage_residual_expert []
factorized_expert_topk []
shared_routed_factorized_expert_topk []

exec
/bin/bash -lc "python - <<'PY'
import json,glob
wanted=['shared_routed_expert_topk','shared_two_stage_residual_expert','shared_lowrank_routed_expert_topk','shared_lowrank_two_stage_residual_expert','factorized_expert_topk','shared_routed_factorized_expert_topk']
for arch in wanted:
    hits=[]
    for path in sorted(glob.glob('research/history/round_summaries/round_*.json')):
        d=json.load(open(path))
        r=d.get('result',{})
        a=d.get('action',{})
        if r.get('architecture')==arch or a.get('family_name')==arch:
            cfg=d.get('resolved_candidate_env_config',{})
            hits.append((d['round'], r.get('objective_score'), r.get('total_cost_ratio'), r.get('exceed_alpha_0_50'), cfg.get('K'), cfg.get('NUM_EXPERTS'), cfg.get('LATENTS_PER_EXPERT'), cfg.get('ACTIVE_EXPERTS'), cfg.get('FACTORIZED_HIDDEN_DIM'), cfg.get('TRUNK_RANK')))
    print('\\n',arch)
    for h in hits[-8:]:
        print(h)
PY" in /root/sparsify-ascend
 succeeded in 101ms:
exec
/bin/bash -lc "python - <<'PY'
import json,glob
for arch in ['lowrank_expert_topk','lowrank_expert_residual','factorized_topk','group_topk','shared_routed_factorized_expert_residual']:
    print('\\n',arch)
    for path in sorted(glob.glob('research/history/round_summaries/round_*.json')):
        d=json.load(open(path))
        r=d.get('result',{})
        a=d.get('action',{})
        if r.get('architecture')==arch or a.get('family_name')==arch:
            cfg=d.get('resolved_candidate_env_config',{})
            print((d['round'], r.get('objective_score'), r.get('total_cost_ratio'), r.get('exceed_alpha_0_50'), cfg.get('K'), cfg.get('NUM_EXPERTS'), cfg.get('LATENTS_PER_EXPERT'), cfg.get('ACTIVE_EXPERTS'), cfg.get('FACTORIZED_HIDDEN_DIM'), cfg.get('TRUNK_RANK')))
PY" in /root/sparsify-ascend
 succeeded in 0ms:

 lowrank_expert_topk
(35, '0.8394564723968506', '0.66', '0.17945647239685059', '128', None, None, None, None, None)
(36, '1.015589920282364', '0.81', '0.2055899202823639', '160', None, None, None, None, None)
(42, '1.0642871379852294', '0.95', '0.11428713798522949', '192', None, None, None, '256', None)
(43, None, '0.52', None, '192', None, None, None, '256', '128')
(48, None, '0.52', None, '192', None, None, None, '256', '128')
(49, None, '0.52', None, '192', None, None, None, '256', '128')
(50, None, '0.52', None, '192', None, None, None, '256', '128')
(57, '0.6173256361484527', '0.44', '0.17732563614845276', '128', '16', None, None, '256', '16')
(59, '0.6137982034683227', '0.44', '0.17379820346832275', '128', '32', None, None, '256', '16')
(60, '0.6620173346996308', '0.52', '0.14201733469963074', '128', '32', None, None, '256', '64')
(67, '0.6769593048095703', '0.53', '0.1469593048095703', '128', '64', None, None, '256', '64')
(70, '0.6394657206535339', '0.47', '0.16946572065353394', '128', '32', None, None, '256', '32')
(82, '0.676720757484436', '0.52', '0.15672075748443604', '128', '16', None, None, '256', '64')
(84, None, '0.77', None, '128', '32', None, '2', '256', '64')
(85, None, '0.77', None, '128', '32', None, '2', '256', '64')
(86, '1.0442525339126587', '0.77', '0.2742525339126587', '128', '16', None, '2', '256', '64')
(94, '0.6441662907600403', '0.5', '0.14416629076004028', '128', '32', None, None, '256', '48')
(95, '0.6302139568328857', '0.48', '0.15021395683288574', '128', '32', None, None, '256', '40')
(96, '0.6029948616027831', '0.44', '0.1629948616027832', '96', '32', None, None, '256', '40')
(97, '0.6158684706687927', '0.46', '0.15586847066879272', '112', '32', None, None, '256', '40')
(131, '0.5243795585632325', '0.32', '0.20437955856323242', '128', '64', '320', '1', '256', '40')
(143, '0.48536633133888246', '0.33', '0.15536633133888245', '112', '64', '224', '2', '256', '40')
(145, '0.520280032157898', '0.28', '0.24028003215789795', '112', '64', '224', '1', '256', '40')
(170, '0.4398040068149567', '0.27', '0.16980400681495667', '96', '64', '224', '2', '256', '16')
(171, '0.4284583234786987', '0.24', '0.18845832347869873', '96', '64', '160', '2', '256', '16')
(175, '0.4358420467376709', '0.26', '0.1758420467376709', '96', '64', '224', '2', '256', '8')
(187, '0.426606148481369', '0.25', '0.17660614848136902', '96', '64', '192', '2', '256', '16')
(190, '0.4845352983474731', '0.21', '0.27453529834747314', '96', '64', '192', '1', '256', '16')
(197, '0.426606148481369', '0.25', '0.17660614848136902', '96', '64', '192', '2', '256', '16')
(200, '0.402860', '0.214844', '0.188017', '64', '64', '192', '2', '256', '16')
(207, '0.396871', '0.199219', '0.197653', '64', '64', '160', '2', '256', '16')
(208, '0.388078', '0.185547', '0.202531', '64', '64', '160', '2', '256', '8')
(214, '0.378973', '0.191406', '0.187566', '32', '128', '192', '2', '256', '16')

 lowrank_expert_residual
(37, '1.2398590564727783', '1.2', '0.03985905647277832', '192', None, None, None, '256', None)
(38, '0.8917803955078125', '0.77', '0.1217803955078125', '192', None, None, None, '256', '128')
(39, '0.8917803955078125', '0.77', '0.1217803955078125', '192', None, None, None, '256', '128')
(44, '0.7595523166656494', '0.58', '0.17955231666564941', '192', None, None, None, '256', '16')
(45, '0.7315976107120514', '0.54', '0.1915976107120514', '160', None, None, None, '256', '16')
(46, '0.7239976906776429', '0.54', '0.18399769067764282', '160', None, None, None, '256', '16')
(47, '0.7228644573688507', '0.54', '0.1828644573688507', '160', None, None, None, '256', '16')
(55, '0.8607148039340973', '0.73', '0.1307148039340973', '160', '16', None, None, '256', '16')
(56, '0.831306459903717', '0.69', '0.14130645990371704', '128', '16', None, None, '256', '16')
(58, '0.8430120968818664', '0.69', '0.15301209688186646', '128', '16', None, None, '256', '16')
(61, '0.8946072947978974', '0.77', '0.12460729479789734', '128', '32', None, None, '256', '64')
(62, '0.9035693299770355', '0.77', '0.13356932997703552', '128', '32', None, None, '256', '64')
(63, '0.8709468054771423', '0.73', '0.14094680547714233', '96', '32', None, None, '256', '64')
(65, '0.8529161524772644', '0.72', '0.1329161524772644', '128', '32', None, None, '256', '32')
(66, '0.8524033045768737', '0.71', '0.14240330457687378', '128', '32', None, None, '256', '24')
(76, '0.8630668330192566', '0.73', '0.1330668330192566', '128', '64', None, None, '256', '32')
(79, '0.8687806832790375', '0.73', '0.13878068327903748', '128', '64', None, None, '256', '32')
(81, '0.8523135101795196', '0.71', '0.14231351017951965', '128', '64', None, None, '256', '24')
(83, '0.8563797688484192', '0.71', '0.1463797688484192', '128', '16', None, None, '256', '32')
(98, '0.8164383792877197', '0.69', '0.12643837928771973', '96', '32', None, None, '256', '40')
(99, '0.8292816495895385', '0.71', '0.11928164958953857', '112', '32', None, None, '256', '40')
(100, '0.8424809682369232', '0.73', '0.11248096823692322', '128', '32', None, None, '256', '40')
(124, '0.8233464479446411', '0.7', '0.12334644794464111', '112', '32', None, None, '256', '32')
(125, '0.8129293549060822', '0.68', '0.13292935490608215', '96', '32', None, None, '256', '32')
(146, '0.6827811467647552', '0.58', '0.10278114676475525', '112', '64', '224', '2', '256', '40')
(151, '0.6495276176929474', '0.54', '0.10952761769294739', '112', '64', '224', '2', '256', '16')
(152, '0.6367051196098328', '0.52', '0.11670511960983276', '96', '64', '224', '2', '256', '16')
(153, '0.6368698394298553', '0.46', '0.17686983942985535', '96', '64', '224', '1', '256', '16')
(158, '0.6368698394298553', '0.46', '0.17686983942985535', '96', '64', '224', '1', '256', '16')
(229, '0.602006', '0.441406', '0.160599', '32', '128', '192', '2', '256', '16')

 factorized_topk
(4, '0.6578242945671082', '0.33', '0.32782429456710815', '64', None, None, None, None, None)
(19, '0.7696953809261322', '0.51', '0.2596953809261322', '160', None, None, None, None, None)
(20, '0.6092800569534302', '0.32', '0.2892800569534302', '160', None, None, None, '256', None)
(21, '0.6437280035018921', '0.36', '0.2837280035018921', '192', None, None, None, '256', None)
(22, '0.6860680413246154', '0.42', '0.2660680413246155', '192', None, None, None, '384', None)
(173, '0.5451426708698273', '0.24', '0.30514267086982727', '96', '64', '160', '2', '256', '16')
(220, '0.491881', '0.164062', '0.327819', '32', '128', '192', '2', '256', '16')

 group_topk
(9, '0.6193198430538178', '0.33', '0.28931984305381775', '64', None, None, None, None, None)
(28, '0.7127433824539184', '0.48', '0.23274338245391846', '192', None, None, None, '256', None)
(227, '0.600232', '0.289062', '0.311170', '32', '128', '192', '2', '256', '16')

 shared_routed_factorized_expert_residual
(150, '0.7323945963382721', '0.49', '0.2423945963382721', '112', '64', '224', '2', '256', '40')
(231, '0.698899', '0.394531', '0.304368', '32', '128', '192', '2', '256', '16')

exec
/bin/bash -lc "sed -n '2091,2646p' sparsify/sparse_coder.py" in /root/sparsify-ascend
 succeeded in 0ms:
class SharedTwoStageResidualExpertSparseCoder(SparseCoder):
    """Shared sparse coarse stage followed by expert-routed residual cleanup."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        (
            _base_num_latents,
            self.num_experts,
            self.latents_per_expert,
            self.active_experts,
        ) = _resolve_expert_layout(cfg, d_in)
        self.num_shared_experts = 1
        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
        self.expert_num_latents = self.num_experts * self.latents_per_expert
        self.num_latents = self.shared_num_latents + self.expert_num_latents

        if cfg.stage1_ratio is not None:
            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
        else:
            self.stage1_k = max(1, cfg.k // 2)
        self.stage2_k = max(1, cfg.k - self.stage1_k)

        if self.stage1_k > self.shared_num_latents:
            raise ValueError(
                "shared_two_stage_residual_expert requires "
                "stage1_k <= shared_experts * latents_per_expert, "
                f"got stage1_k={self.stage1_k}, "
                f"shared_experts={self.num_shared_experts}, "
                f"latents_per_expert={self.latents_per_expert}"
            )
        if self.stage2_k > self.active_experts * self.latents_per_expert:
            raise ValueError(
                "shared_two_stage_residual_expert requires "
                "stage2_k <= active_experts * latents_per_expert, "
                f"got stage2_k={self.stage2_k}, "
                f"active_experts={self.active_experts}, "
                f"latents_per_expert={self.latents_per_expert}"
            )

        self.shared_encoders = nn.Parameter(
            torch.empty(
                self.num_shared_experts,
                self.latents_per_expert,
                d_in,
                device=device,
                dtype=dtype,
            )
        )
        self.shared_encoder_bias = nn.Parameter(
            torch.zeros(
                self.num_shared_experts,
                self.latents_per_expert,
                device=device,
                dtype=dtype,
            )
        )
        self.router = nn.Linear(d_in, self.num_experts, device=device, dtype=dtype)
        self.expert_encoders = nn.Parameter(
            torch.empty(
                self.num_experts,
                self.latents_per_expert,
                d_in,
                device=device,
                dtype=dtype,
            )
        )
        self.expert_encoder_bias = nn.Parameter(
            torch.zeros(
                self.num_experts,
                self.latents_per_expert,
                device=device,
                dtype=dtype,
            )
        )

        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
        nn.init.kaiming_uniform_(self.router.weight, a=5**0.5)
        self.router.bias.data.zero_()
        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)

        if decoder:
            shared_decoder = self.shared_encoders.data.reshape(
                self.shared_num_latents, d_in
            )
            expert_decoder = self.expert_encoders.data.reshape(
                self.expert_num_latents, d_in
            )
            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
            self.W_dec = nn.Parameter(decoder_init.clone())
            if self.cfg.normalize_decoder:
                self.set_decoder_norm_to_unit_norm()
        else:
            self.W_dec = None

        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))

    def _encoder_linear_layers(self):
        return [("router", self.router)]

    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
        return [
            (
                "shared_expert_encoder",
                self.num_shared_experts * self.d_in * self.latents_per_expert,
                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
            ),
            (
                "active_expert_encoder",
                self.active_experts * self.d_in * self.latents_per_expert,
                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
            ),
        ]

    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
        assert self.W_dec is not None, "Decoder weight was not initialized."
        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)

    def _encode_shared_stage(
        self, x_centered: Tensor
    ) -> tuple[Tensor, Tensor, Tensor]:
        original_shape = x_centered.shape[:-1]
        flat_x = x_centered.reshape(-1, self.d_in)
        shared_pre_acts = (
            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
            + self.shared_encoder_bias.unsqueeze(0)
        )
        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)

        top_acts, top_indices = torch.topk(
            shared_acts, self.stage1_k, dim=-1, sorted=False
        )
        target_shape = (*original_shape, self.stage1_k)
        acts_shape = (*original_shape, self.shared_num_latents)
        return (
            top_acts.reshape(target_shape),
            top_indices.reshape(target_shape),
            shared_acts.reshape(acts_shape),
        )

    def _encode_expert_stage(
        self, residual: Tensor
    ) -> tuple[Tensor, Tensor, Tensor]:
        original_shape = residual.shape[:-1]
        flat_x = residual.reshape(-1, self.d_in)

        router_logits = self.router(flat_x)
        selected_expert_idx, selected_probs = _select_active_expert_indices(
            router_logits, self.active_experts
        )
        selected_weight = self.expert_encoders[selected_expert_idx]
        selected_bias = self.expert_encoder_bias[selected_expert_idx]
        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
        acts = F.relu(pre_acts) * selected_probs.unsqueeze(-1)
        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
            acts,
            selected_expert_idx,
            self.stage2_k,
            self.latents_per_expert,
            self.expert_num_latents,
            index_offset=self.shared_num_latents,
        )

        target_shape = (*original_shape, self.stage2_k)
        acts_shape = (*original_shape, self.expert_num_latents)
        return (
            top_acts.reshape(target_shape),
            top_indices.reshape(target_shape),
            full_acts.reshape(acts_shape),
        )

    def encode(self, x: Tensor) -> EncoderOutput:
        x_centered = x - self.b_dec
        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
            x_centered
        )
        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)

        residual = x_centered - stage1_out
        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
            residual
        )

        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
        return EncoderOutput(combined_acts, combined_indices, combined_full)

    @device_autocast
    def forward(
        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
    ) -> ForwardOutput:
        x_centered = x - self.b_dec
        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
            x_centered
        )
        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)

        residual = x_centered - stage1_out
        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
            residual
        )

        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)

        sparse_out = self._decode_sparse(combined_acts, combined_indices)
        sae_out = sparse_out + self.b_dec

        if y is None:
            y = x

        e = y - sae_out
        total_variance = (y - y.mean(0)).pow(2).sum()

        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
            k_aux = y.shape[-1] // 2
            scale = min(num_dead / k_aux, 1.0)
            k_aux = min(k_aux, num_dead)
            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
            auxk_loss = (e_hat - e.detach()).pow(2).sum()
            auxk_loss = scale * auxk_loss / total_variance
        else:
            auxk_loss = sae_out.new_tensor(0.0)

        l2_loss = e.pow(2).sum()
        fvu = l2_loss / total_variance

        return ForwardOutput(
            sae_out,
            combined_acts,
            combined_indices,
            fvu,
            auxk_loss,
        )


class FactorizedExpertTopKSparseCoder(SparseCoder):
    """Route into an expert-local top-k head after a shared low-rank projection."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        (
            _base_num_latents,
            self.num_experts,
            self.latents_per_expert,
            self.active_experts,
        ) = _resolve_expert_layout(cfg, d_in)
        self.num_latents = self.num_experts * self.latents_per_expert
        if cfg.k > self.active_experts * self.latents_per_expert:
            raise ValueError(
                "factorized_expert_topk requires k <= active_experts * latents_per_expert, "
                f"got k={cfg.k}, active_experts={self.active_experts}, "
                f"latents_per_expert={self.latents_per_expert}"
            )

        hidden_dim = (
            cfg.factorized_hidden_dim
            if cfg.factorized_hidden_dim is not None
            else min(self.latents_per_expert, max(d_in // 2, cfg.k * 4))
        )
        self.factor_encoder = nn.Linear(
            d_in, hidden_dim, device=device, dtype=dtype
        )
        self.router = nn.Linear(
            hidden_dim, self.num_experts, device=device, dtype=dtype
        )
        self.expert_heads = nn.Parameter(
            torch.empty(
                self.num_experts,
                self.latents_per_expert,
                hidden_dim,
                device=device,
                dtype=dtype,
            )
        )
        self.expert_head_bias = nn.Parameter(
            torch.zeros(
                self.num_experts,
                self.latents_per_expert,
                device=device,
                dtype=dtype,
            )
        )
        self.factor_encoder.bias.data.zero_()
        self.router.bias.data.zero_()
        nn.init.kaiming_uniform_(self.factor_encoder.weight, a=5**0.5)
        nn.init.kaiming_uniform_(self.router.weight, a=5**0.5)
        nn.init.kaiming_uniform_(self.expert_heads, a=5**0.5)

        if decoder:
            effective_encoder = torch.einsum(
                "elh,hd->eld", self.expert_heads.data, self.factor_encoder.weight.data
            ).reshape(self.num_latents, d_in)
            self.W_dec = nn.Parameter(effective_encoder.clone())
            if self.cfg.normalize_decoder:
                self.set_decoder_norm_to_unit_norm()
        else:
            self.W_dec = None

        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))

    def _encoder_linear_layers(self):
        return [("factor_encoder", self.factor_encoder), ("router", self.router)]

    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
        return [
            (
                "active_expert_head",
                self.active_experts
                * self.factor_encoder.out_features
                * self.latents_per_expert,
                f"active={self.active_experts}×{self.factor_encoder.out_features}x{self.latents_per_expert}",
            )
        ]

    def encode(self, x: Tensor) -> EncoderOutput:
        x = x - self.b_dec
        original_shape = x.shape[:-1]
        flat_x = x.reshape(-1, self.d_in)

        hidden = F.relu(self.factor_encoder(flat_x))
        router_logits = self.router(hidden)
        selected_expert_idx, selected_probs = _select_active_expert_indices(
            router_logits, self.active_experts
        )
        selected_weight = self.expert_heads[selected_expert_idx]
        selected_bias = self.expert_head_bias[selected_expert_idx]
        pre_acts = torch.einsum("bh,balh->bal", hidden, selected_weight) + selected_bias
        acts = F.relu(pre_acts) * selected_probs.unsqueeze(-1)
        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
            acts,
            selected_expert_idx,
            self.cfg.k,
            self.latents_per_expert,
            self.num_latents,
        )

        target_shape = (*original_shape, self.cfg.k)
        acts_shape = (*original_shape, self.num_latents)
        return EncoderOutput(
            top_acts.reshape(target_shape),
            top_indices.reshape(target_shape),
            full_acts.reshape(acts_shape),
        )


class SharedRoutedFactorizedExpertTopKSparseCoder(SparseCoder):
    """Always-on shared factorized expert plus routed factorized experts."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        (
            _base_num_latents,
            self.num_routed_experts,
            self.latents_per_expert,
            self.active_experts,
        ) = _resolve_expert_layout(cfg, d_in)
        self.num_shared_experts = 1
        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
        self.routed_num_latents = (
            self.num_routed_experts * self.latents_per_expert
        )
        self.num_latents = self.shared_num_latents + self.routed_num_latents
        max_active_latents = (
            self.num_shared_experts + self.active_experts
        ) * self.latents_per_expert
        if cfg.k > max_active_latents:
            raise ValueError(
                "shared_routed_factorized_expert_topk requires "
                "k <= (shared_experts + active_experts) * latents_per_expert, "
                f"got k={cfg.k}, shared_experts={self.num_shared_experts}, "
                f"active_experts={self.active_experts}, "
                f"latents_per_expert={self.latents_per_expert}"
            )

        hidden_dim = (
            cfg.factorized_hidden_dim
            if cfg.factorized_hidden_dim is not None
            else min(self.latents_per_expert, max(d_in // 2, cfg.k * 4))
        )
        self.factor_encoder = nn.Linear(
            d_in, hidden_dim, device=device, dtype=dtype
        )
        self.router = nn.Linear(
            hidden_dim, self.num_routed_experts, device=device, dtype=dtype
        )
        self.shared_heads = nn.Parameter(
            torch.empty(
                self.num_shared_experts,
                self.latents_per_expert,
                hidden_dim,
                device=device,
                dtype=dtype,
            )
        )
        self.shared_head_bias = nn.Parameter(
            torch.zeros(
                self.num_shared_experts,
                self.latents_per_expert,
                device=device,
                dtype=dtype,
            )
        )
        self.routed_heads = nn.Parameter(
            torch.empty(
                self.num_routed_experts,
                self.latents_per_expert,
                hidden_dim,
                device=device,
                dtype=dtype,
            )
        )
        self.routed_head_bias = nn.Parameter(
            torch.zeros(
                self.num_routed_experts,
                self.latents_per_expert,
                device=device,
                dtype=dtype,
            )
        )
        self.factor_encoder.bias.data.zero_()
        self.router.bias.data.zero_()
        nn.init.kaiming_uniform_(self.factor_encoder.weight, a=5**0.5)
        nn.init.kaiming_uniform_(self.router.weight, a=5**0.5)
        nn.init.kaiming_uniform_(self.shared_heads, a=5**0.5)
        nn.init.kaiming_uniform_(self.routed_heads, a=5**0.5)

        if decoder:
            shared_decoder = torch.einsum(
                "slh,hd->sld",
                self.shared_heads.data,
                self.factor_encoder.weight.data,
            ).reshape(self.shared_num_latents, d_in)
            routed_decoder = torch.einsum(
                "elh,hd->eld",
                self.routed_heads.data,
                self.factor_encoder.weight.data,
            ).reshape(self.routed_num_latents, d_in)
            decoder_init = torch.cat((shared_decoder, routed_decoder), dim=0)
            self.W_dec = nn.Parameter(decoder_init.clone())
            if self.cfg.normalize_decoder:
                self.set_decoder_norm_to_unit_norm()
        else:
            self.W_dec = None

        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))

    def _encoder_linear_layers(self):
        return [("factor_encoder", self.factor_encoder), ("router", self.router)]

    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
        hidden_dim = self.factor_encoder.out_features
        return [
            (
                "shared_factorized_head",
                self.num_shared_experts * hidden_dim * self.latents_per_expert,
                f"shared={self.num_shared_experts}×{hidden_dim}x{self.latents_per_expert}",
            ),
            (
                "active_routed_factorized_head",
                self.active_experts * hidden_dim * self.latents_per_expert,
                f"active={self.active_experts}×{hidden_dim}x{self.latents_per_expert}",
            ),
        ]

    def encode(self, x: Tensor) -> EncoderOutput:
        x = x - self.b_dec
        original_shape = x.shape[:-1]
        flat_x = x.reshape(-1, self.d_in)
        batch = flat_x.shape[0]

        hidden = F.relu(self.factor_encoder(flat_x))
        shared_pre_acts = (
            torch.einsum("bh,slh->bsl", hidden, self.shared_heads)
            + self.shared_head_bias.unsqueeze(0)
        )
        shared_acts = F.relu(shared_pre_acts)

        router_logits = self.router(hidden)
        selected_expert_idx, selected_probs = _select_active_expert_indices(
            router_logits, self.active_experts
        )
        selected_weight = self.routed_heads[selected_expert_idx]
        selected_bias = self.routed_head_bias[selected_expert_idx]
        routed_pre_acts = (
            torch.einsum("bh,balh->bal", hidden, selected_weight) + selected_bias
        )
        routed_acts = F.relu(routed_pre_acts) * selected_probs.unsqueeze(-1)

        flat_shared_acts = shared_acts.reshape(batch, -1)
        flat_routed_acts = routed_acts.reshape(batch, -1)
        flat_acts = torch.cat((flat_shared_acts, flat_routed_acts), dim=-1)

        shared_offsets = torch.arange(
            self.shared_num_latents, device=flat_x.device
        ).view(1, -1)
        local_offsets = torch.arange(
            self.latents_per_expert, device=flat_x.device
        ).view(1, 1, self.latents_per_expert)
        routed_offsets = (
            self.shared_num_latents
            + selected_expert_idx.unsqueeze(-1) * self.latents_per_expert
            + local_offsets
        ).reshape(batch, -1)
        flat_indices = torch.cat(
            (shared_offsets.expand(batch, -1), routed_offsets), dim=-1
        )

        top_acts, top_pos = torch.topk(flat_acts, self.cfg.k, dim=-1, sorted=False)
        top_indices = flat_indices.gather(1, top_pos)

        full_acts = flat_acts.new_zeros(batch, self.num_latents)
        full_acts.scatter_(1, flat_indices, flat_acts)

        target_shape = (*original_shape, self.cfg.k)
        acts_shape = (*original_shape, self.num_latents)
        return EncoderOutput(
            top_acts.reshape(target_shape),
            top_indices.reshape(target_shape),
            full_acts.reshape(acts_shape),
        )



codex
这轮如果要做真正新的结构槽位，现有家族里最接近且还没覆盖 prompt 重点组合的是 “shared coarse stage + product-key routed micro-experts”。我会按新增 architecture 的最小接线做一版，只用现有 env 旋钮，不引入新参数，然后跑 exact smoke 和 allowlist 自检，再决定是否把它作为 Round 258 的 architecture probe 返回。
exec
/bin/bash -lc "sed -n '200,260p' sparsify/config.py" in /root/sparsify-ascend
 succeeded in 0ms:
            raise ValueError(f"Elbow threshold file not found: {self.elbow_threshold_path}")

        if self.compile_model:
            from .device import get_device_type
            if get_device_type() != "cuda":
                self.compile_model = False

        if self.use_hadamard:
            bs = self.hadamard_block_size
            if bs <= 0 or (bs & (bs - 1)) != 0:
                raise ValueError(
                    f"hadamard_block_size must be a positive power of 2, got {bs}"
                )

        # Architecture validation
        valid_archs = (
            "topk",
            "batch_topk",
            "adaptive_budget_topk",
            "bucketed_topk",
            "codebook_topk",
            "residual_vq",
            "two_code_residual_vq",
            "lowrank_residual_vq",
            "jumprelu",
            "gated",
            "routed",
            "expert_topk",
            "expert_jumprelu",
            "product_key_expert_jumprelu",
            "adaptive_active_expert_jumprelu",
            "hashed_expert_jumprelu",
            "factorized_router_expert_topk",
            "shared_routed_expert_topk",
            "shared_two_stage_residual_expert",
            "factorized_expert_topk",
            "shared_routed_factorized_expert_topk",
            "shared_routed_factorized_expert_residual",
            "shared_lowrank_routed_expert_topk",
            "shared_lowrank_two_stage_residual_expert",
            "shared_lowrank_routed_expert_residual",
            "lowrank_expert_topk",
            "lowrank_expert_jumprelu",
            "lowrank_expert_residual",
            "two_stage_residual_expert",
            "group_topk",
            "factorized_topk",
            "lowrank_residual",
            "lowrank_two_stage_residual",
            "routed_lowrank_two_stage_residual",
            "bucketed_lowrank_residual",
            "whitened_lowrank_residual",
            "lowrank_adaptive_budget_residual",
            "lowrank_gated_residual",
            "lowrank_jumprelu_residual",
            "lowrank_multi_branch_residual",
            "lowrank_factorized_residual",
            "lowrank_soft_codebook_residual",
            "lowrank_gated_soft_codebook_residual",
            "lowrank_grouped_soft_codebook_residual",
            "lowrank_two_stage_soft_codebook_residual",

exec
/bin/bash -lc "sed -n '1,120p' research/AutoResearch/override_registry.py" in /root/sparsify-ascend
 succeeded in 0ms:
"""Override validation and baseline resolution for the autoresearch loop."""

from __future__ import annotations

from typing import Any

from .types import BASE_ENV_DEFAULTS


COMMON_OVERRIDE_KEYS = set(BASE_ENV_DEFAULTS.keys()) | {
    "NUM_GROUPS",
    "ACTIVE_GROUPS",
    "ORTHO_LAMBDA",
    "RESIDUAL_FROM",
    "MATRYOSHKA_KS",
    "MATRYOSHKA_WEIGHTS",
}

_LOWRANK_KEYS = {"TRUNK_RANK"}
_TWOSTAGE_KEYS = {"TRUNK_RANK", "STAGE1_RATIO"}
_SOFT_CODEBOOK_TWOSTAGE_KEYS = {"TRUNK_RANK", "NUM_CODES", "STAGE1_RATIO"}
_EXPERT_KEYS = {"NUM_EXPERTS", "ACTIVE_EXPERTS", "LATENTS_PER_EXPERT"}
_FACTORIZED_EXPERT_KEYS = {
    "FACTORIZED_HIDDEN_DIM",
    "NUM_EXPERTS",
    "ACTIVE_EXPERTS",
    "LATENTS_PER_EXPERT",
}
_FACTORIZED_EXPERT_RESIDUAL_KEYS = _FACTORIZED_EXPERT_KEYS | {"STAGE1_RATIO"}
_SHARED_LOWRANK_ROUTED_EXPERT_KEYS = _FACTORIZED_EXPERT_KEYS
_SHARED_LOWRANK_ROUTED_EXPERT_RESIDUAL_KEYS = (
    _FACTORIZED_EXPERT_KEYS | {"STAGE1_RATIO"}
)
_LOWRANK_EXPERT_KEYS = {"TRUNK_RANK", "NUM_EXPERTS", "ACTIVE_EXPERTS", "LATENTS_PER_EXPERT"}
_LOWRANK_EXPERT_RESIDUAL_KEYS = {
    "TRUNK_RANK",
    "STAGE1_RATIO",
    "NUM_EXPERTS",
    "ACTIVE_EXPERTS",
    "LATENTS_PER_EXPERT",
}

ARCHITECTURE_OVERRIDE_KEYS: dict[str, set[str]] = {
    "topk": set(),
    "jumprelu": {
        "JUMPRELU_INIT_THRESHOLD",
        "JUMPRELU_BANDWIDTH",
    },
    "gated": {
        "GATED_TEMPERATURE",
        "GATED_INIT_LOGIT",
    },
    "expert_topk": _EXPERT_KEYS,
    "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
    "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
    "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
    "shared_routed_expert_topk": _EXPERT_KEYS,
    "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
    "factorized_topk": {"FACTORIZED_HIDDEN_DIM"},
    "factorized_expert_topk": _FACTORIZED_EXPERT_KEYS,
    "shared_routed_factorized_expert_residual": _FACTORIZED_EXPERT_RESIDUAL_KEYS,
    "shared_lowrank_routed_expert_topk": _SHARED_LOWRANK_ROUTED_EXPERT_KEYS,
    "shared_lowrank_routed_expert_residual": _SHARED_LOWRANK_ROUTED_EXPERT_RESIDUAL_KEYS,
    "lowrank_residual": _LOWRANK_KEYS,
    "lowrank_expert_topk": _LOWRANK_EXPERT_KEYS,
    "lowrank_expert_jumprelu": _LOWRANK_EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
    "lowrank_expert_residual": _LOWRANK_EXPERT_RESIDUAL_KEYS,
    "two_stage_residual_expert": {
        "STAGE1_RATIO",
        "NUM_EXPERTS",
        "ACTIVE_EXPERTS",
        "LATENTS_PER_EXPERT",
    },
    "lowrank_gated_residual": _LOWRANK_KEYS,
    "lowrank_jumprelu_residual": _LOWRANK_KEYS,
    "lowrank_grouped_residual": _LOWRANK_KEYS,
    "lowrank_adaptive_budget_residual": _LOWRANK_KEYS,
    "bucketed_lowrank_residual": _LOWRANK_KEYS,
    "whitened_lowrank_residual": _LOWRANK_KEYS,
    "whitened_lowrank_gated_residual": _LOWRANK_KEYS,
    "lowrank_multi_branch_residual": _LOWRANK_KEYS,
    "lowrank_factorized_residual": {"TRUNK_RANK", "FACTORIZED_HIDDEN_DIM"},
    "lowrank_two_stage_residual": _TWOSTAGE_KEYS,
    "routed_lowrank_two_stage_residual": _TWOSTAGE_KEYS,
    "lowrank_residual_vq": {"TRUNK_RANK", "NUM_CODES"},
    "lowrank_soft_codebook_residual": {"TRUNK_RANK", "NUM_CODES"},
    "lowrank_grouped_soft_codebook_residual": {"TRUNK_RANK", "NUM_CODES"},
    "lowrank_gated_soft_codebook_residual": {"TRUNK_RANK", "NUM_CODES"},
    "lowrank_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
    "bucketed_lowrank_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
    "whitened_lowrank_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
    "lowrank_asymmetric_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
    "routed_lowrank_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
    "routed_lowrank_asymmetric_two_stage_soft_codebook_residual": _SOFT_CODEBOOK_TWOSTAGE_KEYS,
}

STRUCTURAL_OVERRIDE_KEYS = {
    "ARCHITECTURE",
    "K",
    "EXPANSION_FACTOR",
    "NUM_GROUPS",
    "ACTIVE_GROUPS",
    "MATRYOSHKA_KS",
    "RESIDUAL_FROM",
    "TRUNK_RANK",
    "NUM_CODES",
    "STAGE1_RATIO",
    "FACTORIZED_HIDDEN_DIM",
    "NUM_EXPERTS",
    "ACTIVE_EXPERTS",
    "LATENTS_PER_EXPERT",
}

DYNAMIC_OVERRIDE_KEYS = {
    "LR",
    "AUXK_ALPHA",
    "JUMPRELU_INIT_THRESHOLD",
    "JUMPRELU_BANDWIDTH",
    "GATED_TEMPERATURE",
    "GATED_INIT_LOGIT",

exec
/bin/bash -lc "rg -n \"valid_archs|architecture == \\\"product_key_expert_jumprelu\\\"|SharedTwoStageResidualExpertSparseCoder|ProductKeyExpertJumpReLUSparseCoder\" sparsify/config.py sparsify/sparse_coder.py" in /root/sparsify-ascend
 succeeded in 0ms:
sparsify/sparse_coder.py:450:    if architecture == "product_key_expert_jumprelu":
sparsify/sparse_coder.py:451:        return ProductKeyExpertJumpReLUSparseCoder
sparsify/sparse_coder.py:461:        return SharedTwoStageResidualExpertSparseCoder
sparsify/sparse_coder.py:1494:class ProductKeyExpertJumpReLUSparseCoder(SparseCoder):
sparsify/sparse_coder.py:2091:class SharedTwoStageResidualExpertSparseCoder(SparseCoder):
sparsify/config.py:215:        valid_archs = (
sparsify/config.py:272:        if self.sae.architecture not in valid_archs:
sparsify/config.py:275:                f"Must be one of {valid_archs}"

codex
准备落盘新 family `shared_product_key_expert_jumprelu`。它不是对已有 `shared_two_stage_residual_expert` 的换名，而是把第二阶段的 flat router 换成 product-key compositional routing，同时保留 shared coarse stage 和 expert 内 JumpReLU cleanup，用来直接测试 “shared/coarse + PK micro-experts” 这个还没被当前实现覆盖的槽位。
apply patch
patch: completed
/root/sparsify-ascend/research/AutoResearch/override_registry.py
/root/sparsify-ascend/sparsify/config.py
/root/sparsify-ascend/sparsify/sparse_coder.py
diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

codex
代码已落盘到 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`。现在做最小自检：exact architecture string 的 instantiate/forward/backward smoke、成本估算，以及 `ARCHITECTURE` override allowlist 校验，确认它不是静默注册残缺。
exec
/bin/bash -lc "python - <<'PY'
from research.AutoResearch.override_registry import validate_env_overrides
print(validate_env_overrides([{'key':'ARCHITECTURE','value':'shared_product_key_expert_jumprelu'}], fallback_architecture='expert_jumprelu'))
print(validate_env_overrides([
 {'key':'ARCHITECTURE','value':'shared_product_key_expert_jumprelu'},
 {'key':'STAGE1_RATIO','value':'0.25'},
 {'key':'NUM_EXPERTS','value':'128'},
 {'key':'ACTIVE_EXPERTS','value':'2'},
 {'key':'LATENTS_PER_EXPERT','value':'192'}
], fallback_architecture='expert_jumprelu'))
PY" in /root/sparsify-ascend
 succeeded in 0ms:
[]
[]

exec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify import SparseCoder
from sparsify.config import SparseCoderConfig
arch='shared_product_key_expert_jumprelu'
cfg=SparseCoderConfig(
    architecture=arch,
    k=32,
    expansion_factor=1,
    num_experts=128,
    active_experts=2,
    latents_per_expert=192,
    stage1_ratio=0.25,
)
sae=SparseCoder(1024, cfg, device='cpu', dtype=torch.float32)
x=torch.randn(4,1024)
out=sae(x)
out.fvu.backward()
print('integration_smoke: OK', arch, out.fvu.item())
PY" in /root/sparsify-ascend
exec
/bin/bash -lc "python - <<'PY'
from sparsify.sparse_coder import _get_sae_class
from sparsify.config import SparseCoderConfig
D=1024; N=4096
arch='shared_product_key_expert_jumprelu'
cfg=SparseCoderConfig(architecture=arch,k=32,expansion_factor=1,num_experts=128,active_experts=2,latents_per_expert=192,stage1_ratio=0.25)
m=_get_sae_class(arch)(D,cfg,device='cpu')
print(m.selection_cost_estimate(N))
PY" in /root/sparsify-ascend
diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

 succeeded in 4197ms:
Using fused decoder on CUDA.
{'total_accesses': 613376, 'original_matmul_accesses': 4194304, 'ratio': 0.15, 'budget_ratio': 0.1, 'feasible': True, 'breakdown': [{'name': 'left_router', 'accesses': 11264, 'shape': '1024x11'}, {'name': 'right_router', 'accesses': 12288, 'shape': '1024x12'}, {'name': 'shared_expert_encoder', 'accesses': 196608, 'shape': 'shared=1×1024x192'}, {'name': 'active_expert_encoder', 'accesses': 393216, 'shape': 'active=2×1024x192'}], 'deployment_accesses': 163840, 'deployment_ratio': 0.04, 'deployment_breakdown': [{'name': 'sparse_lookup', 'accesses': 163840, 'shape': 'K=32×(d=1024+n=4096)'}], 'combined_accesses': 777216, 'combined_ratio': 0.19, 'combined_budget_ratio': 0.12, 'combined_feasible': True}

 succeeded in 4459ms:
Using fused decoder on CUDA.
integration_smoke: OK shared_product_key_expert_jumprelu 1.3128962516784668

diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

codex
接线自检通过了：新 architecture 已能 exact string instantiate/forward/backward，`ARCHITECTURE` override allowlist 也已认得，成本口径是 exact `0.185303x`。我再快速核对一下落盘 diff 和门槛，再返回 Round 258 的 JSON。
exec
/bin/bash -lc 'git diff -- sparsify/config.py sparsify/sparse_coder.py research/AutoResearch/override_registry.py' in /root/sparsify-ascend
 succeeded in 0ms:
diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e..9065b6f 100644
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@ ARCHITECTURE_OVERRIDE_KEYS: dict[str, set[str]] = {
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e..53bb68d 100644
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@ class TrainConfig(Serializable):
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d..6c2e432 100644
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@ def _get_sae_class(architecture: str) -> type:
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@ class SharedTwoStageResidualExpertSparseCoder(SparseCoder):
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

exec
/bin/bash -lc "python - <<'PY'
obj_inc=0.3544909989833832
cost=777216/4194304
print('cost',cost)
print('threshold_exceed_to_beat_r235', obj_inc-cost)
PY" in /root/sparsify-ascend
 succeeded in 0ms:
cost 0.185302734375
threshold_exceed_to_beat_r235 0.1691882646083832

exec
/bin/bash -lc "python - <<'PY'
from research.AutoResearch.compatibility import compute_selection_cost
c=compute_selection_cost('shared_product_key_expert_jumprelu',k=32,ef=1,d_in=1024,n_output=4096,extra_config={'num_experts':128,'active_experts':2,'latents_per_expert':192,'stage1_ratio':0.25})
print(c)
PY" in /root/sparsify-ascend
diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

 succeeded in 4690ms:
{'total_accesses': 613376, 'original_matmul_accesses': 4194304, 'ratio': 0.15, 'budget_ratio': 0.1, 'feasible': True, 'breakdown': [{'name': 'left_router', 'accesses': 11264, 'shape': '1024x11'}, {'name': 'right_router', 'accesses': 12288, 'shape': '1024x12'}, {'name': 'shared_expert_encoder', 'accesses': 196608, 'shape': 'shared=1×1024x192'}, {'name': 'active_expert_encoder', 'accesses': 393216, 'shape': 'active=2×1024x192'}], 'deployment_accesses': 163840, 'deployment_ratio': 0.04, 'deployment_breakdown': [{'name': 'sparse_lookup', 'accesses': 163840, 'shape': 'K=32×(d=1024+n=4096)'}], 'combined_accesses': 777216, 'combined_ratio': 0.19, 'combined_budget_ratio': 0.12, 'combined_feasible': True}

diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

exec
/bin/bash -lc "python - <<'PY'
import json
r235=json.load(open('research/history/round_summaries/round_0235.json'))
obj=float(r235['result']['objective_score'])
cost=777216/4194304
print(obj, cost, obj-cost)
PY" in /root/sparsify-ascend
 succeeded in 0ms:
0.358553 0.185302734375 0.173250265625

diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

codex
{"command":"run","hypothesis":"候选 family 设为 `shared_product_key_expert_jumprelu`。最终重构公式为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh(x) u_i + Σ_{e∈E_pk(r_1(x))} Σ_{j∈S_e^τ(r_1(x))} γ_{e,j}(x) b_{e,j}`，其中 `r_1(x)=x-Σ_{i∈S_sh(x)} α_i^sh(x) u_i`，`{u_i}` 是 always-on shared coarse 静态向量库，`{b_{e,j}}` 是各 routed expert 的静态子库原子，`E_pk(r_1(x))` 由 left/right product-key router 给出组合式 expert addressing，expert 内再用 JumpReLU 形成局部 support。导出到 LUTurbo/Lottable 的在线链路仍是“shared coarse 子库查表/加权和 -> residual 上做 product-key expert 选择 -> expert 内 JumpReLU sparse support -> 静态库查表加权和 -> 选择性补偿”，静态库不随样本动态生成，因此兼容性判断为扩展兼容。它与现有 `shared_two_stage_residual_expert` 的精确差异不是换名，而是把第二阶段的 flat router 改成了 product-key compositional routing；与 `product_key_expert_jumprelu` 的精确差异则是新增了 always-on shared coarse stage，让 routed micro-experts 只做 residual cleanup。相对 r235 只把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新 family，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4`、`NUM_EXPERTS=128`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=192`、`STAGE1_RATIO=0.25` 与其余主 recipe 不变，直接回答当前 prompt 里最高优先级但尚未被现有实现正面覆盖的问题：`shared/coarse + PK-routed micro-experts` 这种不对称分工，是否能比 plain routed / shared+flat-router / router-only 结构更慢地恶化 small-K exceed。按本地 exact 成本复核，该 matched probe 的 selection_accesses=613376、deployment_accesses=163840、combined_accesses=777216、exact total_cost_ratio=0.185302734375，仍远低于 1.5×h×n 硬预算；若要打赢 r235 在当前 controller 口径下的 exact objective=0.358553，则需要把 `exceed_alpha_0.50` 压到 `0.173250265625` 以下。门槛不低，但它是当前最贴近“shared/coarse 主干 + compositional tiny cleanup experts”主问题、且具有明确新信息增量的 matched architecture probe。","summary":"已实际新增并注册 `shared_product_key_expert_jumprelu`。建议以 r235 为 reference，只改 `ARCHITECTURE=shared_product_key_expert_jumprelu`，做一轮新的 matched architecture probe，直接判断“shared coarse library + product-key routed micro-experts”这个结构槽位本身是否值得继续。","change_type":"edit_sae_code","expected_win":"explore_unknown","family_name":"shared_product_key_expert_jumprelu","family_stage":"prototype","self_review":"这轮满足 Round 258 的核心约束：只做 1 个 architecture probe，并且相对 reference 只改 `ARCHITECTURE`。之所以不继续回到 `product_key_expert_jumprelu` 或 `expert_jumprelu` 上补参数点，是因为 round257 已经说明 `K=48` 这种同 family deploy-knee 延伸没有把 objective 拉过 r251；而 Round 258 明确要求切回结构槽位判断。之所以不选 `shared_two_stage_residual_expert`、`shared_lowrank_*`、`factorized_expert_topk` 这些已有 family，是因为它们在当前 target 的相近 matched lane 上都已经给过较清晰负信号；继续重复它们只会得到低信息增量。新 family 的价值在于它正好补上当前实现缺失的组合：`shared coarse stage` 先吃平滑主干，第二阶段不是 flat router，而是 `product-key` compositional routing 选 very-short-path micro-experts 做 residual cleanup。这比简单重测 `shared_two_stage_residual_expert` 更能回答 prompt 里关于 `product-key / compositional dictionary + shared/residual cleanup` 的核心问题。主要风险也很清楚：exact cost `0.1853x` 高于 r235 的 `0.1641x`，所以如果它不能把 exceed 至少压到约 `0.1733` 以下，就仍然不够构成正收益；但这正是本轮 probe 要干净回答的结构性问题。","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"shared_product_key_expert_jumprelu"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py","research/AutoResearch/override_registry.py"],"notes_to_memory":["已实际新增并注册 `shared_product_key_expert_jumprelu`，真实落盘文件只有 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`；没有新增 tunable env key，也没有改 `scripts/autoresearch_test.sh`。","新 family 的结构语义是 `shared coarse sparse stage + product-key routed JumpReLU residual experts`：这不是 `shared_two_stage_residual_expert` 的别名，因为第二阶段从 flat full router 换成了 compositional product-key expert addressing；也不是 `product_key_expert_jumprelu` 的别名，因为它显式增加了 always-on shared coarse branch。","本地 exact architecture smoke 已通过：`SparseCoderConfig(architecture='shared_product_key_expert_jumprelu', k=32, expansion_factor=1, num_experts=128, active_experts=2, latents_per_expert=192, stage1_ratio=0.25)` 在 CPU 上可成功 instantiate、forward、backward。","本地 override allowlist 自检已通过：`validate_env_overrides([{key:'ARCHITECTURE', value:'shared_product_key_expert_jumprelu'}], fallback_architecture='expert_jumprelu')` 返回空列表；连同 `STAGE1_RATIO/NUM_EXPERTS/ACTIVE_EXPERTS/LATENTS_PER_EXPERT` 的组合校验也返回空列表。","本地 exact 成本复核：`shared_product_key_expert_jumprelu K=32 EF=1 NUM_EXPERTS=128 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=192 STAGE1_RATIO=0.25` 的 selection_accesses=613376、deployment_accesses=163840、combined_accesses=777216、exact total_cost_ratio=0.185302734375；打赢 r235 exact objective=0.358553 所需阈值为 `exceed_alpha_0.50 < 0.173250265625`。"],"next_hypotheses":["如果 `shared_product_key_expert_jumprelu` 在 matched lane 上把 exceed 明显压低并接近或打赢 r235，则下一轮应继续留在该 family，只改 `STAGE1_RATIO`，判断 shared/coarse 与 PK residual cleanup 的预算分工是不是主控制旋钮。","如果它的 FVU 改善但 objective 仍不够，说明“shared coarse + compositional router”本身还不足以关闭当前 small-K exceed 缺口；后续若继续追这条线，应优先想办法进一步缩短 shared stage 或细化 micro-expert 容量，而不是回到 flat-router shared family 重复试验。","如果它在 objective 上仍明显输给 r235，尤其是 exceed 仍停在 `0.18+` 区间，则可更明确地下调 `shared/coarse + routed cleanup` 这条现有 family 族在当前 target 上的优先级；之后应把结构预算转向别的 genuinely new slot，而不是继续在 shared residual 变体里细扫。"],"reference_round":235}
diff --git a/research/AutoResearch/override_registry.py b/research/AutoResearch/override_registry.py
index c9c343e5badb24b3e8a924a7b9e831faeb96c5b3..9065b6f70da048029b9f83f52d93f7c30488d536
--- a/research/AutoResearch/override_registry.py
+++ b/research/AutoResearch/override_registry.py
@@ -53,6 +53,7 @@
     "expert_topk": _EXPERT_KEYS,
     "expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "product_key_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
+    "shared_product_key_expert_jumprelu": _EXPERT_KEYS | {"STAGE1_RATIO", "JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "hashed_expert_jumprelu": _EXPERT_KEYS | {"JUMPRELU_INIT_THRESHOLD", "JUMPRELU_BANDWIDTH"},
     "shared_routed_expert_topk": _EXPERT_KEYS,
     "shared_two_stage_residual_expert": _EXPERT_KEYS | {"STAGE1_RATIO"},
diff --git a/sparsify/config.py b/sparsify/config.py
index 2a4be4e9ef889c50b793e685401987a79e6e4bd8..53bb68d7aafc8bf52dd28925e8bc4e08761fb920
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -227,6 +227,7 @@
             "expert_topk",
             "expert_jumprelu",
             "product_key_expert_jumprelu",
+            "shared_product_key_expert_jumprelu",
             "adaptive_active_expert_jumprelu",
             "hashed_expert_jumprelu",
             "factorized_router_expert_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index d6c4e9d8f1e8aca096b6c4e2e1b3d32f8db59775..6c2e4329c6d0600ced463e471ef6bc1f36a536f3
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -449,6 +449,8 @@
         return ExpertJumpReLUSparseCoder
     if architecture == "product_key_expert_jumprelu":
         return ProductKeyExpertJumpReLUSparseCoder
+    if architecture == "shared_product_key_expert_jumprelu":
+        return SharedProductKeyExpertJumpReLUSparseCoder
     if architecture == "adaptive_active_expert_jumprelu":
         return AdaptiveActiveExpertJumpReLUSparseCoder
     if architecture == "hashed_expert_jumprelu":
@@ -2337,6 +2339,290 @@
         )
 
 
+class SharedProductKeyExpertJumpReLUSparseCoder(SparseCoder):
+    """Shared sparse coarse stage followed by PK-routed JumpReLU expert cleanup."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        nn.Module.__init__(self)
+        self.cfg = cfg
+        self.d_in = d_in
+        (
+            _base_num_latents,
+            self.num_experts,
+            self.latents_per_expert,
+            self.active_experts,
+        ) = _resolve_expert_layout(cfg, d_in)
+        self.num_shared_experts = 1
+        self.shared_num_latents = self.num_shared_experts * self.latents_per_expert
+        self.expert_num_latents = self.num_experts * self.latents_per_expert
+        self.num_latents = self.shared_num_latents + self.expert_num_latents
+
+        if cfg.stage1_ratio is not None:
+            self.stage1_k = max(1, round(cfg.k * cfg.stage1_ratio))
+        else:
+            self.stage1_k = max(1, cfg.k // 2)
+        self.stage2_k = max(1, cfg.k - self.stage1_k)
+
+        if self.stage1_k > self.shared_num_latents:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage1_k <= shared_experts * latents_per_expert, "
+                f"got stage1_k={self.stage1_k}, "
+                f"shared_experts={self.num_shared_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+        if self.stage2_k > self.active_experts * self.latents_per_expert:
+            raise ValueError(
+                "shared_product_key_expert_jumprelu requires "
+                "stage2_k <= active_experts * latents_per_expert, "
+                f"got stage2_k={self.stage2_k}, "
+                f"active_experts={self.active_experts}, "
+                f"latents_per_expert={self.latents_per_expert}"
+            )
+
+        self.shared_encoders = nn.Parameter(
+            torch.empty(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.shared_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_shared_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.left_keys, self.right_keys = _product_key_pair_grid(self.num_experts)
+        pair_ids = torch.arange(self.num_experts, device=device)
+        self.register_buffer("pair_left_index", pair_ids // self.right_keys)
+        self.register_buffer("pair_right_index", pair_ids % self.right_keys)
+
+        self.left_router = nn.Linear(
+            d_in, self.left_keys, device=device, dtype=dtype
+        )
+        self.right_router = nn.Linear(
+            d_in, self.right_keys, device=device, dtype=dtype
+        )
+        self.expert_encoders = nn.Parameter(
+            torch.empty(
+                self.num_experts,
+                self.latents_per_expert,
+                d_in,
+                device=device,
+                dtype=dtype,
+            )
+        )
+        self.expert_encoder_bias = nn.Parameter(
+            torch.zeros(
+                self.num_experts,
+                self.latents_per_expert,
+                device=device,
+                dtype=dtype,
+            )
+        )
+
+        nn.init.kaiming_uniform_(self.shared_encoders, a=5**0.5)
+        nn.init.kaiming_uniform_(self.left_router.weight, a=5**0.5)
+        nn.init.kaiming_uniform_(self.right_router.weight, a=5**0.5)
+        self.left_router.bias.data.zero_()
+        self.right_router.bias.data.zero_()
+        nn.init.kaiming_uniform_(self.expert_encoders, a=5**0.5)
+
+        param_dtype = self.expert_encoder_bias.dtype
+        threshold = torch.full(
+            (self.num_experts, self.latents_per_expert),
+            cfg.jumprelu_init_threshold,
+            device=self.expert_encoder_bias.device,
+            dtype=param_dtype,
+        ).clamp_min(torch.finfo(param_dtype).tiny)
+        self.log_threshold = nn.Parameter(torch.log(torch.expm1(threshold)))
+
+        if decoder:
+            shared_decoder = self.shared_encoders.data.reshape(
+                self.shared_num_latents, d_in
+            )
+            expert_decoder = self.expert_encoders.data.reshape(
+                self.expert_num_latents, d_in
+            )
+            decoder_init = torch.cat((shared_decoder, expert_decoder), dim=0)
+            self.W_dec = nn.Parameter(decoder_init.clone())
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+        self.b_dec = nn.Parameter(torch.zeros(d_in, dtype=dtype, device=device))
+
+    @property
+    def threshold(self) -> Tensor:
+        return F.softplus(self.log_threshold)
+
+    def _encoder_linear_layers(self):
+        return [("left_router", self.left_router), ("right_router", self.right_router)]
+
+    def _extra_encode_accesses(self) -> list[tuple[str, int, str]]:
+        return [
+            (
+                "shared_expert_encoder",
+                self.num_shared_experts * self.d_in * self.latents_per_expert,
+                f"shared={self.num_shared_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+            (
+                "active_expert_encoder",
+                self.active_experts * self.d_in * self.latents_per_expert,
+                f"active={self.active_experts}×{self.d_in}x{self.latents_per_expert}",
+            ),
+        ]
+
+    def _decode_sparse(self, acts: Tensor, indices: Tensor) -> Tensor:
+        assert self.W_dec is not None, "Decoder weight was not initialized."
+        return decoder_impl(indices, acts.to(self.dtype), self.W_dec.mT)
+
+    def _encode_shared_stage(
+        self, x_centered: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = x_centered.shape[:-1]
+        flat_x = x_centered.reshape(-1, self.d_in)
+        shared_pre_acts = (
+            torch.einsum("bd,sld->bsl", flat_x, self.shared_encoders)
+            + self.shared_encoder_bias.unsqueeze(0)
+        )
+        shared_acts = F.relu(shared_pre_acts).reshape(-1, self.shared_num_latents)
+
+        top_acts, top_indices = torch.topk(
+            shared_acts, self.stage1_k, dim=-1, sorted=False
+        )
+        target_shape = (*original_shape, self.stage1_k)
+        acts_shape = (*original_shape, self.shared_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            shared_acts.reshape(acts_shape),
+        )
+
+    def _encode_expert_stage(
+        self, residual: Tensor
+    ) -> tuple[Tensor, Tensor, Tensor]:
+        original_shape = residual.shape[:-1]
+        flat_x = residual.reshape(-1, self.d_in)
+
+        left_logits = self.left_router(flat_x)
+        right_logits = self.right_router(flat_x)
+        expert_logits = (
+            left_logits[:, self.pair_left_index]
+            + right_logits[:, self.pair_right_index]
+        )
+        selected_expert_idx, selected_probs = _select_active_expert_indices(
+            expert_logits, self.active_experts
+        )
+        selected_weight = self.expert_encoders[selected_expert_idx]
+        selected_bias = self.expert_encoder_bias[selected_expert_idx]
+        selected_threshold = self.threshold[selected_expert_idx]
+        pre_acts = torch.einsum("bd,bald->bal", flat_x, selected_weight) + selected_bias
+        positive = F.relu(pre_acts)
+        gate = torch.sigmoid(
+            (positive - selected_threshold) / self.cfg.jumprelu_bandwidth
+        )
+        acts = positive * gate * selected_probs.unsqueeze(-1)
+        top_acts, top_indices, full_acts = _finalize_routed_expert_acts(
+            acts,
+            selected_expert_idx,
+            self.stage2_k,
+            self.latents_per_expert,
+            self.expert_num_latents,
+            index_offset=self.shared_num_latents,
+        )
+
+        target_shape = (*original_shape, self.stage2_k)
+        acts_shape = (*original_shape, self.expert_num_latents)
+        return (
+            top_acts.reshape(target_shape),
+            top_indices.reshape(target_shape),
+            full_acts.reshape(acts_shape),
+        )
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+        return EncoderOutput(combined_acts, combined_indices, combined_full)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        stage1_acts, stage1_indices, stage1_full = self._encode_shared_stage(
+            x_centered
+        )
+        stage1_out = self._decode_sparse(stage1_acts, stage1_indices)
+
+        residual = x_centered - stage1_out
+        stage2_acts, stage2_indices, stage2_full = self._encode_expert_stage(
+            residual
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_full = torch.cat((stage1_full, stage2_full), dim=-1)
+
+        sparse_out = self._decode_sparse(combined_acts, combined_indices)
+        sae_out = sparse_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], combined_full, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = self._decode_sparse(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class FactorizedExpertTopKSparseCoder(SparseCoder):
     """Route into an expert-local top-k head after a shared low-rank projection."""
 

tokens used
444,043
