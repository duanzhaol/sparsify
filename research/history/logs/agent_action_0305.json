{"command":"run","hypothesis":"最终重构公式保持为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh b_i^sh + Σ_{j∈S_{e1(r1)}^τ(r1)} β_j c_{e1,j} + Σ_{m∈S_{e2(r1)}^τ(r2)} γ_m c_{e2,m}`，其中 `r1(x)=x-shared(x)`，`r2(x)=r1(x)-expert1_cleanup(r1)`，`(e1,e2)` 是同一次 product-key 路由在 `r1` 上选出的有序 top-2 静态 expert 子库。在线导出仍是“shared 静态库查表 + PK 选择静态 expert 子库 + 有限加权和 + 选择性补偿”，静态库不随样本动态生成，因此兼容性判断为 `直接兼容`。相对 r304，本轮只改 `NUM_EXPERTS=3072`，其余 `ARCHITECTURE=shared_cascade_product_key_expert_jumprelu, K=32, EF=1, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.25, OPTIMIZER=adam, LR=8e-4` 保持不变；主假设是：在刚刚验证有效的 ordered cascade scaffold 上，把 routed micro-expert bank 从 2048 细化到 3072，能比继续扫 `K/STAGE1_RATIO` 更直接扩大 tail-specialized 容量，从而把 `exceed_alpha_0.50` 再向下压一格，并换回比新增 encoder cost 更大的 objective 改善。该配置本地精确成本复核为 `selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`，仍稳定位于当前 `0.08~0.14` 主战场并满足预算；若以 controller/memory 当前记录的 r304 精确结果 `objective=0.267223` 为基准，要击败它需要把 `exceed_alpha_0.50` 压到 `0.160045265625` 以下。","summary":"建议以 r304 为 reference，只改 `NUM_EXPERTS=3072`。这是对当前新 winning scaffold 的一个高信息容量点验证：不是继续扫低信息的 `K/STAGE1_RATIO` 邻域，而是直接测试 finer routed micro-experts 是否能在 cascade cleanup 语义上继续压低 hard-token exceed。","change_type":"param_only","expected_win":"lower_objective","family_name":"shared_cascade_product_key_expert_jumprelu","family_stage":"prototype","self_review":"这轮严格满足单变量原则：相对 r304 的完整配置只改 `NUM_EXPERTS`。选择这条线而不是立刻再开新 family，有两个原因：一是当前 policy/历史上新 family 仍存在 incubation 风险，二是 r304 刚把 cascade scaffold 抬成当前 objective anchor，此时最有价值的不是回到 `K/STAGE1_RATIO` 细扫，而是回答一个仍未闭环的高优先级问题：更细粒度 routed micro-experts 是否会在 ordered cascade cleanup 上兑现为更低的 tail exceed。主要风险也很清楚：2048->3072 可能只是重复 fixed-AE 线上“cost 上升但 exceed 改善有限”的模式；如果如此，这轮就应被视为给该容量轴划出 knee，而不是继续线性扩 `NUM_EXPERTS`。","needs_sanity":false,"env_overrides":[{"key":"NUM_EXPERTS","value":"3072"}],"touched_files":[],"notes_to_memory":["r304 `shared_cascade_product_key_expert_jumprelu @ NUM_EXPERTS=2048` 的 controller/memory 精确记录为 objective=0.267223、cost=0.102295x、exceed=0.164929、fvu=0.15110117197036743；后续阈值判断应以这组 exact 值为准，而不是 prompt 头部的四舍五入摘要。","本轮选择 `NUM_EXPERTS=3072` 是在当前 best scaffold 上做一个高信息容量点验证，不是回到 prompt 已下调优先级的 `K/STAGE1_RATIO/LATENTS_PER_EXPERT` 近邻细扫。","本地 override allowlist 校验已通过：`validate_env_overrides([{key:'NUM_EXPERTS', value:'3072'}], fallback_architecture='shared_cascade_product_key_expert_jumprelu') == []`。","本地 exact-string smoke 已通过：`shared_cascade_product_key_expert_jumprelu` 在 `K=32, EF=1, NUM_EXPERTS=3072, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.25` 下可 instantiate、forward、backward。","本地精确成本复核：`selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`；若要击败 r304 的 `objective=0.267223`，需要 `exceed_alpha_0.50 <= 0.160045265625`，即相对当前 `0.164929` 还需再下降约 `0.00488`。","这轮若失败，应更倾向于认定当前 cascade scaffold 的主瓶颈不只是 routed bank 粒度，而需要新的条件触发 cleanup 语义；不应继续在线性扩 `NUM_EXPERTS` 上消耗预算。"],"next_hypotheses":["如果该点把 `exceed_alpha_0.50` 压到约 `0.160` 或更低并刷新 objective，则下一轮保留该 family，只改 `STAGE1_RATIO` 做一次最小 knee 校准，判断 shared/coarse 与 cascade cleanup 的预算分工是否还能继续压 exceed。","如果该点 exceed 略降但 objective 仍未优于 r304，则把 `NUM_EXPERTS=3072` 视为这条 scaffold 上的容量 knee；后续不再继续线性扩 bank，而应优先切到新的条件触发 cleanup 结构槽位。","如果该点与 r304 持平或更差，则可更明确地下调“更多 micro-experts 本身就能修复 low-K tail exceed”这一假设的优先级；下一轮默认应优先换结构语义，而不是继续留在同 family。"],"reference_round":304}