{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_two_stage_soft_codebook_residual"},{"key":"EXPANSION_FACTOR","value":"2"},{"key":"K","value":"128"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.0e-3"},{"key":"AUXK_ALPHA","value":"0.015625"}],"expected_win":"lower_fvu","experiment_tier":"full","family_name":"lowrank_two_stage_soft_codebook_residual","family_stage":"mainline","hypothesis":"最终重构公式保持为 x_hat = U(Vx) + Σ_m pi_m c_m + Σ_i alpha_i^(1) b_i + Σ_j alpha_j^(2) b_j。low-rank trunk、soft codebook、两阶段 residual 都是静态向量库上的有限加权和，因此可导出到 LUTurbo/Lottable 的“查表 + 加权求和 + 选择性补偿”推理链路；该 family 属于扩展兼容。当前 EF=2 已稳定满足 encoder 选择成本硬约束，`signum + LR=3.0e-3` 是已复现的低成本主线锚点，而 `adam` 在同配方下直接爆炸，说明 optimizer 分支可暂时关闭。按主线顺序，下一步应进入 loss/preprocess 轴；本轮只改一个主变量 `AUXK_ALPHA`，将其从默认 `0.03125` 下调到 `0.015625`，回答唯一问题：在低 EF 可部署 regime 下，较弱的辅助稀疏正则是否能减少对主重构目标的干扰，从而改善 FVU，同时几乎不增加训练运行代价。","needs_sanity":false,"next_hypotheses":["如果较低 `AUXK_ALPHA` 改善终点或曲线形状，且 `tokens_per_sec`、总耗时、`final_total_tokens` 基本不变，则再做一次 clean baseline 复现确认。","如果较低 `AUXK_ALPHA` 无益或退化，则关闭该 loss 分支，保留默认 `0.03125` 并再考虑其他轻量 loss/preprocess 轴。","在 loss/preprocess 轴未得出清晰信号前，不改 K，也不改 TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM。"],"notes_to_memory":["兼容性判断：`lowrank_two_stage_soft_codebook_residual` 为扩展兼容；导出形式不变，仍是 trunk 库 + codebook 库 + residual 字典库的有限加权和。","Optimizer 分支结论：`adam` 在 EF=2、LR=3.0e-3 下出现爆炸性退化，应回到 `signum + LR=3.0e-3` 主线锚点。","本轮严格单变量，只改 `AUXK_ALPHA`；这是 loss 轴上的轻量改动，不会改变 encoder 选择成本，也不应显著增加训练时长。重点继续检查曲线形状、`tokens_per_sec`、总耗时与 `final_total_tokens`。"],"primary_variable":"auxk_alpha","self_review":"这符合当前主线顺序：EF 已确认在预算内，clean baseline 与 LR 轴已完成，optimizer 分支也因 `adam` 爆炸而得到负结论，因此现在应转向 loss/preprocess。选择 `AUXK_ALPHA` 而不是更重的 preprocess 变化，是因为它改动最小、归因最清晰、运行代价最低。","summary":"关闭 `adam` 分支，回到已复现的 `signum + LR=3.0e-3` 主线配方，仅将 `AUXK_ALPHA` 从 `0.03125` 下调到 `0.015625`，测试更弱辅助稀疏正则能否在低成本 EF=2 regime 下进一步改善 FVU。","touched_files":[]}