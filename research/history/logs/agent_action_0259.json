{"command":"run","hypothesis":"候选 family 设为 `shared_product_key_factorized_expert_topk`。最终重构公式为 `x_hat = Σ_{i∈S_sh(h(x))} α_i^sh(x) u_i + Σ_{e∈E_pk(h(x))} Σ_{j∈S_e(h(x))} γ_{e,j}(x) b_{e,j}`，其中 `h(x)` 是共享 low-rank bottleneck，`{u_i}` 是 always-on shared 静态向量库，`{b_{e,j}}` 是各 routed factorized expert 的静态子库原子，`E_pk(h(x))` 由 left/right product-key router 在 expert 对上给出组合式寻址。导出到 LUTurbo/Lottable 的在线链路仍是“共享 low-rank bottleneck -> shared 子库查表/加权和 + product-key expert 选择 -> routed tiny factorized expert 子库查表/加权和 -> 选择性补偿”，静态库不随样本动态生成，因此兼容性判断为直接兼容。它与现有 `shared_routed_factorized_expert_topk` 的精确差异不是重命名，而是把 flat `hidden_dim -> num_experts` router 换成了 compositional product-key routing；与刚失败的 `shared_product_key_expert_jumprelu` 的精确差异则是把高成本的 full-dim residual expert 头换成 tiny factorized expert heads，明确测试“PK 路由能否救活 shared bottleneck + tiny experts 这条低成本槽位”。相对 r235 只把 `ARCHITECTURE` 从 `expert_jumprelu` 切到新 family，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4`、`NUM_EXPERTS=128`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=192`、`FACTORIZED_HIDDEN_DIM=256` 与其余主 recipe 不变，直接回答当前高优先级但尚未被当前实现覆盖的问题：当 shared bottleneck 与 tiny routed experts 已知本身都偏弱时，product-key compositional routing 是否足以把这条 low-cost lane 拉回到值得继续的 objective 区间。按本地 exact 成本复核，该 matched probe 的 selection_accesses=415488、deployment_accesses=163840、combined_accesses=579328、exact total_cost_ratio=0.1381072998046875，仍明显低于预算；若要打赢本地 r235 exact objective=0.358553，则需要把 `exceed_alpha_0.50` 压到 `0.22044570019531247` 以下。这个门槛明显低于 round258 那条 `shared_product_key_expert_jumprelu` 的 `0.1733` 门槛，因此它是当前更合理的“shared/product-key/tiny-expert” matched slot closure probe。","summary":"已实际新增并注册 `shared_product_key_factorized_expert_topk`。建议以 r235 为 reference，只改 `ARCHITECTURE=shared_product_key_factorized_expert_topk`，做一轮新的 matched architecture probe，直接判断“shared bottleneck + PK routing + tiny factorized experts”这个低成本结构槽位本身值不值得继续。","change_type":"edit_sae_code","expected_win":"explore_unknown","family_name":"shared_product_key_factorized_expert_topk","family_stage":"prototype","self_review":"这轮继续遵守 Round 259 的核心约束：只做 1 个 architecture probe，并且相对 reference 只改 `ARCHITECTURE`。之所以不继续重复 `shared_product_key_expert_jumprelu` 或其他已测 shared/coarse family，是因为 round258 已给出清晰负信号：shared coarse + PK residual full heads 在 exact `0.1853x` 下仍只把 exceed 压到 `0.1829`，objective 明显不够；继续沿那条线补参数会变成 prompt 明确下调优先级的低信息细扫。之所以不回到 `shared_routed_factorized_expert_topk` 原 family，是因为它在同 lane 的旧结果很差，但旧结论并不能回答“问题到底在 shared+tiny experts，还是在 flat router”。新 family 的信息增量正好在这里：保持 shared + tiny factorized experts 不变，只把路由机制换成 product-key compositional addressing。主要风险也很清楚：如果 factorized expert heads 本身就是主要瓶颈，那么即使 PK router 降低选择难度，也可能仍然挡不住 exceed；但这正是本轮要干净回答的结构问题。","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"shared_product_key_factorized_expert_topk"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py","research/AutoResearch/override_registry.py"],"notes_to_memory":["round258 已给出 `shared_product_key_expert_jumprelu` 的清晰负信号：objective=0.368166、cost=0.185303x、exceed=0.182863；说明“shared coarse + PK residual full heads”在当前 matched lane 下不值得继续主导预算。","已实际新增并注册 `shared_product_key_factorized_expert_topk`，真实落盘文件只有 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`；没有新增 tunable env key，也没有改 `scripts/autoresearch_test.sh`。","新 family 的结构语义是 `shared factorized branch + product-key routed factorized experts`：这不是 `shared_routed_factorized_expert_topk` 的别名，因为 routed expert 选择从 flat hidden router 改成了 compositional product-key expert addressing；也不是 `shared_product_key_expert_jumprelu` 的别名，因为 routed expert 头从 full-dim JumpReLU cleanup 换成了 tiny factorized heads。","本地 exact architecture smoke 已通过：`SparseCoderConfig(architecture='shared_product_key_factorized_expert_topk', k=32, expansion_factor=1, num_experts=128, active_experts=2, latents_per_expert=192, factorized_hidden_dim=256)` 在 CPU 上可成功 instantiate、forward、backward。","本地 override allowlist 自检已通过：`validate_env_overrides([{key:'ARCHITECTURE', value:'shared_product_key_factorized_expert_topk'}], fallback_architecture='expert_jumprelu')` 返回空列表；连同 `NUM_EXPERTS/ACTIVE_EXPERTS/LATENTS_PER_EXPERT/FACTORIZED_HIDDEN_DIM` 的组合校验也返回空列表。","本地 exact 成本复核：`shared_product_key_factorized_expert_topk K=32 EF=1 NUM_EXPERTS=128 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=192 FACTORIZED_HIDDEN_DIM=256` 的 selection_accesses=415488、deployment_accesses=163840、combined_accesses=579328、exact total_cost_ratio=0.1381072998046875；打赢本地 r235 exact objective=0.358553 所需阈值为 `exceed_alpha_0.50 < 0.22044570019531247`。","当前工作区真实兼容 frontier 仍以 `research/history/frontier.json` 为准：best compatible point 依然是 r251 `product_key_expert_jumprelu`，而不是 prompt 里的旧 `expert_jumprelu` anchor；本轮只把 r235 当 matched architecture probe 的 patch anchor。"],"next_hypotheses":["如果 `shared_product_key_factorized_expert_topk` surprisingly 把 exceed 压到 `0.22` 以下并在 matched lane 上接近或打赢 r235，则下一轮应继续留在该 family，只改 `FACTORIZED_HIDDEN_DIM`，判断 tiny expert 头宽度是不是这条低成本 PK+shared 线的主控制旋钮。","如果它的 objective 仍明显输给 r235，尤其是 exceed 继续停在 `0.25+` 或更差区间，则可以更明确地下调“shared bottleneck + tiny factorized experts”这一整类结构在当前 target 上的优先级；后续结构预算应回到别的 genuinely new slot，而不是继续围绕 factorized expert 家族细扫。","如果它比 `shared_routed_factorized_expert_topk` 有明显改善但仍不够 competitive，则可把结论记为：product-key router 对 shared+tiny-expert lane 有帮助，但不足以单独解决 objective 缺口；之后若还追这条方向，更可能需要额外的 residual cleanup 或 variable-active-path，而不是继续只换路由器。"],"reference_round":235}