{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"two_stage_residual_expert"}],"expected_win":"explore_unknown","experiment_tier":"full","family_name":"two_stage_residual_expert","family_stage":"mainline","hypothesis":"最终重构公式为 x_hat = Σ_{i∈S_1(x)} α_i(x) b_i + Σ_{e∈E(r_1)} Σ_{j∈S_e(r_1)} γ_{e,j}(x) c_{e,j}，其中 r_1 = x - Σ_{i∈S_1(x)} α_i(x) b_i，B={b_i} 是静态 coarse library，{C_e} 是静态 expert residual 子库。导出到 LUTurbo/Lottable 推理链路时仍是“coarse 查表 + expert residual 子库查表 + 有限加权和 + 选择性补偿”，静态库不随样本动态生成，因此 `two_stage_residual_expert` 判定为扩展兼容；`batch_topk`、`adaptive_budget_topk` 等不兼容 family 仍不能驱动 frontier。唯一主假设：相对 r146 只把 `ARCHITECTURE` 从 `lowrank_expert_residual` 切到 `two_stage_residual_expert`，保持 K=112、EF=1、STAGE1_RATIO=0.25、NUM_EXPERTS=64、ACTIVE_EXPERTS=2、LATENTS_PER_EXPERT=224 以及其余训练配方不变，直接回答“去掉 trunk、改成 coarse library + expert residual”在约 0.48x total_cost 下，是否能在低到中成本带形成比当前 `<0.5x` anchor 更有价值的新 Pareto 段。","needs_sanity":true,"next_hypotheses":["若该点在约 0.48x 下显著优于当前 `<0.5x` 最佳 FVU=0.1010，则下一轮只改 `K` 或 `STAGE1_RATIO`，继续把 trunk-free staged 结构向更低成本带压缩。","若该点仍明显劣于当前 `<0.5x` frontier，则可更明确地下调 `two_stage_residual_expert` 在当前 target 的优先级，后续不再让 trunk-free staged 方向主导预算。","若该点优于该 family 旧结果但仍未进前沿，则说明改进主要来自当前 matched recipe 而非结构本身，后续应转向别的未充分验证 family，而不是继续在该 family 上做多轮细扫。"],"notes_to_memory":["r147 的 `shared_lowrank_routed_expert_residual` 在约 0.55x 成本下仅得到 FVU=0.1038，已基本说明 shared-lowrank hetero residual 这条线在当前压缩 recipe 下没有胜过 r146 或当前 `<0.5x` 最佳点。","本地成本自检：`two_stage_residual_expert` 在 K=112、EF=1、STAGE1_RATIO=0.25、NUM_EXPERTS=64、ACTIVE_EXPERTS=2、LATENTS_PER_EXPERT=224 下 selection_ratio≈0.38、deployment_ratio≈0.11、combined_ratio≈0.48，低于 1.5×h×n 硬约束。","本地 smoke 已过：`two_stage_residual_expert` 在上述 recipe 下可 instantiate 并 forward；这轮是相对 r146 的单变量 architecture swap，不是对 `lowrank_expert_*` 或 `shared_routed_expert_topk` 的局部扫参。","旧的 `two_stage_residual_expert` 结果主要来自 ACTIVE_EXPERTS=1、较宽 LATENTS_PER_EXPERT=320 或更旧的 recipe；本轮用当前 matched recipe 复验，可更干净地回答 trunk-free staged 方向是否还值得保留。"],"reference_round":146,"self_review":"这轮满足单变量原则：相对 r146 只改 `ARCHITECTURE`。在 r147 已给出 shared-lowrank hetero residual 的新负信号后，继续围绕该 family 或 `lowrank_expert_*` 做局部旋钮微调都不符合当前结构扩展优先级；切到 `two_stage_residual_expert` 能用当前最新的 matched recipe 直接测试另一条高优先级但近期未复验的 trunk-free staged 结构。风险是该 family 历史上已有偏弱信号，但当前 recipe 的 ACTIVE_EXPERTS=2、LATENTS_PER_EXPERT=224 与旧结果差异足够大，仍有必要做一次干净的低中成本复验。","summary":"建议以 r146 为 reference，仅设置 `ARCHITECTURE=two_stage_residual_expert`。这是一次低到中成本带的 matched architecture probe：在约 0.48x total_cost 下复验 trunk-free staged residual expert，直接判断“coarse library + expert residual”是否比当前已测的 shared-lowrank hetero residual 更值得继续。","touched_files":[]}