{"change_type":"edit_sae_code","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"shared_product_key_expert_jumprelu_residual"}],"expected_win":"lower_objective","family_name":"shared_product_key_expert_jumprelu_residual","family_stage":"prototype","hypothesis":"最终重构公式设为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh(x) b_i^sh + Σ_{e∈E_pk(r1(x))} Σ_{j∈S_e^τ(r1(x))} α_{e,j}(x) b_{e,j} + Σ_{m∈S_res^τ(r2(x))} γ_m(x) c_m`，其中 `r1(x)=x-Σ_i α_i^sh(x) b_i^sh`，`r2(x)=r1(x)-Σ_{e,j} α_{e,j}(x) b_{e,j}`；`{b_i^sh}` 是 always-on shared 静态子库，`{b_{e,j}}` 是 PK 路由到的静态 expert 子库，`{c_m}` 是 tiny factorized residual 静态子库，最后一段系数 `γ_m(x)` 改为 JumpReLU-gated residual scorer。导出到 LUTurbo/Lottable 时在线链路仍是“shared 查表 -> PK expert 子库查表 -> JumpReLU residual 子库查表 -> 加权求和 -> 选择性补偿”，不涉及动态字典生成，因此该 family 判定为 `扩展兼容`；不兼容 family 仍不得驱动当前 objective leaderboard。本轮唯一主假设是：当前 shared+PK scaffold 的主要缺口在 hard-token tail cleanup，而不是 shared bank 数量或 adaptive-active 轻微插值。相对 r282 只改 `ARCHITECTURE=shared_product_key_expert_jumprelu_residual`，保持 `K=32, EF=1, NUM_EXPERTS=2048, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.25, OPTIMIZER=adam, LR=8e-4` 与其余 recipe 不变，直接测试“把最终 tiny residual cleanup 从 plain factorized top-k 改成 thresholded JumpReLU residual”能否在近同成本下更强压低 `exceed_alpha_0.50`。本地精确成本自检显示该 family 在当前 recipe 下 `selection_accesses=325696`、`deployment_accesses=163840`、`combined_accesses=489536`、`exact total_cost_ratio=0.1167144775390625`，满足预算；若想击败当前 incumbent `objective=0.2684179902076721`，需要把 `exceed_alpha_0.50` 压到 `0.1517035126686096` 以下。","needs_sanity":true,"next_hypotheses":["如果该点以相同约 `0.1167x` 成本明显优于 r292 的 plain residual tail，并接近或击败 incumbent，则下一轮留在该 family，只改 `STAGE1_RATIO`，校准 shared coarse 与 JumpReLU tail cleanup 的预算分工。","如果该点优于 r292 但仍未赢 incumbent，则记录为“thresholded tail cleanup 比 plain residual 更适合当前 scaffold”，下一轮优先只改 `FACTORIZED_HIDDEN_DIM` 向左探一格，尝试回收部分 cost 而保持 exceed 收益。","如果该点与 r292 持平或更差，则可更明确地下调 shared+PK 后接 tiny residual tail 这一整条邻域的优先级，后续应转向别的 scaffold-level cleanup 升级，而不是继续在 residual 语义上细扫。"],"notes_to_memory":["已实际新增并注册 `shared_product_key_expert_jumprelu_residual`，真实落盘文件仅有 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`。","该 family 与 `shared_product_key_expert_residual` 的精确差异不是新增库或改 K，而是把最后一段 tiny factorized residual cleanup 的系数形成方式从 plain ReLU top-k 改成 JumpReLU-gated residual selection；shared coarse 与 PK expert cleanup 语义保持不变。","本地 exact-string smoke 已通过：`SparseCoderConfig(architecture='shared_product_key_expert_jumprelu_residual', k=32, expansion_factor=1, num_experts=2048, active_experts=2, latents_per_expert=56, stage1_ratio=0.25, factorized_hidden_dim=256)` 在 CUDA 上可成功 instantiate、forward、backward。","本地 override allowlist 校验已通过：对 `ARCHITECTURE/K/EXPANSION_FACTOR/NUM_EXPERTS/ACTIVE_EXPERTS/LATENTS_PER_EXPERT/STAGE1_RATIO/FACTORIZED_HIDDEN_DIM/OPTIMIZER/LR` 的完整 recipe，`validate_env_overrides(...) == []`。","本地成本自检已通过：`selection_accesses=325696`、`deployment_accesses=163840`、`combined_accesses=489536`、`exact total_cost_ratio=0.1167144775390625`，低于 1.5×h×n 硬约束。","当前 recipe 下 residual hidden dim 实际被 `min(latents_per_expert, FACTORIZED_HIDDEN_DIM)` 裁到 `56`，因此该新 family 的精确成本与现有 `shared_product_key_expert_residual` 同档，适合做 matched cleanup-semantics probe。"],"reference_round":282,"self_review":"这轮满足代码类变更约束：我先真实落盘了新 family，再返回 `edit_sae_code`。修改范围保持最小，只做了 architecture whitelist、factory dispatch、具体类实现和 override allowlist，没有新增 tunable env key，也没有改 launcher / controller 语义。主假设也保持单一：只回答“thresholded tail cleanup 语义是否比 plain residual 更能压 low-K exceed”。主要风险是 JumpReLU 可能过度抑制 tiny residual bank，导致 tail 容量反而下降；如果结果如此，就应停止继续在这一条 residual-tail 语义上细扫。","summary":"已实现并注册 `shared_product_key_expert_jumprelu_residual`。建议本轮以 r282 为 reference，只改 `ARCHITECTURE`，做一轮 shared+PK scaffold 上的 matched cleanup-semantics probe，直接判断 JumpReLU tail residual 能否在约 `0.1167x` 成本下把 `exceed_alpha_0.50` 明显压低。","touched_files":["sparsify/config.py","sparsify/sparse_coder.py","research/AutoResearch/override_registry.py"]}