{"command":"run","hypothesis":"A soft residual codebook may recover the stalled mid-K lane better than hard residual VQ: `lowrank_soft_codebook_residual` keeps the strong low-rank trunk, then uses a learned soft codebook projection to absorb coarse residual structure before sparse correction, avoiding the brittle discrete assignment that hurt the hard-VQ branch.","summary":"Added a new prototype family, `lowrank_soft_codebook_residual`, in `sparsify/` and registered it in config/factory dispatch. The architecture applies the existing low-rank trunk first, then projects the residual through a soft codebook mixture before the usual sparse residual correction. Validation passed on the concrete dispatched architecture: exact smoke test succeeded for `architecture=lowrank_soft_codebook_residual, K=24, EF=12`, and a gradient check showed all parameters receive gradients, so this round stays architecture-only without repeating the prior DDP integration issue.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"lower_fvu","family_name":"lowrank_soft_codebook_residual","family_stage":"prototype","self_review":"This satisfies the severe-stagnation policy by introducing a genuinely different residual mechanism rather than another K move or scalar tweak. The experiment remains pinned to `K=24, EF=12, signum, LR=3.2e-3, AUXK_ALPHA=0.0`, so attribution is clean. Main risk: the hard discrete residual branch already lost, so the win depends on soft code mixing materially improving optimization instead of just rephrasing the same codebook idea.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_soft_codebook_residual"},{"key":"K","value":"24"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["Added new prototype architecture `lowrank_soft_codebook_residual` that combines the existing low-rank trunk with a soft codebook residual projection before sparse correction.","Integration chain completed for `lowrank_soft_codebook_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation.","Exact smoke test passed for `lowrank_soft_codebook_residual` at `K=24, EF=12` on the concrete dispatched architecture class.","Gradient check confirmed all parameters in `lowrank_soft_codebook_residual` received gradients, reducing risk of DDP unused-parameter crashes."],"next_hypotheses":["If `lowrank_soft_codebook_residual` is competitive but slightly worse at `K=24`, test `K=16` to see whether soft coarse residual coding helps more under tighter sparsity.","If `lowrank_soft_codebook_residual` wins at `K=24`, follow with `K=32` to check whether the gain persists across the mid-K band.","If it clearly loses at `K=24`, retire the codebook-projection branch and move to another mechanism family rather than tuning code count or router softness."],"primary_variable":"architecture"}

--- STDERR ---
OpenAI Codex v0.116.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: none
session id: 019d17e8-09af-7ab3-8981-5a034238157c
--------
user
Continue the SAE research session. Round 62.
Return one JSON object matching the action schema. No markdown fences.

Policy guidance:
SEVERE STAGNATION: 26 rounds without improvement.
1. If only K=128 tested, MUST try K=64 or K=32
2. Otherwise try fundamentally different architecture
3. Stop minor tweaks — they haven't worked

K coverage: [1, 2, 4, 8, 16, 24, 32].

Structured update:
{
  "round": 62,
  "frontier": [
    "k=32 ef=12 fvu=0.08931536227464676 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @a6b9904",
    "k=24 ef=12 fvu=0.09700598567724228 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @c9ce6fc",
    "k=16 ef=12 fvu=0.10837031900882721 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @b071b4c",
    "k=8 ef=12 fvu=0.1280982345342636 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @b840cf0",
    "k=4 ef=12 fvu=0.1484663188457489 arch=lowrank_residual lr=1.6e-3 opt=signum @7752961",
    "k=2 ef=12 fvu=0.16997215151786804 arch=lowrank_residual lr=1.6e-3 opt=signum @6c46e80",
    "k=1 ef=12 fvu=0.17282412946224213 arch=lowrank_two_stage_residual lr=1.6e-3 opt=signum @2d897d7"
  ],
  "latest_results": [
    "r59_1774223476 lowrank_residual_vq k24 discard fvu=0.10183659940958023 | Added a new prototype family, `lowrank_residual_vq`, in `spa",
    "r60_1774224160 lowrank_multi_branch_residual k24 discard fvu=0.10131727159023285 | Repaired `lowrank_multi_branch_residual` so the original rou",
    "r61_1774224459 lowrank_factorized_residual k24 discard fvu=0.12982133030891418 | Added a new prototype family, `lowrank_factorized_residual`,"
  ],
  "latest_round_summary": [
    "r61 lowrank_factorized_residual edit_sae_code -> discard fvu=0.12982133030891418 299s | A factorized residual scorer may recover the stalled mid-K lane better than d..."
  ],
  "agent_state": {
    "consecutive_crashes": 0,
    "consecutive_no_improve": 26,
    "rounds_since_new_family": 62
  },
  "pending_hints": [],
  "next_hypotheses": [
    "If `lowrank_factorized_residual` is competitive but slightly worse at `K=24`, test `K=16` to see whether bottleneck residual mixing helps more under tighter sparsity.",
    "If `lowrank_factorized_residual` wins at `K=24`, follow with `K=32` to check whether the gain persists across the mid-K band.",
    "If it clearly loses at `K=24`, retire the factorized-residual branch and move to another mechanism family rather than tuning hidden width or bottleneck size."
  ]
}
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
mcp startup: no servers
codex
The factorized residual branch failed badly enough that it should be retired, not tuned. I’m switching to one more genuinely different family: a low-rank trunk with a learned residual codebook basis and sample-specific code coefficients before the sparse correction, which is distinct from the hard-VQ and multi-branch losses we already ruled out.
exec
/bin/bash -lc "sed -n '1518,1716p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:


class LowRankResidualVQSparseCoder(LowRankResidualSparseCoder):
    """Low-rank trunk with hard codebook residual modeling before sparse correction."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
        self.num_codes = min(512, max(64, cfg.k * 4))
        self.codebook = nn.Parameter(
            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
        )
        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
exec
/bin/bash -lc "sed -n '530,734p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
        return EncoderOutput(top_acts, top_indices, acts)


class CodebookTopKSparseCoder(SparseCoder):
    """Reconstruct a coarse codebook vector, then sparsify the residual."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        self.num_latents = cfg.num_latents or d_in * cfg.expansion_factor
exec
/bin/bash -lc 'rg -n "lowrank_factorized_residual|lowrank_residual_vq|lowrank_multi_branch_residual|codebook" sparsify/config.py sparsify/sparse_coder.py' in /root/sparsify-ascend succeeded in 0ms:
sparsify/sparse_coder.py:300:    if architecture == "codebook_topk":
sparsify/sparse_coder.py:306:    if architecture == "lowrank_residual_vq":
sparsify/sparse_coder.py:334:    if architecture == "lowrank_multi_branch_residual":
sparsify/sparse_coder.py:336:    if architecture == "lowrank_factorized_residual":
sparsify/sparse_coder.py:535:    """Reconstruct a coarse codebook vector, then sparsify the residual."""
sparsify/sparse_coder.py:552:        self.codebook = nn.Parameter(
sparsify/sparse_coder.py:580:        coarse = routing @ self.codebook
sparsify/sparse_coder.py:638:    """Hard codebook reconstruction with sparse residual correction."""
sparsify/sparse_coder.py:655:        self.codebook = nn.Parameter(
sparsify/sparse_coder.py:681:        coarse = routing @ self.codebook
sparsify/sparse_coder.py:739:    """Two hard codebook reconstructions followed by sparse residual correction."""
sparsify/sparse_coder.py:751:        self.second_codebook = nn.Parameter(
sparsify/sparse_coder.py:760:        self, x: Tensor, router: nn.Linear, codebook: Tensor
sparsify/sparse_coder.py:769:        coarse = routing @ codebook
sparsify/sparse_coder.py:774:            x, self.code_router, self.codebook
sparsify/sparse_coder.py:778:            second_input, self.second_code_router, self.second_codebook
sparsify/sparse_coder.py:1521:    """Low-rank trunk with hard codebook residual modeling before sparse correction."""
sparsify/sparse_coder.py:1534:        self.codebook = nn.Parameter(
sparsify/sparse_coder.py:1548:        coarse = routing @ self.codebook
sparsify/config.py:198:            "codebook_topk",
codex
I’ve got the implementation path. This branch will use a soft residual codebook projection instead of hard code selection, so the codebook becomes a learned coarse residual basis rather than a discrete router. That keeps the hypothesis architecture-only while staying materially different from `lowrank_residual_vq`.
file update
M /root/sparsify-ascend/sparsify/sparse_coder.py
@@ -337,2 +337,4 @@
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
@@ -1821,2 +1823,89 @@
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
M /root/sparsify-ascend/sparsify/config.py
@@ -215,2 +215,3 @@
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
apply_patch(auto_approved=true) exited 0 in 7ms:
Success. Updated the following files:
M /root/sparsify-ascend/sparsify/config.py
M /root/sparsify-ascend/sparsify/sparse_coder.py
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index f4b3c6400656f74fffb7497df9a45ac9484599eb..d55878773d0eb41e716062fbb1437a6d7c7fc585
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -213,6 +213,7 @@
             "lowrank_jumprelu_residual",
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index b4a5ed4604e5e65d2185b951e48da6157e0cdfba..295fd21597795590bea0e6fb5c2cff1a0aee0f88
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -335,6 +335,8 @@
         return LowRankMultiBranchResidualSparseCoder
     if architecture == "lowrank_factorized_residual":
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1819,6 +1821,93 @@
         )
 
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index f4b3c6400656f74fffb7497df9a45ac9484599eb..d55878773d0eb41e716062fbb1437a6d7c7fc585
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -213,6 +213,7 @@
             "lowrank_jumprelu_residual",
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index b4a5ed4604e5e65d2185b951e48da6157e0cdfba..295fd21597795590bea0e6fb5c2cff1a0aee0f88
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -335,6 +335,8 @@
         return LowRankMultiBranchResidualSparseCoder
     if architecture == "lowrank_factorized_residual":
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1819,6 +1821,93 @@
         )
 
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
The new family is registered. I’m running the concrete dispatch checks now: exact `(architecture, K, EF)` smoke on the real class plus a gradient pass to make sure no parameters are stranded before handing off the round.
exec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class

arch='lowrank_soft_codebook_residual'
cls = _get_sae_class(arch)
cfg = SparseCoderConfig(architecture=arch, k=24, expansion_factor=12)
sae = cls(128, cfg, device='cpu')
for i, (name, p) in enumerate(sae.named_parameters()):
    print(i, name, tuple(p.shape))
PY" in /root/sparsify-ascendexec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class

arch='lowrank_soft_codebook_residual'
cls = _get_sae_class(arch)
cfg = SparseCoderConfig(architecture=arch, k=24, expansion_factor=12)
sae = cls(128, cfg, device='cpu', dtype=torch.float32)
x = torch.randn(4, 128)
out = sae(x)
out.fvu.backward()
for i,(name,p) in enumerate(sae.named_parameters()):
    print(i, name, p.grad is None, 0.0 if p.grad is None else float(p.grad.abs().sum()))
PY" in /root/sparsify-ascendexec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class

device = 'cuda' if torch.cuda.is_available() else 'cpu'
try:
    import torch_npu
    if torch.npu.is_available():
        device = 'npu'
except ImportError:
    pass

arch = 'lowrank_soft_codebook_residual'
k = 24
ef = 12
cfg = SparseCoderConfig(architecture=arch, k=k, expansion_factor=ef)
cls = _get_sae_class(arch)
sae = cls(1024, cfg, device=device, dtype=torch.float32)
x = torch.randn(4, 1024, device=device)
out = sae(x)
out.fvu.backward()
print('integration_smoke: OK', arch, k, ef, device)
PY" in /root/sparsify-ascendfile update:
diff --git a/sparsify/config.py b/sparsify/config.py
index f4b3c6400656f74fffb7497df9a45ac9484599eb..d55878773d0eb41e716062fbb1437a6d7c7fc585
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -213,6 +213,7 @@
             "lowrank_jumprelu_residual",
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index b4a5ed4604e5e65d2185b951e48da6157e0cdfba..295fd21597795590bea0e6fb5c2cff1a0aee0f88
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -335,6 +335,8 @@
         return LowRankMultiBranchResidualSparseCoder
     if architecture == "lowrank_factorized_residual":
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1819,6 +1821,93 @@
         )
 
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

 succeeded in 4.12s:
Using fused decoder on CUDA.
0 W_dec (1536, 128)
1 b_dec (128,)
2 codebook (48, 128)
3 trunk_encoder.weight (48, 128)
4 trunk_decoder.weight (128, 48)
5 encoder.weight (1536, 128)
6 encoder.bias (1536,)
7 code_router.weight (48, 128)
8 code_router.bias (48,)
 succeeded in 4.18s:
Using fused decoder on CUDA.
0 W_dec False 59.013511657714844
1 b_dec False 0.9779850244522095
2 codebook False 0.9612920880317688
3 trunk_encoder.weight False 37.83129119873047
4 trunk_decoder.weight False 21.06627655029297
5 encoder.weight False 40.47006607055664
6 encoder.bias False 0.3767796754837036
7 code_router.weight False 0.20253042876720428
8 code_router.bias False 0.0017004545079544187
 succeeded in 4.70s:
Using fused decoder on CUDA.
integration_smoke: OK lowrank_soft_codebook_residual 24 12 cuda
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index f4b3c6400656f74fffb7497df9a45ac9484599eb..d55878773d0eb41e716062fbb1437a6d7c7fc585
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -213,6 +213,7 @@
             "lowrank_jumprelu_residual",
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index b4a5ed4604e5e65d2185b951e48da6157e0cdfba..295fd21597795590bea0e6fb5c2cff1a0aee0f88
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -335,6 +335,8 @@
         return LowRankMultiBranchResidualSparseCoder
     if architecture == "lowrank_factorized_residual":
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1819,6 +1821,93 @@
         )
 
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
{"command":"run","hypothesis":"A soft residual codebook may recover the stalled mid-K lane better than hard residual VQ: `lowrank_soft_codebook_residual` keeps the strong low-rank trunk, then uses a learned soft codebook projection to absorb coarse residual structure before sparse correction, avoiding the brittle discrete assignment that hurt the hard-VQ branch.","summary":"Added a new prototype family, `lowrank_soft_codebook_residual`, in `sparsify/` and registered it in config/factory dispatch. The architecture applies the existing low-rank trunk first, then projects the residual through a soft codebook mixture before the usual sparse residual correction. Validation passed on the concrete dispatched architecture: exact smoke test succeeded for `architecture=lowrank_soft_codebook_residual, K=24, EF=12`, and a gradient check showed all parameters receive gradients, so this round stays architecture-only without repeating the prior DDP integration issue.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"lower_fvu","family_name":"lowrank_soft_codebook_residual","family_stage":"prototype","self_review":"This satisfies the severe-stagnation policy by introducing a genuinely different residual mechanism rather than another K move or scalar tweak. The experiment remains pinned to `K=24, EF=12, signum, LR=3.2e-3, AUXK_ALPHA=0.0`, so attribution is clean. Main risk: the hard discrete residual branch already lost, so the win depends on soft code mixing materially improving optimization instead of just rephrasing the same codebook idea.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_soft_codebook_residual"},{"key":"K","value":"24"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["Added new prototype architecture `lowrank_soft_codebook_residual` that combines the existing low-rank trunk with a soft codebook residual projection before sparse correction.","Integration chain completed for `lowrank_soft_codebook_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation.","Exact smoke test passed for `lowrank_soft_codebook_residual` at `K=24, EF=12` on the concrete dispatched architecture class.","Gradient check confirmed all parameters in `lowrank_soft_codebook_residual` received gradients, reducing risk of DDP unused-parameter crashes."],"next_hypotheses":["If `lowrank_soft_codebook_residual` is competitive but slightly worse at `K=24`, test `K=16` to see whether soft coarse residual coding helps more under tighter sparsity.","If `lowrank_soft_codebook_residual` wins at `K=24`, follow with `K=32` to check whether the gain persists across the mid-K band.","If it clearly loses at `K=24`, retire the codebook-projection branch and move to another mechanism family rather than tuning code count or router softness."],"primary_variable":"architecture"}
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index f4b3c6400656f74fffb7497df9a45ac9484599eb..d55878773d0eb41e716062fbb1437a6d7c7fc585
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -213,6 +213,7 @@
             "lowrank_jumprelu_residual",
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
+            "lowrank_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index b4a5ed4604e5e65d2185b951e48da6157e0cdfba..295fd21597795590bea0e6fb5c2cff1a0aee0f88
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -335,6 +335,8 @@
         return LowRankMultiBranchResidualSparseCoder
     if architecture == "lowrank_factorized_residual":
         return LowRankFactorizedResidualSparseCoder
+    if architecture == "lowrank_soft_codebook_residual":
+        return LowRankSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1819,6 +1821,93 @@
         )
 
 
+class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with soft codebook residual projection before sparse correction."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        return fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+        top_acts, top_indices, pre_acts = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.cfg.k
+        )
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + coarse + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], pre_acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = (
+                trunk
+                + coarse
+                + self.decode_residual(auxk_acts, auxk_indices)
+                + self.b_dec
+            )
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

tokens used
207,919
