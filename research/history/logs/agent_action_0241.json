{"change_type":"param_only","command":"run","env_overrides":[{"key":"K","value":"64"}],"expected_win":"better_fvu_within_low_cost_region","experiment_tier":"full","family_name":"topk","family_stage":"frontier_mapping","hypothesis":"候选 family: topk。最终重构公式为 x_hat = Σ_i α_i b_i，其中 {b_i} 来自单个静态向量库；在线侧只做单样本 top-k 选择与系数计算，部署侧仍是单库查表 + 加权求和 + 选择性补偿，因此可直接导出到 LUTurbo/Lottable 推理链路，兼容性判断为直接兼容。相对 reference_round=r240 只改一个 env 参数 `K=64`，保持 `ARCHITECTURE=topk`、`EXPANSION_FACTOR=1`、`OPTIMIZER=signum`、`LR=0.003`、`AUXK_ALPHA=0.03125` 及其余配置不变，测试左端低成本前沿从 `K=32 -> 48 -> 56` 的连续改善是否会平滑延续到已有锚点 `K=64`，并在约 0.31x 总成本处对这段 Pareto 斜率做一次干净确认。选择这一步而不是继续补更细中间点或切回已给出负信号的新 family，是因为 r240 已满足当前开放假设的前半条件：`K56` 明显优于 `K48`，且总时长继续下降；当前最高信息量的问题是这条左侧 topk 前沿能否无缝回接到既有 `K64` 质量锚点。","needs_sanity":false,"next_hypotheses":["如果 `topk@EF1,K64` 复现或优于现有 0.31x 点的 FVU=0.2583，且 `tokens_per_sec`、`duration_sec`、`final_total_tokens` 没有明显恶化，则可停止对 `K32-64` 区间的细密映射，把注意力转回 `0.31x-0.38x` 之间更稀疏的缺口。","如果 `K64` 明显劣于现有锚点或运行代价异常，则说明左端序列存在较强噪声或阶段性不可比因素，后续应优先做 clean repeat 而不是继续沿 K 轴外推。","如果质量改善存在但运行显著更慢，则按运行时优先级将其记为负面系统信号，不把该配方升格为左侧前沿确认点。"],"notes_to_memory":["r238 和 r239 已证明 plain `topk` 主导 `<0.31x` 左端低成本前沿；r240 进一步表明从 `K48` 到 `K56` 的改进不仅存在，而且运行代价没有恶化。","当前更高信息量的问题不是再开新 family，而是确认这条左侧 topk 前沿是否能平滑回接到既有 `K64` 锚点，从而完成 `0.28x-0.31x` 区间的闭环。","评估本轮必须同时比较 `val_fvu`、`tokens_per_sec`、`duration_sec`、`final_total_tokens`；若只是轻微质量收益但运行明显更慢，应直接视为负面信号。"],"reference_round":240,"self_review":"该提案严格遵守单变量原则：相对 r240 只改 `K`，不联动 architecture、EF、LR、optimizer、AUXK_ALPHA 或任何新参数。主要风险是 `K64` 已有历史点，本轮可能只得到重复信息；但在左端低成本序列刚形成连续正信号的情况下，这个 clean confirm 的信息价值高于继续做更细碎的局部插值或重开已证伪 family。","summary":"相对 r240 的 `topk@EF1,K56` 做一次严格 param_only 左端确认，仅设置 `K=64`，验证左侧低成本 topk 前沿是否能平滑回接到已有 0.31x 锚点。","touched_files":[]}