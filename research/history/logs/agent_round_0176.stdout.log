{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"expert_topk"}],"expected_win":"explore_unknown","experiment_tier":"full","family_name":"expert_topk","family_stage":"probe","hypothesis":"候选 family 设为 `expert_topk`。最终重构公式为 `x_hat = Σ_{e∈E(x)} Σ_{i∈S_e(x)} α_{e,i}(x) b_{e,i}`：其中 `{b_{e,i}}` 是各个 expert 的静态子库原子，在线侧只做轻量 router 选择少量 active experts，并在对应静态子库内做 local top-k 与有限加权和，因此可直接导出到 LUTurbo/Lottable 的“静态 expert 子库选择 + 子库查表 + 加权求和”推理链路，兼容性判断为 `直接兼容`。`batch_topk`、`adaptive_budget_topk` 这类 batch/sample-coupled family 仍是 `不兼容`，只能作为历史参考，不能驱动当前 frontier。当前 `<0.25x` 主战场里，`shared_routed_expert_topk` 的 `K≈108-112` 与 `LATENTS_PER_EXPERT≈160` 局部 knee 已基本摸清，最近两轮 `factorized_topk` 与 `lowrank_expert_topk` 也没有形成新的 low-cost frontier 扩展；因此下一步更高信息量的问题不是继续补同 family 插值，而是做一个 matched-cost architecture swap，直接回答“shared 分支是否真是当前 low-cost 质量的必要组成，还是更短的 routed-only active path 反而更值”。相对 r165 只把 `ARCHITECTURE` 从 `shared_routed_expert_topk` 切到 `expert_topk`，保持 `K=108`、`EXPANSION_FACTOR=1`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=160`、`NUM_EXPERTS=64`、`adam`、`LR=8e-4` 与其余训练配方不变，直接比较“matched low-cost anchor 下，去掉 always-on shared branch 后的 routed-only MoE-like 结构”是否能在更低总成本下逼近或击败现有 low-cost anchors。本地成本自检显示该候选在当前 1024x4096 proxy 下 selection_accesses=393216、deployment_accesses=442368、combined_accesses=835584，对应 combined_ratio 约 `0.20x`，明确落在当前 `<0.25x` 主战场内，也显著低于 1.5×h×n 硬约束。","needs_sanity":false,"next_hypotheses":["如果 `expert_topk@K108,ACTIVE_EXPERTS=2,LATENTS_PER_EXPERT=160` 在约 `0.20x` 下把 FVU 压到接近或优于当前 `0.20x-0.21x` anchors，则下一轮继续留在该 family，只改 `K=112`，测试 routed-only 低成本线是否仍主要受 lookup budget 限制。","如果该点仍明显弱于同带 `shared_routed_expert_topk` anchors，则说明当前 low-cost 成功点的关键增益确实来自 shared branch，而不是 routed-only active path；后续应停止再把 `expert_topk` 作为主线挑战者，转去别的兼容结构槽位。"],"notes_to_memory":["`expert_topk` 的 LUTurbo 导出形式是静态 expert 子库上的有限加权和，兼容性应保持为 `直接兼容`。","这轮是以当前 low-cost matched baseline r165 为 anchor 的 architecture-level probe，不是继续沿 `shared_routed_expert_topk` 或 `lowrank_expert_topk` 做 `K/LATENTS_PER_EXPERT` 线性插值。","本地成本自检：相对 r165 仅切到 `expert_topk` 后，selection_accesses=393216、deployment_accesses=442368、combined_accesses=835584，对当前 1024x4096 proxy 的 total_cost 约 `0.20x`，显著低于当前 `shared_routed_expert_topk K=108` 的约 `0.24x`。","相比之下，`factorized_expert_topk`、`shared_routed_factorized_expert_topk`、`shared_lowrank_routed_expert_topk` 在当前 target 上已经给过更强的低成本负信号，当前不应优先继续复验它们。"],"reference_round":165,"self_review":"这轮严格遵守 param_only 单变量约束：相对 r165 只改 `ARCHITECTURE`。选择 r165 而不是继续基于 r175 做同 family 的内部轴压缩，是因为当前策略已明确要求把 `shared_routed_expert_topk` 视为 matched baseline / cost anchor，而不是继续沿已有负边际的 family 内部轴推进。选择 `expert_topk` 而不是已多轮失利的 factorized/shared-lowrank 变体，是因为它同时满足三点：成本路径清晰落在 `<0.25x`、active path 更短、且在当前 exact recipe 下仍缺一个干净的 matched-cost architecture 对照。主要风险是 routed-only 去掉 shared branch 后质量会明显退化；但这正是当前最需要被干净回答的结构问题。","summary":"以 r165 为 reference，只改 `ARCHITECTURE=expert_topk`，做一轮新的 matched-cost low-cost architecture probe。目标是在约 `0.20x` 总成本下直接判断 routed-only、短 active path 的直接兼容 MoE-like 结构，是否能在当前主战场挑战现有 `shared_routed_expert_topk` low-cost anchors。","touched_files":[]}

--- STDERR ---
OpenAI Codex v0.117.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 019d3efc-9c83-72d0-b12a-8b9644429238
--------
user
继续 LUTurbo 研究会话。当前轮次：Round 176。

返回一个符合 action schema 的 JSON 对象，不要使用 markdown 代码块。

提醒：每轮只改一个 env 参数；不要声明 primary_variable；param_only 默认应显式给出 reference_round。只有当前 target 冷启动且暂无 reference_round 时，才允许显式返回 reference_round=null，相对 target_profile_baseline 只改 1 个 env 参数。默认只改 sparsify/；只有在新增 tunable 参数或修复参数接线时，才允许改 research/AutoResearch/ 与 scripts/autoresearch_test.sh 的必要文件。如果返回 `edit_sae_code` / `edit_perf_code`，JSON 必须对应已经实际落盘的代码修改；不要只写计划。如果新增 env key，先本地确认 allowlist/wiring 已接通，再返回该 key。Frontier 基于 (total_cost, FVU) 2D Pareto front，total_cost = encoder + deployment；最终返回 JSON。

LUTurbo 兼容性硬约束：
1. 提案前必须先写出最终重构公式，并说明它如何导出到 LUTurbo/Lottable 推理链路。
2. 每个 family 必须先判断兼容性：直接兼容 / 扩展兼容 / 不兼容。
3. 明确标记为不兼容的 family，不得继续占据 frontier，也不得继续作为主线推进。
4. 当前 frontier 只代表兼容 family 的最优点；不兼容 family 只能作为历史参考，不能驱动后续决策。
当前 target：训练 hookpoint 为 layers.[3].self_attn.q_proj。
成本 proxy：train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096。
5. 成本硬约束：total_cost (encoder + deployment) 不得超过 1.5×h×n（原始 matmul 的 1.5 倍）。
   超过此阈值的配置将被 policy 拦截。
   降低 encoder 成本：减小 EXPANSION_FACTOR / TRUNK_RANK / NUM_CODES、使用低秩 scorer、探索非全字典选择机制。
   降低部署成本：减小 K / TRUNK_RANK / NUM_CODES。
   可通过 TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM 等 env_overrides 调节。

策略引导：
Round 176 策略模式：低开销探索
原因：当前 <0.25x 区域最佳 FVU=0.1369，明显落后于当前最佳可行点 FVU=0.0424，需要定向补低开销前沿。
当前低成本参考 family：shared_routed_expert_topk
当前低成本参考配方：ARCHITECTURE=shared_routed_expert_topk K=112 EXPANSION_FACTOR=1 OPTIMIZER=adam LR=8e-4
当前低成本完整配置（source=low_cost_frontier_best）：
  ARCHITECTURE=shared_routed_expert_topk
  EXPANSION_FACTOR=1
  K=112
  HOOKPOINTS=layers.[3].self_attn.q_proj
  OPTIMIZER=adam
  LR=8e-4
  BATCH_SIZE=1
  GRAD_ACC_STEPS=8
  MICRO_ACC_STEPS=1
  AUXK_ALPHA=0.03125
  DEAD_FEATURE_THRESHOLD=10000000
  USE_HADAMARD=0
  COMPILE_MODEL=1
  ACTIVE_EXPERTS=2
  FACTORIZED_HIDDEN_DIM=256
  LATENTS_PER_EXPERT=160
  NUM_EXPERTS=64
  STAGE1_RATIO=0.25
  TRUNK_RANK=40
本轮要求：
1. 当前阶段重点是 `<0.25x total_cost` 主战场；`0.25x-0.35x` 只作辅助对照，`>0.4x` 不应继续主导预算。
2. 当前低成本 baseline 主要是 `shared_routed_expert_topk` 一线；它现在更适合作为 matched baseline / cost anchor，而不是默认继续细扫的主线。
3. 允许新开 family，但只有在其成本路径明确落在 `<0.25x` 或至少不明显超过 `0.35x` 时才值得优先尝试。
4. MoE-like 方向只有在 router 足够轻、expert 更小、总激活路径仍短、且最终仍能导出为静态子库有限加权和时才值得尝试。
5. 不要让 `0.5x` 左右的成功点重新把注意力拉回中成本区；它们只保留作少量质量锚点。
6. 如果最近几轮都只是同一 family 上的 `K` 或 `LATENTS_PER_EXPERT` 微调且没有形成新的 frontier 扩展，应优先切到 architecture-level probe，而不是继续补线性插值点。

成本硬约束：total_cost (encoder + deployment) 不得超过 1.5×h×n，超过将被拦截。

兼容性注册表：直接兼容 11 个，扩展兼容 24 个，不兼容 3 个。

Agent 状态：round=175, consecutive_crashes=0, consecutive_no_improve=4, rounds_since_new_family=21

当前 target profile：
  training_hookpoint=layers.[3].self_attn.q_proj
  cost_proxy=train on q_proj activations; cost proxy uses fused QKV deployment matrix 1024x4096
  original_matmul=1024x4096 (4194304 accesses)

当前研究阶段：`<0.25x` 低成本区优先。
  当前要回答的是“谁能在 `<0.25x` 区域打败现有 low-cost anchors”，而不是“哪个 0.5x 左右结构最好”。
  `0.25x-0.35x` 只作辅助对照带；`>0.4x` 只保留少量质量锚点，不应继续主导预算。

当前 low-cost 参考线：
  1. `shared_routed_expert_topk` 是当前 `<0.25x` 区域最强兼容 baseline；它现在应作为 matched baseline / cost anchor，而不是默认继续细扫的主线。
  2. `expert_topk` 只作为更极左侧成本 anchor；若质量明显落后，不应继续主导预算。

当前高优先级方向：
  1. 新的 matched-cost architecture probe，而不是继续沿同一 family 做线性插值。
  2. `shared bottleneck + expert-specific head`、expert 内部 low-rank、以及更轻的 scorer / router。
  3. hetero/shared experts 与 active path 更短的 sparse MoE-like 结构。
  4. very-light residual / factorized 变体。
     仅当它们能保持在 low-cost 带内时才值得尝试；不要默认引入会自然抬高到中成本区的 trunk / residual 负担。

当前应下调优先级的方向：
  1. 天然落在 `0.45x-0.55x` 左右的 `lowrank_expert_residual` 式结构，除非只作少量质量对照。
  2. 已经多轮失败的 shared-lowrank two-stage / hetero residual 变体。
  3. 连续在同一 family 上补 `K` / `LATENTS_PER_EXPERT` 插值点却没有形成新的 frontier 扩展的局部细扫。
  4. 任何只是把成本抬到 `>0.4x` 却不能直接回答 `<0.25x` 问题的结构探针。

训练代理 frontier（2D Pareto: total_cost vs FVU）：
  total=0.13x (sel=0.06x + deploy=0.06x)  fvu=0.36355817317962646  arch=expert_topk  K=64 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.19x (sel=0.06x + deploy=0.12x)  fvu=0.3148818016052246  arch=expert_topk  K=128 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.2x (sel=0.13x + deploy=0.06x)  fvu=0.15513789653778076  arch=shared_routed_expert_topk  K=64 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.21x (sel=0.13x + deploy=0.08x)  fvu=0.14842143654823303  arch=shared_routed_expert_topk  K=80 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.22x (sel=0.16x + deploy=0.06x)  fvu=0.14638903737068176  arch=shared_routed_expert_topk  K=64 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.23x (sel=0.13x + deploy=0.09x)  fvu=0.14208984375  arch=shared_routed_expert_topk  K=96 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.23x (sel=0.13x + deploy=0.1x)  fvu=0.13922066986560822  arch=shared_routed_expert_topk  K=104 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  total=0.24x (sel=0.13x + deploy=0.11x)  fvu=0.13799256086349487  arch=shared_routed_expert_topk  K=108 EF=1  lr=8e-4 opt=adam [FEASIBLE]
  参考：`0.25x-0.35x` 辅助带当前有 5 个 frontier 点。

成本状态：
  当前最佳 <0.25x 点 → shared_routed_expert_topk (K=112, EF=1) [FEASIBLE]:
    总成本: 0.24x | 预算比率: 0.16x | encoder: 0.13x | 部署: 0.11x | FVU: 0.13694503903388977
  当前最佳 0.25x-0.5x 点 → lowrank_expert_residual (K=96, EF=1) [FEASIBLE]:
    总成本: 0.49x | 预算比率: 0.33x | encoder: 0.38x | 部署: 0.11x | FVU: 0.09837484359741211
  最优可行点 → lowrank_expert_residual (K=192, EF=1) [FEASIBLE]:
    总成本: 1.06x | 预算比率: 0.71x | encoder: 0.5x | 部署: 0.56x | FVU: 0.04238538444042206
  降低 encoder 成本: 降 EF / TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM
  降低部署成本: 降 K / TRUNK_RANK / NUM_CODES

当前成本 profile：hookpoint=layers.[3].self_attn.q_proj | proxy=1024x4096
Encoder 侧成本速查表（非最终 budget，ratio 指 encoder-only）：
  架构                                        EF=2    EF=3    EF=4    EF=6    EF=8    EF=12 
  topk                                      0.5x+    0.75x+   1.0x+    1.5x+    2.0x-    3.0x-  
  gated                                     1.0x+    1.5x+    2.0x-    3.0x-    4.0x-    6.0x-  
  factorized_topk                           0.38x+   0.5x+    0.62x+   0.88x+   1.12x+   1.62x- 
  lowrank_residual                          0.62x+   0.88x+   1.12x+   1.62x-   2.12x-   3.12x- 
  lowrank_soft_codebook_residual            0.66x+   0.91x+   1.16x+   1.66x-   2.16x-   3.16x- 
  lowrank_two_stage_soft_codebook_residual  1.16x+   1.66x-   2.16x-   3.16x-   4.16x-   6.16x- 
  注意：以上仅为 encoder 选择成本（不依赖 K）。最终 budget 以 total_cost = selection + deployment 为准。
  部署查表成本 K×n 额外贡献约：K=32 +3%，K=64 +6%，K=128 +12%。

最近几轮（兼容架构，2）：
  r173 factorized_topk param_only -> discard fvu=0.2532755434513092 466s | 候选 family 设为 `factorized_topk`。最终重构公式为 `x_hat = Σ_i α_i(x) b_i`：其中 `{b_i}` 是单...
  r175 lowrank_expert_topk param_only -> discard fvu=0.14512735605239868 1549s | 候选 family 保持为 `lowrank_expert_topk`。最终重构公式为 `x_hat = Σ_r β_r(x) u_r + Σ_{e∈E(...

最近可用 reference_round 的完整配置：
  r165 shared_routed_expert_topk (decision=keep, source=resolved_candidate_env_config)
  ARCHITECTURE=shared_routed_expert_topk
  EXPANSION_FACTOR=1
  K=108
  HOOKPOINTS=layers.[3].self_attn.q_proj
  OPTIMIZER=adam
  LR=8e-4
  BATCH_SIZE=1
  GRAD_ACC_STEPS=8
  MICRO_ACC_STEPS=1
  AUXK_ALPHA=0.03125
  DEAD_FEATURE_THRESHOLD=10000000
  USE_HADAMARD=0
  COMPILE_MODEL=1
  ACTIVE_EXPERTS=2
  FACTORIZED_HIDDEN_DIM=256
  LATENTS_PER_EXPERT=160
  NUM_EXPERTS=64
  STAGE1_RATIO=0.25
  TRUNK_RANK=40

  r175 lowrank_expert_topk (decision=discard, source=resolved_candidate_env_config)
  ARCHITECTURE=lowrank_expert_topk
  EXPANSION_FACTOR=1
  K=96
  HOOKPOINTS=layers.[3].self_attn.q_proj
  OPTIMIZER=adam
  LR=8e-4
  BATCH_SIZE=1
  GRAD_ACC_STEPS=8
  MICRO_ACC_STEPS=1
  AUXK_ALPHA=0.03125
  DEAD_FEATURE_THRESHOLD=10000000
  USE_HADAMARD=0
  COMPILE_MODEL=1
  ACTIVE_EXPERTS=2
  FACTORIZED_HIDDEN_DIM=256
  LATENTS_PER_EXPERT=224
  NUM_EXPERTS=64
  STAGE1_RATIO=0.25
  TRUNK_RANK=8

  r173 factorized_topk (decision=discard, source=resolved_candidate_env_config)
  ARCHITECTURE=factorized_topk
  EXPANSION_FACTOR=1
  K=96
  HOOKPOINTS=layers.[3].self_attn.q_proj
  OPTIMIZER=adam
  LR=8e-4
  BATCH_SIZE=1
  GRAD_ACC_STEPS=8
  MICRO_ACC_STEPS=1
  AUXK_ALPHA=0.03125
  DEAD_FEATURE_THRESHOLD=10000000
  USE_HADAMARD=0
  COMPILE_MODEL=1
  ACTIVE_EXPERTS=2
  FACTORIZED_HIDDEN_DIM=256
  LATENTS_PER_EXPERT=160
  NUM_EXPERTS=64
  STAGE1_RATIO=0.25
  TRUNK_RANK=16

  r158 lowrank_expert_residual (decision=discard, source=resolved_candidate_env_config)
  ARCHITECTURE=lowrank_expert_residual
  EXPANSION_FACTOR=1
  K=96
  HOOKPOINTS=layers.[3].self_attn.q_proj
  OPTIMIZER=adam
  LR=8e-4
  BATCH_SIZE=1
  GRAD_ACC_STEPS=8
  MICRO_ACC_STEPS=1
  AUXK_ALPHA=0.03125
  DEAD_FEATURE_THRESHOLD=10000000
  USE_HADAMARD=0
  COMPILE_MODEL=1
  ACTIVE_EXPERTS=1
  FACTORIZED_HIDDEN_DIM=256
  LATENTS_PER_EXPERT=224
  NUM_EXPERTS=64
  STAGE1_RATIO=0.25
  TRUNK_RANK=16

操作提示：
  1. 当前主战场仍是 `total_cost < 0.25x`。`0.25x-0.35x` 只作辅助对照，`>0.4x` 只保留少量质量锚点；但接下来应优先寻找新的 low-cost 结构突破，而不是继续在同一 family 上做线性参数插值。
  2. 当前 `<0.25x` 最强低成本线 `shared_routed_expert_topk` 现在应视为 matched baseline / cost anchor，而不是默认继续细扫的主线。`K≈108-112`、`LATENTS_PER_EXPERT≈160` 这条局部 knee 已基本摸清，后续更重要的问题是是否存在新的 low-cost 结构能在相近成本下明显打败它。
  3. 在 `<0.25x` 区域，优先尝试有结构信息增量的 low-cost probe：例如 `shared bottleneck + expert-specific head`、expert 内部 low-rank、更轻 scorer / router、hetero/shared experts、active path 更短的 sparse MoE-like 变体。若最近 2-3 轮都只是同一 family 上的 `K` 或 `LATENTS_PER_EXPERT` 微调且没有形成新的 frontier 扩展，则下一轮应优先换结构槽位，而不是继续补插值点。

当前背景文档（弱先验，不是结论）：
# Prior Research History: Target-Restart Brief

## 1. 文档用途

这份文档的作用不是给出“当前最优主线”，而是给 Agent 一份更低偏置的背景说明。

应当这样使用它：

- 把它当成兼容性约束、工程不变量、和未决问题清单
- 不要把旧位置、旧轮次、旧 frontier 的 family 排名当成当前 target 的默认真理
- 不要把这里出现的候选方向理解成固定 opening order
- 当前 target 上的结论，必须依赖当前 target 自己的新结果

当前固定事实：

- 训练 hookpoint 固定为 `layers.[3].self_attn.q_proj`
- 成本 proxy 固定按 fused QKV 部署矩阵 `1024 x 4096` 统计
- 训练代理目标是维护 `(total_cost, FVU)` 的 2D Pareto frontier

## 1.1 当前 target 的弱先验更新

下面这些只算当前 target 的弱先验，不是结论：

- 当前主战场应放在 `<0.25x total_cost` 区域；`0.25x-0.35x` 只作辅助对照，`>0.4x` 只保留少量质量锚点
- 当前 `<0.25x` 区域最强兼容前沿主要由 `shared_routed_expert_topk` 构成；它现在更适合作为 matched low-cost baseline / cost anchor，而不是默认继续细扫的主线
- 目前这条 baseline 上的 `K≈108-112` 与 `LATENTS_PER_EXPERT≈160` 已经提供了足够局部证据；继续在线性 `K/LATENTS` 轴上补点的信息增量很小
- `expert_topk` 仍有极低成本 anchor 价值，但在 `<0.25x` 的质量明显弱于当前 shared+routed 主线
- `lowrank_expert_residual` 在 `0.49x-0.54x` 一带给出了当前更强的质量锚点，但这不意味着后续应让中成本结构继续主导搜索预算

一句话：当前 target 上，优先问题不是“哪个中成本结构最好”，也不是“`K=110` 和 `K=111` 谁更好”，而是“谁能在 `<0.25x` 区域用新的结构槽位打败现有 low-cost anchors”。

## 2. LUTurbo/Lottable 兼容性约束

硬约束：

1. 最终重构必须能写成一个或多个静态向量库上的有限加权和。
2. 在线侧允许选择、路由、低秩系数、分阶段 residual，但静态库本身不能依赖当前样本动态生成。
3. 如果一个方案依赖 batch 级联动、跨样本共享预算、或输入依赖的动态生成字典，则不能进入主线。

当前 run 的成本定义：

- `selection_cost`: encoder 端为选出在线原子所需的访存
- `deployment_cost`: trunk / sparse lookup / codebook lookup 等部署端访存
- `total_cost = selection_cost + deployment_cost`
- 当前成本 proxy 以 fused QKV 原始矩阵 `1024 x 4096` 为分母
- 成本硬约束：`total_cost <= 1.5 x (1024 x 4096)`

兼容性注册表：

| family | final form | search note | structural note | compatibility |
|---|---|---|---|---|
| `topk` | `x_hat = Σ_i α_i b_i` | candidate | 单库稀疏和，最直接的可导出形式 | 直接兼容 |
| `factorized_topk` | `x_hat = Σ_i α_i b_i` | candidate | factorized scorer 只改变在线打分链路 | 直接兼容 |
| `jumprelu` | `x_hat = Σ_i α_i b_i` | candidate | threshold 只改变支持形成方式 | 直接兼容 |
| `group_topk` | `x_hat = Σ_i α_i b_i` | candidate | 组内竞争不改变 decoder 形式 | 直接兼容 |
| `gated` | `x_hat = Σ_i α_i b_i` | candidate | 门控仍导出到同一静态库和 | 直接兼容 |
| `routed` | `x_hat = Σ_i α_i b_i` | candidate | route 只改 support ranking | 直接兼容 |
| `expert_topk` | `x_hat = Σ_e Σ_{i∈S_e(x)} α_{e,i} b_{e,i}` | candidate | 轻量 router 只选择静态子库；激活路径仍是子库上的有限加权和 | 直接兼容 |
| `shared_routed_expert_topk` | `x_hat = Σ_{i∈S_shared(x)} α_i^sh b_i^sh + Σ_{e∈E(x)} Σ_{j∈S_e(x)} α_{e,j} b_{e,j}` | candidate | shared 子库始终激活，routed experts 只在静态子库间选择；最终仍是静态库有限加权和 | 直接兼容 |
| `shared_routed_factorized_expert_topk` | `x_hat = Σ_{i∈S_shared(h(x))} α_i^sh b_i^sh + Σ_{e∈E(h(x))} Σ_{j∈S_e(h(x))} α_{e,j} b_{e,j}` | candidate | factorized / low-rank basis 只改变 shared+routed 的打分链路，decoder 仍是静态子库有限加权和 | 直接兼容 |
| `bucketed_topk` | `x_hat = Σ_i α_i b_i` | candidate | bucket 决策不改变导出形式 | 直接兼容 |
| `whitened_topk` | `x_hat = Σ_i α_i b_i` | candidate | 预处理路径需要在当前 target 单独验证 | 直接兼容 |
| `batch_topk` | batch-coupled | blocked | 依赖 batch 共享预算 | 不兼容 |
| `adaptive_budget_topk` | batch-coupled / sample-coupled quota | blocked | 当前实现不是单样本静态导出 | 不兼容 |
| `lowrank_expert_topk` | `x_hat = Σ_r β_r u_r + Σ_{i∈S_{e(x)}(x)} α_{e,i} b_{e,i}` | candidate | low-rank trunk 吃掉平滑主干，router 只选择静态 expert 子库，最终仍是静态库上的有限加权和 | 扩展兼容 |
| `lowrank_residual` | `x_hat = Σ_r β_r u_r + Σ_i α_i b_i` | candidate | trunk + sparse residual | 扩展兼容 |
| `lowrank_expert_residual` | `x_hat = Σ_r β_r u_r + Σ_{i∈S_{e(x)}(x)} α_{e,i} b_{e,i} + Σ_j γ_j c_j` | candidate | trunk + expert 子库 + 全局 residual 补偿都落在静态向量库上 | 扩展兼容 |
| `lowrank_factorized_residual` | `x_hat = Σ_r β_r u_r + Σ_i α_i b_i` | candidate | 低秩 trunk + factorize
[truncated]
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
codex
当前要从 `<0.25x` 主战场里选一个新的 matched-cost 结构槽位。我先做本地 cost 自检，筛掉会自然抬到中成本区或已有强负信号的候选。
exec
/bin/bash -lc "python - <<'PY'
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class

base = dict(expansion_factor=1, k=96, factorized_hidden_dim=256, trunk_rank=8, num_experts=64, latents_per_expert=224, active_experts=2, stage1_ratio=0.25)
arches = [
    'expert_topk',
    'factorized_expert_topk',
    'shared_routed_factorized_expert_topk',
    'shared_lowrank_two_stage_residual_expert',
    'lowrank_factorized_residual',
    'shared_routed_expert_topk',
]
for arch in arches:
    cfg = SparseCoderConfig(architecture=arch, **base)
    cls = _get_sae_class(cfg.architecture)
    m = cls(1024, cfg, decoder=False)
    c = m.selection_cost_estimate(4096)
    print(arch, c['combined_accesses'], c['combined_ratio'], c['ratio'], c['deployment_ratio'])
PY" in /root/sparsify-ascend
 succeeded in 4569ms:
Using fused decoder on CUDA.
expert_topk 917504 0.22 0.12 0.09
factorized_expert_topk 786432 0.19 0.09 0.09
shared_routed_factorized_expert_topk 843776 0.2 0.11 0.09
shared_lowrank_two_stage_residual_expert 1187840 0.28 0.19 0.09
lowrank_factorized_residual 966656 0.23 0.13 0.1
shared_routed_expert_topk 1146880 0.27 0.18 0.09

codex
cost 自检完成。`expert_topk@r175-recipe` 会落在约 `0.22x`，而且比 `shared_routed_expert_topk` 更短 active path；我再核对这条 family 在当前 target 上是否已经给过同配方的近邻负信号。
exec
/bin/bash -lc "python - <<'PY'
import csv
from pathlib import Path
p=Path('research/history/results.tsv')
with p.open() as f:
    r=csv.DictReader(f, delimiter='\\t')
    for row in r:
        if row['architecture']=='expert_topk':
            print(row['experiment_id'], row['k'], row['val_fvu'], row['description'])
PY" in /root/sparsify-ascend
 succeeded in 0ms:
r23_1774715886 64 0.36355817317962646 修复 `Unknown architecture: 'expert_topk'` 的注册链阻塞。已在 `sparsify/config.py` 把 `expert_topk` 加入合法 architecture 白名单，在 `sparsify/sparse_coder.py` 增加 `_get_sae_class()` dispatch，并实现最小 `ExpertTopKSparseCoder` 原型；随后用失败时相同的 `architecture=expert_topk, K=64, EF=1` 完成 instantiate/forward/backward smoke test，通过。
r24_1774716149 64 0.36355817317962646 不再改实现，直接重跑已修复的 `expert_topk` prototype。相对 r10 的完整配置只改一个 env 参数：`ARCHITECTURE=expert_topk`，保持 `K=64`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 不变，先拿到这个新 family 在当前 target 的首个有效训练结果。
r25_1774716612 128 0.3148818016052246 相对 r23 的完整配置，本轮只改一个 env 参数：`K=128`。保持 `ARCHITECTURE=expert_topk`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 不变，专门测试单 expert 路由原型在更高局部 sparse budget 下的 FVU 改善幅度。预计 total_cost 约从 0.13x 升到 0.19x（selection 约 0.06x，deployment 约 0.12x）。
r26_1774717099 192 0.29111021757125854 相对 r25 的完整配置，本轮只改一个 env 参数：`K=192`。保持 `ARCHITECTURE=expert_topk`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 不变，专门测单 expert 局部预算继续增大后的质量斜率。预计 total_cost 约从 0.19x 升到 0.25x（selection 约 0.06x，deployment 约 0.19x）。
r27_1774717830 192 0.29111021757125854 修复 AutoResearch 对 expert_topk 的兼容性注册后，基于 r21 做一次低开销 MoE-like probe：仅把 ARCHITECTURE 改为 expert_topk，保留 K=192、EF=1、adam、8e-4，验证轻量 router + 单 expert 子库是否能在 0.25x 总成本附近提供比现有低成本点更好的 FVU。
r51_1774750001 192 0.1273030936717987 下一轮不再回到 plain selector 邻域，也不继续在 lowrank_expert_residual 上做局部压缩；先回答高优先级的真 MoE-like 问题。相对 r26 只改 NUM_EXPERTS=16，是对 expert_topk 做最干净的单变量稀疏扩容验证：active path 仍短，cost proxy 预计 selection≈0.25x、deployment≈0.19x、total≈0.44x，正好落在当前最需要补强的低成本带。
r52_1774750624 224 0.14567476511001587 继续追 MoE-like 这条已出现明确信号的新结构线，而不是回到 plain selector 邻域。相对 r51 只改 `K=224`，是在已验证 `NUM_EXPERTS=16` 生效的前提下，测试 routed sublibrary 的低成本 Pareto 能否继续向下推进；按当前 cost proxy 估算，encoder 仍约 0.25x，deployment 约 0.22x，total 约 0.47x，仍留在当前重点关注的 <0.5x 带内。
r68_1774767244 192 0.12482442706823349 r67 已经对 `lowrank_expert_topk` 的继续扩容给出负信号，说明“更大 routed capacity + trunk”这条线当前不值得继续加码。本轮转向更干净的直接兼容 MoE-like 探针：相对 r51 只增大 `expert_topk` 的 `NUM_EXPERTS`，用不含 trunk 的 routed-only 结构回答容量收益是否仍成立。按当前代码路径成本估算，`expert_topk K=192 NUM_EXPERTS=32 EF=1` 约在 0.45x total_cost，仍落在本轮优先关注的低成本带。
r69_1774768189 160 0.13341990113258362 r68 已经证明 `expert_topk` 在 true sparse-capacity 模式下相对旧锚点有真实收益。Round 69 不再继续扩容，而是做一次更有信息量的左移 probe：相对 r68 只降 `K`，直接回答这条 routed-only MoE-like 结构能否在更低部署成本下仍保持可用 FVU。按当前代码路径成本估算，`expert_topk K=160 NUM_EXPERTS=32 EF=1` 约为 selection 0.26x、deployment 0.16x、total 0.41x，仍稳定位于本轮优先关注的低成本带。
r73_1774773563 160 0.12601399421691895 Round 73 不回到旧 family 的 K/LR 邻域，而是沿当前最干净的 routed-only MoE-like 方向做一次单变量 sparse-capacity probe：以 r69 为参考，只把 `NUM_EXPERTS` 提到 64。这个动作正对当前“总容量与 active path 解耦”这一高优先级问题，而且成本预计仍停留在约 0.23x 附近，适合补当前明显欠覆盖的低成本前沿。
r74_1774774441 160 0.12429413199424744 Round 74 不回到旧 family 的 K 轴，也不立刻切去别的结构；沿 r73 的正信号继续做单变量 sparse-capacity continuation。只把 `NUM_EXPERTS` 提到 128，直接判断 routed-only MoE-like 前沿的容量收益是否还在持续，而成本仍预计留在 <0.5x 的低成本带。
r75_1774774688 160 0.16780081391334534 Round 75 继续沿 r73、r74 的正信号做单变量 sparse-capacity continuation，不回到旧 family 的 K 轴微调。只把 `NUM_EXPERTS` 提到 256，直接判断 routed-only `expert_topk` 的容量收益是否还能继续换到更低 FVU，同时仍留在低成本带。 [INVALID: requested structural parameter did not reach runtime]
r78_1774785956 192 0.1345272958278656 Round 78 不继续追 r77 已明显失效的 `factorized_expert_topk`，也不回到旧 family 的局部微调；而是在当前最干净、已验证有效的直接兼容 MoE-like 线上做一次高信息量 matched probe。以 r74 为 reference，只把 `K` 从 160 提到 192，直接区分当前 routed-only `expert_topk` 低成本前沿的主要瓶颈究竟还是总容量，还是已经转到单 token 的 lookup budget。
r93_1774800062 160 0.20399117469787598 不继续重复 `factorized_expert_topk` 的 matched 对照，也不回到旧 family 的局部 K 轴。以 r74 为 reference，只加 `ACTIVE_EXPERTS=2`，直接回答 routed-only `expert_topk` 的下一步关键结构问题：当前收益瓶颈到底在总容量，还是在每个 token 只看一个 expert 的过短 active path。
r136_1774845689 128 0.2147471308708191 相对 r25 的完整配置，本轮只改一个 env 参数：`NUM_EXPERTS=128`。保持 `ARCHITECTURE=expert_topk`、`K=128`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 与其余训练配方不变，做 routed-only 容量重验证。按当前 `expert_topk` 默认 expert layout，这会从默认 4-expert base 模式切到显式 sparse-capacity 模式，而 `ACTIVE_EXPERTS` 仍默认 1；本地成本估计为 selection≈0.28x、deployment≈0.12x、total≈0.41x，落在 <0.5x 低成本带内，并显著低于 1.5×h×n 硬约束。
r137_1774846552 128 0.16879674792289734 相对 r136 的完整配置，本轮只改一个 env 参数：`LATENTS_PER_EXPERT=320`。保持 `ARCHITECTURE=expert_topk`、`K=128`、`NUM_EXPERTS=128`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 与其余训练配方不变，直接检验 r136 的负结果是否主要由默认 per-expert width 过宽造成。按本地 cost 估计，selection 将从约 0.28x 降到约 0.11x，deployment 保持约 0.12x，total_cost 约 0.23x，明显低于当前 `<0.5x` 带并远低于 1.5×h×n 硬约束。

codex
我现在补一个更贴近当前 low-cost anchor 的对照：把 r165 的现成 cost anchor recipe 切到几个 architecture，看看哪些还能留在 `<0.25x`，以及 `expert_topk` 这种更短 active path 是否值得正面复验。
exec
/bin/bash -lc "python - <<'PY'
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class
base = dict(expansion_factor=1, k=108, factorized_hidden_dim=256, trunk_rank=40, num_experts=64, latents_per_expert=160, active_experts=2, stage1_ratio=0.25)
arches = ['expert_topk','factorized_expert_topk','shared_lowrank_routed_expert_topk','shared_routed_factorized_expert_topk','lowrank_expert_topk']
for arch in arches:
    cfg = SparseCoderConfig(architecture=arch, **base)
    cls = _get_sae_class(cfg.architecture)
    m = cls(1024, cfg, decoder=False)
    c = m.selection_cost_estimate(4096)
    print(arch, c['combined_accesses'], c['combined_ratio'], c['ratio'], c['deployment_ratio'])
PY" in /root/sparsify-ascend
 succeeded in 4142ms:
Using fused decoder on CUDA.
expert_topk 835584 0.2 0.09 0.11
factorized_expert_topk 802816 0.19 0.09 0.11
shared_lowrank_routed_expert_topk 1089536 0.26 0.15 0.11
shared_routed_factorized_expert_topk 843776 0.2 0.1 0.11
lowrank_expert_topk 1081344 0.26 0.11 0.14

exec
/bin/bash -lc "python - <<'PY'
from sparsify.config import SparseCoderConfig
from sparsify.sparse_coder import _get_sae_class
cfg = SparseCoderConfig(architecture='expert_topk', expansion_factor=1, k=108, factorized_hidden_dim=256, trunk_rank=40, num_experts=64, latents_per_expert=160, active_experts=2, stage1_ratio=0.25)
cls = _get_sae_class(cfg.architecture)
m = cls(1024, cfg, decoder=False)
print(m.selection_cost_estimate(4096))
PY" in /root/sparsify-ascend
 succeeded in 4007ms:
Using fused decoder on CUDA.
{'total_accesses': 393216, 'original_matmul_accesses': 4194304, 'ratio': 0.09, 'budget_ratio': 0.06, 'feasible': True, 'breakdown': [{'name': 'router', 'accesses': 65536, 'shape': '1024x64'}, {'name': 'active_expert_encoder', 'accesses': 327680, 'shape': 'active=2×1024x160'}], 'deployment_accesses': 442368, 'deployment_ratio': 0.11, 'deployment_breakdown': [{'name': 'sparse_lookup', 'accesses': 442368, 'shape': 'K=108×n=4096'}], 'combined_accesses': 835584, 'combined_ratio': 0.2, 'combined_budget_ratio': 0.13, 'combined_feasible': True}

codex
{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"expert_topk"}],"expected_win":"explore_unknown","experiment_tier":"full","family_name":"expert_topk","family_stage":"probe","hypothesis":"候选 family 设为 `expert_topk`。最终重构公式为 `x_hat = Σ_{e∈E(x)} Σ_{i∈S_e(x)} α_{e,i}(x) b_{e,i}`：其中 `{b_{e,i}}` 是各个 expert 的静态子库原子，在线侧只做轻量 router 选择少量 active experts，并在对应静态子库内做 local top-k 与有限加权和，因此可直接导出到 LUTurbo/Lottable 的“静态 expert 子库选择 + 子库查表 + 加权求和”推理链路，兼容性判断为 `直接兼容`。`batch_topk`、`adaptive_budget_topk` 这类 batch/sample-coupled family 仍是 `不兼容`，只能作为历史参考，不能驱动当前 frontier。当前 `<0.25x` 主战场里，`shared_routed_expert_topk` 的 `K≈108-112` 与 `LATENTS_PER_EXPERT≈160` 局部 knee 已基本摸清，最近两轮 `factorized_topk` 与 `lowrank_expert_topk` 也没有形成新的 low-cost frontier 扩展；因此下一步更高信息量的问题不是继续补同 family 插值，而是做一个 matched-cost architecture swap，直接回答“shared 分支是否真是当前 low-cost 质量的必要组成，还是更短的 routed-only active path 反而更值”。相对 r165 只把 `ARCHITECTURE` 从 `shared_routed_expert_topk` 切到 `expert_topk`，保持 `K=108`、`EXPANSION_FACTOR=1`、`ACTIVE_EXPERTS=2`、`LATENTS_PER_EXPERT=160`、`NUM_EXPERTS=64`、`adam`、`LR=8e-4` 与其余训练配方不变，直接比较“matched low-cost anchor 下，去掉 always-on shared branch 后的 routed-only MoE-like 结构”是否能在更低总成本下逼近或击败现有 low-cost anchors。本地成本自检显示该候选在当前 1024x4096 proxy 下 selection_accesses=393216、deployment_accesses=442368、combined_accesses=835584，对应 combined_ratio 约 `0.20x`，明确落在当前 `<0.25x` 主战场内，也显著低于 1.5×h×n 硬约束。","needs_sanity":false,"next_hypotheses":["如果 `expert_topk@K108,ACTIVE_EXPERTS=2,LATENTS_PER_EXPERT=160` 在约 `0.20x` 下把 FVU 压到接近或优于当前 `0.20x-0.21x` anchors，则下一轮继续留在该 family，只改 `K=112`，测试 routed-only 低成本线是否仍主要受 lookup budget 限制。","如果该点仍明显弱于同带 `shared_routed_expert_topk` anchors，则说明当前 low-cost 成功点的关键增益确实来自 shared branch，而不是 routed-only active path；后续应停止再把 `expert_topk` 作为主线挑战者，转去别的兼容结构槽位。"],"notes_to_memory":["`expert_topk` 的 LUTurbo 导出形式是静态 expert 子库上的有限加权和，兼容性应保持为 `直接兼容`。","这轮是以当前 low-cost matched baseline r165 为 anchor 的 architecture-level probe，不是继续沿 `shared_routed_expert_topk` 或 `lowrank_expert_topk` 做 `K/LATENTS_PER_EXPERT` 线性插值。","本地成本自检：相对 r165 仅切到 `expert_topk` 后，selection_accesses=393216、deployment_accesses=442368、combined_accesses=835584，对当前 1024x4096 proxy 的 total_cost 约 `0.20x`，显著低于当前 `shared_routed_expert_topk K=108` 的约 `0.24x`。","相比之下，`factorized_expert_topk`、`shared_routed_factorized_expert_topk`、`shared_lowrank_routed_expert_topk` 在当前 target 上已经给过更强的低成本负信号，当前不应优先继续复验它们。"],"reference_round":165,"self_review":"这轮严格遵守 param_only 单变量约束：相对 r165 只改 `ARCHITECTURE`。选择 r165 而不是继续基于 r175 做同 family 的内部轴压缩，是因为当前策略已明确要求把 `shared_routed_expert_topk` 视为 matched baseline / cost anchor，而不是继续沿已有负边际的 family 内部轴推进。选择 `expert_topk` 而不是已多轮失利的 factorized/shared-lowrank 变体，是因为它同时满足三点：成本路径清晰落在 `<0.25x`、active path 更短、且在当前 exact recipe 下仍缺一个干净的 matched-cost architecture 对照。主要风险是 routed-only 去掉 shared branch 后质量会明显退化；但这正是当前最需要被干净回答的结构问题。","summary":"以 r165 为 reference，只改 `ARCHITECTURE=expert_topk`，做一轮新的 matched-cost low-cost architecture probe。目标是在约 `0.20x` 总成本下直接判断 routed-only、短 active path 的直接兼容 MoE-like 结构，是否能在当前主战场挑战现有 `shared_routed_expert_topk` low-cost anchors。","touched_files":[]}
tokens used
318,602
