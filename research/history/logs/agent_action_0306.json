{"change_type":"edit_sae_code","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"shared_conditional_cascade_product_key_expert_jumprelu"}],"expected_win":"lower_objective","family_name":"shared_conditional_cascade_product_key_expert_jumprelu","family_stage":"prototype","hypothesis":"最终重构公式设为 `x_hat = Σ_{i∈S_sh(x)} α_i^sh b_i^sh + Σ_{j∈S_{e1(r1)}^τ(r1)} β_j c_{e1,j} + 1[ρ(r1-expert1_only(r1)) > 0.5·ρ(r1)] · Σ_{m∈S_{e2(r1)}^τ(r2)} γ_m c_{e2,m}`，其中 `r1(x)=x-shared(x)`，`r2(x)=r1(x)-expert1(r1)`，`ρ(·)` 是 token 级均方残差能量，`(e1,e2)` 是同一次 product-key 路由在 `r1` 上选出的有序 top-2 静态 expert 子库。在线导出仍是“shared 静态库查表 -> PK 选静态 expert 子库 -> 第一位 expert cleanup -> 仅当第一位 cleanup 后残差仍高时才打开第二位 expert -> 有限加权和 -> 选择性补偿”，不涉及动态字典生成，因此兼容性判断为 `直接兼容`。它与 `shared_cascade_product_key_expert_jumprelu` 的精确差异不是重命名，而是把第二个 expert 从 always-on 改成 post-cleanup residual-energy trigger；它也不同于 `shared_adaptive_active_product_key_expert_jumprelu`，因为触发决策不再只看路由置信度，而是显式看第一阶段 cleanup 是否失败。相对 r305 本轮只改 `ARCHITECTURE=shared_conditional_cascade_product_key_expert_jumprelu`，其余 `K=32, EF=1, NUM_EXPERTS=3072, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.25, OPTIMIZER=adam, LR=8e-4` 保持不变，直接测试“shared coarse + ordered PK cascade + 条件触发第二专家”能否在近同成本下更强压低 hard-token exceed。该配置本地精确成本复核为 `selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`，仍在预算内；若按 round_0305 摘要中的 `objective=0.267107` 为基准，要击败它需要把 `exceed_alpha_0.50` 压到 `0.159929265625` 以下。","needs_sanity":true,"next_hypotheses":["如果该 family 在相同约 `0.1072x` 成本下把 exceed 压到 `<=0.1599` 并赢过 r305，下一轮优先只改 `NUM_EXPERTS=2048` 或 `3072/4096` 之一做单轮 knee 校准，判断条件触发语义是否真的扩大了 cascade scaffold 的容量交换率。","如果该 family 的 exceed 比 r305 明显下降但 objective 仍未赢，说明“post-cleanup trigger”方向有效但交换率还不够；后续应继续围绕条件 cleanup 语义升级，而不是回到 plain cascade / fixed-AE 的线性插值。","如果该 family 与 r305 持平或更差，则可更明确地下调‘同一 PK expert 对内再做条件级联’这一支线的优先级；下一轮应转向别的 scaffold-level tail-cleanup 语义，而不是继续补这个邻域。"],"notes_to_memory":["已实际落盘新 family `shared_conditional_cascade_product_key_expert_jumprelu`，真实改动文件为 `sparsify/config.py`、`sparsify/sparse_coder.py`、`research/AutoResearch/override_registry.py`。","该 family 的精确结构差异是：保留 `shared_cascade_product_key_expert_jumprelu` 的 shared coarse + ordered PK top-2 expert scaffold，但第二个 expert 只在第一位 expert 的 single-expert cleanup 后残差能量仍超过第一阶段残差能量 50% 时才生效。","本地 exact-string smoke 已通过：`shared_conditional_cascade_product_key_expert_jumprelu` 在 `K=32, EF=1, NUM_EXPERTS=3072, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=56, STAGE1_RATIO=0.25` 下可 instantiate、forward、backward。","本地 override allowlist 校验已通过：`validate_env_overrides([{key:'ARCHITECTURE', value:'shared_conditional_cascade_product_key_expert_jumprelu'}], fallback_architecture='shared_cascade_product_key_expert_jumprelu') == []`。","本地成本自检：`selection_accesses=285696`、`deployment_accesses=163840`、`combined_accesses=449536`、`exact total_cost_ratio=0.107177734375`；与 r305 处于同一成本档并满足预算。","本地 `validate_action(...)` 复核已通过：以 `reference_round=305` 且仅改 `ARCHITECTURE=shared_conditional_cascade_product_key_expert_jumprelu` 的提案不会被当前 policy 拦截。"],"reference_round":305,"self_review":"这轮保持单一主假设：不是继续扫 `NUM_EXPERTS/K/STAGE1_RATIO`，而是在当前 best scaffold 上引入真正新的“第一阶段失败才打开第二阶段 cleanup”语义。主要风险是当前 cost proxy 不会奖励条件路径，因此它必须通过更低的 exceed 才能赢；如果 exceed 只小幅下降，这条线就不应继续吃预算。","summary":"已实现并接通 `shared_conditional_cascade_product_key_expert_jumprelu`。建议以下一轮以 r305 为 reference，只改 `ARCHITECTURE`，做一轮 matched-cost architecture probe，直接裁决“ordered cascade + post-cleanup conditional second expert”是否能比 current cascade scaffold 更有效压低 `exceed_alpha_0.50`。","touched_files":["sparsify/config.py","sparsify/sparse_coder.py","research/AutoResearch/override_registry.py"]}