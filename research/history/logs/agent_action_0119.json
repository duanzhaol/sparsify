{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_two_stage_soft_codebook_residual"},{"key":"EXPANSION_FACTOR","value":"2"},{"key":"K","value":"128"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.0e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"expected_win":"lower_fvu","experiment_tier":"full","family_name":"lowrank_two_stage_soft_codebook_residual","family_stage":"mainline","hypothesis":"最终重构公式保持为 x_hat = U(Vx) + Σ_m pi_m c_m + Σ_i alpha_i^(1) b_i + Σ_j alpha_j^(2) b_j。low-rank trunk、soft codebook、两阶段 residual 都是静态向量库上的有限加权和，因此可导出到 LUTurbo/Lottable 的“查表 + 加权求和 + 选择性补偿”推理链路；该 family 属于扩展兼容。当前 EF=2 已稳定满足 encoder 选择成本硬约束，`signum + LR=3.0e-3` 是已复现的低成本主线锚点；将 `AUXK_ALPHA` 从 0.03125 降到 0.015625 后未见明显退化，说明辅助稀疏损失在该低 EF regime 里可能已非必要。本轮只改一个主变量 `AUXK_ALPHA`，进一步降到 `0.0`，回答唯一问题：完全去掉 auxiliary K loss 是否能减少对主重构目标的约束干扰，在不增加训练代价的前提下进一步改善 FVU 或曲线形状。","needs_sanity":false,"next_hypotheses":["如果 `AUXK_ALPHA=0.0` 改善终点或曲线形状，且 `tokens_per_sec`、总耗时、`final_total_tokens` 基本不变，则再做一次 clean baseline 复现确认。","如果 `AUXK_ALPHA=0.0` 出现退化，则关闭 auxk 降低分支，恢复默认 `0.03125` 或保留已验证等价的较小值，不再继续在该轴上消耗轮次。","在 loss/preprocess 轴未得到清晰正信号前，不改 K，也不改 TRUNK_RANK / NUM_CODES / STAGE1_RATIO / FACTORIZED_HIDDEN_DIM。"],"notes_to_memory":["兼容性判断：`lowrank_two_stage_soft_codebook_residual` 为扩展兼容；导出形式不变，仍是 trunk 库 + codebook 库 + residual 字典库的有限加权和。","`adam` 分支已爆炸，当前主线仍应固定在 `signum + LR=3.0e-3`。","本轮严格单变量，只改 `AUXK_ALPHA`；该改动不影响 encoder 选择成本，也不应显著增加训练时长，评估时继续同时检查曲线形状、`tokens_per_sec`、总耗时与 `final_total_tokens`。"],"primary_variable":"auxk_alpha","self_review":"这仍符合当前主线顺序。EF、clean baseline、LR 和 optimizer 结论都已基本清楚，而上轮把 `AUXK_ALPHA` 减半后没有负面信号，因此现在最小且可归因的后续问题就是测试 auxiliary loss 是否可以彻底移除。相比切换到更重的 preprocess 改动，这个问题更干净、代价更低。","summary":"保持 `lowrank_two_stage_soft_codebook_residual / EF=2 / K=128 / signum / LR=3.0e-3` 主线不变，仅将 `AUXK_ALPHA` 进一步降到 `0.0`，测试低成本 regime 下是否可以完全去掉 auxiliary K loss 来改善重构质量。","touched_files":[]}