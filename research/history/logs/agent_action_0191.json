{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"shared_two_stage_residual_expert"}],"expected_win":"lower_fvu","experiment_tier":"proxy","family_name":"shared_two_stage_residual_expert","family_stage":"prototype","hypothesis":"候选 family 设为 `shared_two_stage_residual_expert`。最终重构公式写成 `x_hat = Σ_{i∈S_sh(x)} α_i^sh(x) u_i + Σ_{e∈E(r)} Σ_{j∈S_e(r)} γ_{e,j}(r) c_{e,j}`，其中 `r = x - Σ_{i∈S_sh(x)} α_i^sh(x) u_i`；`{u_i}` 是 shared coarse 静态向量库，`{c_{e,j}}` 是 expert residual 静态子库。它可直接导出到 LUTurbo/Lottable 的“shared 查表+加权求和，再做 expert residual 查表+选择性补偿”流水线，静态库不随样本动态生成，因此兼容性判断为 `扩展兼容`。唯一主假设：相对 r188 仅切换到该 architecture，在保持 `K=96` 的 small-K low-cost recipe 下，显式把 shared/coarse branch 与 expert residual cleanup 分工开来，会比当前 small-K anchors 更慢地随 K 下降而退化，并在约 `0.23x total_cost` 内把 FVU 压到比现有 `K=96` 非 shared-routed probe 更低、且更接近 `<0.25x` 主前沿。","needs_sanity":false,"next_hypotheses":["如果这轮在约 0.23x 下接近或优于 `shared_routed_expert_topk K=96`，下一轮优先继续留在该 family，只改 `STAGE1_RATIO` 以校准 shared/coarse 与 residual cleanup 的预算分配。","如果这轮仍明显落后于 `<0.25x` anchors，则可基本结束“cheap shared/coarse branch + routed residual cleanup”这条 trunk-free 低成本槽位，把预算转回别的 architecture-level probe。"],"notes_to_memory":["本地成本自检：`shared_two_stage_residual_expert` 在 `K=96, EF=1, NUM_EXPERTS=64, ACTIVE_EXPERTS=2, LATENTS_PER_EXPERT=160, STAGE1_RATIO=0.25` 下 selection≈0.13x、deployment≈0.09x、combined≈0.23x，显著低于 1.5×h×n 硬约束。","该 family 已有一次更高 K 证据：r183 在近同 low-cost recipe 的 `K=108` 上得到 `FVU≈0.1411`；本轮把问题收紧到 `K=96`，直接回答当前最重要的 small-K robustness。"],"reference_round":188,"self_review":"这轮严格满足单变量原则：相对 r188 只改 `ARCHITECTURE`。没有新增 tunable 参数，也没有代码落盘，因此不需要扩展到 `research/AutoResearch/` 或 `scripts/`。选择这轮而不是继续沿 `shared_routed_expert_topk`/`lowrank_expert_topk` 扫 `K` 或 `LATENTS_PER_EXPERT`，是因为当前策略已明确要求优先做 architecture-level probe，并且这条 family 正好对应“先让 cheap shared/coarse branch 吃掉平滑主干，再让 experts 只做 sparse residual cleanup”的当前高优先级结构假设。主要风险是该 family 目前不在旧兼容表显式列名里，若运行时后处理仍按旧表过滤，需要后续补记忆层兼容登记；但从导出形式看它满足 LUTurbo 静态库有限加权和硬约束。","summary":"建议以 r188 为 reference，只改 `ARCHITECTURE=shared_two_stage_residual_expert`。这是一次落在 `<0.25x` 主战场内的 small-K matched architecture probe，用最低额外成本直接验证“shared coarse branch + expert residual cleanup”能否在 `K=96` 比现有 low-cost probes 更稳。","touched_files":[]}