{"command":"run","hypothesis":"最终重构公式为 x_hat = Σ_{r=1..R} β_r(x) u_r + Σ_{i∈S_{e(x)}(x)} α_{e,i}(x) b_{e,i} + Σ_j γ_j(x) c_j，其中 U={u_r} 是静态 low-rank trunk 向量库，{B_e} 是静态 expert 子库，C={c_j} 是静态 residual 补偿库；在线可直接导出到 LUTurbo/Lottable 的“trunk 查表 + expert 子库查表 + residual 补偿查表 + 有限加权和”推理链路，不依赖动态生成字典，因此兼容性判断为扩展兼容。唯一主假设：相对 r60 仅把 ARCHITECTURE 从 `lowrank_expert_topk` 切到 `lowrank_expert_residual`，在保持 `TRUNK_RANK=64`、`NUM_EXPERTS=32`、`K=128`、`EF=1` 不变时，直接回答当前 shared+expert 主干在当前 target 上是否仍明显缺少全局 residual cleanup；如果 r60 的剩余误差主要来自未被 routed expert 覆盖的全局残差结构，则三段式结构应在约 0.7x total_cost 的中成本带把 FVU 显著压低，并形成新的结构性 Pareto 段。","summary":"不回到 plain selector，也不继续做 `lowrank_expert_topk` 的局部 K/LR 微调；以 r60 为 matched reference，只改 `ARCHITECTURE`，验证在已增强的 trunk+expert 主干上，global residual cleanup 是否仍然必要。按当前代码成本估算，这个 matched `lowrank_expert_residual` 点约为 encoder 0.54x、deployment 0.19x、total 0.73x，仍明显低于 1.5x 硬预算，属于允许保留的中成本质量锚点。","change_type":"param_only","expected_win":"explore_unknown","family_name":"lowrank_expert_residual","family_stage":"prototype","self_review":"满足单轮单变量约束：相对 r60 只改 `ARCHITECTURE`。该 family 属于 LUTurbo 扩展兼容 family，不触碰不兼容方向，也不需要新增 tunable 参数接线。之所以优先做这轮 matched 结构切换，而不是继续扫 `TRUNK_RANK` 或 `NUM_EXPERTS`，是因为 r60 已经证明更宽 trunk 的 `lowrank + expert` 在低成本带有效，当前更高信息量的问题变成“ residual cleanup 现在是否仍是主要增益来源”。主要风险是成本会从低成本带抬到中成本带；若 FVU 改善不明显，则说明当前更强的 shared+expert 主干已吃掉大部分可学结构，后续不应继续让三段式局部打磨主导预算。","needs_sanity":false,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_expert_residual"}],"touched_files":[],"notes_to_memory":["`lowrank_expert_residual` 的 LUTurbo 形式保持为 trunk 静态库 + expert 静态子库 + residual 静态补偿库上的有限加权和，兼容性为扩展兼容。","Round 61 是对 r60 的 matched 结构对照：同样的 `TRUNK_RANK=64` 与 `NUM_EXPERTS=32` 下，只看加入 residual cleanup 是否带来足够大的质量收益。","按当前代码成本估算，`lowrank_expert_residual K=128 TRUNK_RANK=64 NUM_EXPERTS=32` 约 total_cost 0.73x，远低于 1.5x 硬预算，可作为中成本结构锚点。"],"next_hypotheses":["如果 matched `lowrank_expert_residual` 显著优于 r60，则下一轮保持 family 不变，只改 `K=160` 或回调 `TRUNK_RANK=32`，判断三段式结构的更优 Pareto 落点是在 residual 预算还是 trunk 预算。","如果它只小幅优于 r60，则说明当前 target 上更强的 `lowrank + expert` 已基本足够，后续应优先继续压 `lowrank_expert_topk` 的成本而不是让 residual family 重新主导搜索。","如果结果异常，先核对 round config 与 checkpoint config 中 `architecture=lowrank_expert_residual` 是否真实生效，再决定是否把该轮当作结构证据。"],"reference_round":60}