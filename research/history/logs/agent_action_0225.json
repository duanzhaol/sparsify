{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"factorized_expert_topk"}],"expected_win":"explore_unknown","family_name":"factorized_expert_topk","family_stage":"prototype","hypothesis":"候选 family 设为 `factorized_expert_topk`。最终重构公式为 `x_hat = Σ_{e∈E(h(x))} Σ_{i∈S_e(h(x))} α_{e,i}(h(x)) b_{e,i}`，其中 `h(x)=Bx` 是共享低维 bottleneck，`E(h(x))` 是轻量 router 选出的少量 active experts，`{b_{e,i}}` 是各 expert 的静态子库原子。在线导出链路仍是“shared bottleneck 投影 + 轻量 expert 路由 + expert 子库查表 + 加权求和 + 选择性在线补偿”，静态库不随样本动态生成，因此该 family 判定为 `扩展兼容`。`expert_topk`、`shared_routed_expert_topk` 仍属兼容 family；`batch_topk`、`adaptive_budget_topk` 等 batch/sample-coupled family 仍属 `不兼容`，不能驱动当前 objective leaderboard。唯一主假设：相对 r212 仅把 `ARCHITECTURE` 从 `expert_topk` 切到 `factorized_expert_topk`，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4`、`ACTIVE_EXPERTS=2`、`NUM_EXPERTS=128`、`LATENTS_PER_EXPERT=192`、`FACTORIZED_HIDDEN_DIM=256`、`STAGE1_RATIO=0.25`、`TRUNK_RANK=16` 与其余 recipe 不变，直接裁决“shared bottleneck + expert-specific head”这条结构槽位在当前 ultra-low-cost lane 是否还值得继续。本地精确成本复核显示该点 selection_accesses=393216、deployment_accesses=163840、combined_accesses=557056，对当前 1024x4096 fused-QKV proxy 的 exact total_cost_ratio=0.1328125；相对 r212 的 exact 0.1640625 下降了 0.03125，因此若要刷新当前 objective=0.35553750753402713，需要 exceed_alpha_0.50 压到约 `≤0.222725`。这不是高把握度翻盘点，但它是当前仍未在 `K=32` 主战场做过干净 matched probe、且仍有现实赢面门槛的剩余结构裁决。","needs_sanity":false,"next_hypotheses":["如果 `factorized_expert_topk@K32` 仍然给出 `exceed_alpha_0.50 > 0.223` 或 objective 明显落后于 r212，则可把“shared bottleneck + expert-specific head”整条 factorized-expert 线在当前 target 上整体降级，不再继续细扫。","如果该 probe 的 objective 接近 r212 但仍略差，说明更轻 scorer/router 方向并未完全失效；下一轮才值得保持 family 不变，只改 `FACTORIZED_HIDDEN_DIM` 做单变量容量归因。","如果该 probe 意外优于 r212，则说明当前 hookpoint 的主要缺口仍可能在 encoder 选择成本而非部署成本；下一轮优先保持 family 不变，只改 `K=48`，测试在仍低 cost 的前提下能否继续换回更低 exceed。"],"notes_to_memory":["当前 `0.19x~0.21x` matched architecture probes 已基本给出负信号：`lowrank_expert_topk`、`shared_two_stage_residual_expert`、`shared_routed_expert_topk`、`shared_lowrank_*` 都未把 objective 拉回 r212 附近。","本地 allowlist 校验通过：`validate_env_overrides([{key: ARCHITECTURE, value: factorized_expert_topk}], fallback_architecture='expert_topk')` 返回空列表。","本地成本复核：`factorized_expert_topk K=32 EF=1 NUM_EXPERTS=128 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=192 FACTORIZED_HIDDEN_DIM=256` 的 exact total_cost_ratio=0.1328125，其中 selection=0.09375x、deployment=0.0390625x。","如果这轮仍守不住 exceed，则当前 target 上“更轻 shared bottleneck expert routing”应视为已被充分裁决，不应继续占用 matched architecture probe 配额。"],"reference_round":212,"self_review":"这轮严格满足单变量原则：相对 r212 只改 `ARCHITECTURE`，没有新增参数，也没有代码改动。没有再选更重的 `two_stage_residual_expert`、`lowrank_expert_residual`、codebook/whitened 系列，是因为本地 exact cost 复核表明它们在当前 matched recipe 下已高到几乎不可能改善 objective；也没有回到已在 `0.21x` 一带明确失败的 shared/coarse 线。选择 `factorized_expert_topk` 的风险同样明确：r211 在更高 K=48 下已经给出 `exceed=0.332294` 的强负信号，所以这一步更像 closure probe 而不是高置信 winning probe。但当前剩余候选里，只有它仍同时满足三点：一是 LUTurbo 可导出；二是 exact total_cost 低于 incumbent，仍有现实的 objective 赢面门槛；三是尚未在当前 `K=32` anchor recipe 上做过一次干净 matched architecture 裁决。","summary":"建议以 r212 为 reference，只改 `ARCHITECTURE=factorized_expert_topk`。这轮不再赌更重结构，而是用 exact total_cost_ratio=0.1328125 的 ultra-low-cost matched probe，直接裁决“shared bottleneck + expert-specific head”这条剩余低成本结构槽位在当前 target 上是否还值得继续。","touched_files":[]}