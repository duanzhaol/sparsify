{"command":"run","hypothesis":"`lowrank_soft_codebook_residual` dominates from `K=2` to `K=16` but loses at `K=1`, while `lowrank_two_stage_residual` still owns the `K=1` endpoint. A hybrid that adds a soft codebook coarse residual projection before the proven two-stage low-rank residual refinement may recover the `K=1` slot without giving up the family’s low-K gains.","summary":"Added and smoke-tested a new architecture, `lowrank_two_stage_soft_codebook_residual`, then propose one architecture experiment at `K=1` with the existing winning low-K recipe otherwise fixed.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"smaller_k","family_name":"lowrank_two_stage_soft_codebook_residual","family_stage":"prototype","self_review":"This is a single-hypothesis architecture step targeted exactly at the remaining uncovered endpoint. It reuses two validated ingredients already showing complementary strengths in this repo and was integration-smoke-tested on the exact launch target `K=1, EF=12`.","needs_sanity":false,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_two_stage_soft_codebook_residual"},{"key":"K","value":"1"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["/root/sparsify-ascend/sparsify/config.py","/root/sparsify-ascend/sparsify/sparse_coder.py"],"notes_to_memory":["Round 70 introduces `lowrank_two_stage_soft_codebook_residual`, a new prototype that combines the soft codebook coarse residual projection with the existing low-rank two-stage residual refinement.","Integration chain completed for `lowrank_two_stage_soft_codebook_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation in `sparsify/sparse_coder.py`.","Exact smoke test passed for `lowrank_two_stage_soft_codebook_residual` at `K=1, EF=12` on the concrete dispatched architecture class (`integration_smoke: OK ... cuda`).","Round 70 should test whether this hybrid can beat the current `K=1` frontier anchor (`lowrank_two_stage_residual`, FVU `0.17282412946224213`) while keeping the stronger `LR=3.2e-3` recipe that benefited the soft-codebook family across `K=2-16`."],"next_hypotheses":["If the hybrid wins at `K=1`, test `K=2` to see whether it can subsume the current `lowrank_soft_codebook_residual` low-K band.","If it is close but slightly behind at `K=1`, consider one confirmatory rerun before deciding whether the deficit is architectural or noise.","If it clearly loses at `K=1`, retain `lowrank_soft_codebook_residual` for `K=2-16`, keep `lowrank_two_stage_residual` at `K=1`, and resume a different new-family search for the endpoint."],"primary_variable":"architecture"}

--- STDERR ---
OpenAI Codex v0.116.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: none
session id: 019d1815-ceb4-76a0-95df-7a0fec4e3c85
--------
user
Continue the SAE research session. Round 70.
Return one JSON object matching the action schema. No markdown fences.

Structured update:
{
  "round": 70,
  "frontier": [
    "k=32 ef=12 fvu=0.08931536227464676 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @a6b9904",
    "k=24 ef=12 fvu=0.09700598567724228 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @c9ce6fc",
    "k=16 ef=12 fvu=0.10692943632602692 arch=lowrank_soft_codebook_residual lr=3.2e-3 opt=signum @3ba9e99",
    "k=8 ef=12 fvu=0.12280897796154022 arch=lowrank_soft_codebook_residual lr=3.2e-3 opt=signum @cbd5142",
    "k=4 ef=12 fvu=0.14181029796600342 arch=lowrank_soft_codebook_residual lr=3.2e-3 opt=signum @63d528c",
    "k=2 ef=12 fvu=0.16154605150222778 arch=lowrank_soft_codebook_residual lr=3.2e-3 opt=signum @99a5676",
    "k=1 ef=12 fvu=0.17282412946224213 arch=lowrank_two_stage_residual lr=1.6e-3 opt=signum @2d897d7"
  ],
  "latest_results": [
    "r67_1774226107 lowrank_soft_codebook_residual k4 keep fvu=0.14181029796600342 | Run one controlled parameter-only experiment at `K=4` with `",
    "r68_1774226369 lowrank_soft_codebook_residual k2 keep fvu=0.16154605150222778 | Run one controlled parameter-only experiment at `K=2` with `",
    "r69_1774226628 lowrank_soft_codebook_residual k1 discard fvu=0.18000911176204681 | Run one controlled parameter-only experiment at `K=1` with `"
  ],
  "latest_round_summary": [
    "r69 lowrank_soft_codebook_residual param_only -> discard fvu=0.18000911176204681 259s | `lowrank_soft_codebook_residual` has now taken the frontier at `K=16`, `K=8`,..."
  ],
  "agent_state": {
    "consecutive_crashes": 0,
    "consecutive_no_improve": 1,
    "rounds_since_new_family": 70
  },
  "pending_hints": [],
  "next_hypotheses": [
    "If `K=1` wins, treat `lowrank_soft_codebook_residual` as the dominant low-K family and shift focus back to mid-K/new-family search.",
    "If `K=1` is close but slightly behind, consider one confirmatory rerun before concluding the family bottoms out at `K=2`.",
    "If `K=1` clearly loses, keep `lowrank_soft_codebook_residual` as strongest in the `K=2-16` band and resume new-family search for the `K=1` slot."
  ]
}
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
mcp startup: no servers
codex
The `K=1` extension for `lowrank_soft_codebook_residual` lost clearly, so I’m switching from param-only descent to a single new-family hypothesis for the `K=1` slot. I’m using the `sae-architecture-integration` skill to check the repo-specific wiring path and then I’ll choose the smallest architecture change that targets ultra-low-K without touching the fixed execution layer.
exec
/bin/bash -lc "sed -n '1,220p' /root/.codex/skills/sae-architecture-integration/SKILL.md" in /root/sparsify-ascend succeeded in 0ms:
---
name: sae-architecture-integration
description: Use when adding a new SAE architecture family in sparsify-ascend, or when a run fails with Unknown architecture, missing factory dispatch, or other architecture-registration issues. This skill gives a repo-specific checklist for wiring new sparse coder families into sparsify/config.py and sparsify/sparse_coder.py, plus the exact smoke-validation sequence to run before declaring the family runnable.
---

# SAE Architecture Integration

Use this skill for only two situations:

- adding a new architecture family under `sparsify/`
- repairing a run that crashed with `Unknown architecture: ...`

Do not use this skill for ordinary hyperparameter sweeps.

## Goal

Close the full integration loop for a new SAE family so the architecture:

- is accepted by config parsing
- is constructible by runtime dispatch
exec
/bin/bash -lc "sed -n '180,260p' sparsify/config.py" in /root/sparsify-ascendexec
/bin/bash -lc "sed -n '1,260p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
        if self.compile_model:
            from .device import get_device_type
            if get_device_type() != "cuda":
                self.compile_model = False

        if self.use_hadamard:
            bs = self.hadamard_block_size
            if bs <= 0 or (bs & (bs - 1)) != 0:
                raise ValueError(
                    f"hadamard_block_size must be a positive power of 2, got {bs}"
                )

        # Architecture validation
        valid_archs = (
            "topk",
            "batch_topk",
            "adaptive_budget_topk",
            "bucketed_topk",
            "codebook_topk",
            "residual_vq",
 succeeded in 0ms:
import json
from fnmatch import fnmatch
from pathlib import Path
from typing import NamedTuple

import einops
import torch
import torch.nn.functional as F
from huggingface_hub import snapshot_download
from natsort import natsorted
from safetensors import safe_open
from safetensors.torch import load_model, save_model
from torch import Tensor, nn

from .config import SparseCoderConfig
from .device import device_autocast
from .fused_encoder import EncoderOutput, fused_encoder
from .utils import decoder_impl


codex
I’ve confirmed the registration chain and I’m reading the existing low-rank families now. The current evidence says `lowrank_soft_codebook_residual` dominates from `K=2` to `K=16` but not at `K=1`, so the next move should be one new architecture aimed specifically at the `K=1` endpoint rather than another rerun.
exec
/bin/bash -lc "sed -n '260,520p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
            fvu,
            auxk_loss,
        )

    @torch.no_grad()
    def set_decoder_norm_to_unit_norm(self):
        assert self.W_dec is not None, "Decoder weight was not initialized."

        eps = torch.finfo(self.W_dec.dtype).eps
        norm = torch.norm(self.W_dec.data, dim=1, keepdim=True)
        self.W_dec.data /= norm + eps

    @torch.no_grad()
    def remove_gradient_parallel_to_decoder_directions(self):
        assert self.W_dec is not None, "Decoder weight was not initialized."
        if self.W_dec.grad is None:
            return

        parallel_component = einops.einsum(
            self.W_dec.grad,
exec
/bin/bash -lc "sed -n '520,980p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
    def encode(self, x: Tensor) -> EncoderOutput:
        x = x - self.b_dec
        low_acts = F.relu(F.linear(x, self.low_encoder.weight, self.low_encoder.bias))
        high_acts = F.relu(
            F.linear(x, self.high_encoder.weight, self.high_encoder.bias)
        )

        norms = x.norm(dim=-1, keepdim=True)
        centered_norms = norms - norms.mean()
        gate = torch.sigmoid(self.bucket_scale * centered_norms + self.bucket_bias)
        acts = (1.0 - gate) * low_acts + gate * high_acts

        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
        return EncoderOutput(top_acts, top_indices, acts)


class CodebookTopKSparseCoder(SparseCoder):
    """Reconstruct a coarse codebook vector, then sparsify the residual."""

    def __init__(
exec
/bin/bash -lc "rg -n \"class (LowRank|Routed|Bucketed|Whitened|TwoStage|JumpReLU|MultiBranch|Factorized|SoftCodebook).*SparseCoder|if architecture == \\\"lowrank_.*\\\"|if architecture == \\\"two_stage_residual\\\"|if architecture == \\\"multi_branch_gated\\\"\" sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
306:    if architecture == "lowrank_residual_vq":
320:    if architecture == "lowrank_residual":
322:    if architecture == "lowrank_two_stage_residual":
330:    if architecture == "lowrank_gated_residual":
332:    if architecture == "lowrank_jumprelu_residual":
334:    if architecture == "lowrank_multi_branch_residual":
336:    if architecture == "lowrank_factorized_residual":
338:    if architecture == "lowrank_soft_codebook_residual":
342:    if architecture == "lowrank_grouped_residual":
344:    if architecture == "two_stage_residual":
346:    if architecture == "multi_branch_gated":
355:class WhitenedTopKSparseCoder(SparseCoder):
474:class BucketedTopKSparseCoder(SparseCoder):
839:class JumpReLUSparseCoder(SparseCoder):
907:class RoutedSparseCoder(SparseCoder):
979:class FactorizedTopKSparseCoder(SparseCoder):
1026:class LowRankResidualSparseCoder(SparseCoder):
1121:class LowRankTwoStageResidualSparseCoder(LowRankResidualSparseCoder):
1242:class RoutedLowRankTwoStageResidualSparseCoder(LowRankTwoStageResidualSparseCoder):
1405:class BucketedLowRankResidualSparseCoder(LowRankResidualSparseCoder):
exec
/bin/bash -lc "sed -n '1026,1248p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
class LowRankResidualSparseCoder(SparseCoder):
    """Model a dense low-rank trunk, then sparsely code the remaining residual."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        self.num_latents = cfg.num_latents or d_in * cfg.expansion_factor

        # Keep a meaningfully expressive dense trunk before sparse residual
        # coding. Tying trunk rank too tightly to expansion factor made the
        # prototype collapse to a minimal rank under the strong EF=16 recipe.
exec
/bin/bash -lc "sed -n '1824,1915p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
class LowRankSoftCodebookResidualSparseCoder(LowRankResidualSparseCoder):
    """Low-rank trunk with soft codebook residual projection before sparse correction."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
        self.num_codes = min(256, max(32, cfg.k * 2))
        self.codebook = nn.Parameter(
            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
        )
        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
        self.code_router.bias.data.zero_()

exec
/bin/bash -lc "sed -n '1911,2015p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
    """Low-rank trunk with a gated sparse residual selector."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
        self.gate_encoder = nn.Linear(
            d_in, self.num_latents, device=device, dtype=dtype
        )

        # Start near the winning low-rank residual recipe, then let the gate
        # branch learn which residual features deserve support at low K.
        self.gate_encoder.weight.data.zero_()
codex
I have enough context to make the round concrete.

Plan:
1. Add one new family that combines the two strongest signals we now have for low K: the `lowrank_two_stage_residual` endpoint behavior and the `lowrank_soft_codebook_residual` gains from `K=2` to `K=16`.
2. Keep the hypothesis narrow: a soft codebook coarse residual projection before the existing two-stage residual refinement, with no recipe changes beyond the architecture string.
3. Smoke-test the exact launch target at `K=1, EF=12` so the next round can be a clean architecture trial rather than a registration gamble.
file update
M /root/sparsify-ascend/sparsify/sparse_coder.py
@@ -339,2 +339,4 @@
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
@@ -1910,2 +1912,125 @@
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
M /root/sparsify-ascend/sparsify/config.py
@@ -216,2 +216,3 @@
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
apply_patch(auto_approved=true) exited 0 in 7ms:
Success. Updated the following files:
M /root/sparsify-ascend/sparsify/config.py
M /root/sparsify-ascend/sparsify/sparse_coder.py
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index d55878773d0eb41e716062fbb1437a6d7c7fc585..fa6f16a656221e396586d1d2d35b1e96b7aad59f
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -214,6 +214,7 @@
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 295fd21597795590bea0e6fb5c2cff1a0aee0f88..0089e7b80e72206f5feb7dfffb2288938be68304
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -337,6 +337,8 @@
         return LowRankFactorizedResidualSparseCoder
     if architecture == "lowrank_soft_codebook_residual":
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1908,6 +1910,129 @@
         )
 
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index d55878773d0eb41e716062fbb1437a6d7c7fc585..fa6f16a656221e396586d1d2d35b1e96b7aad59f
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -214,6 +214,7 @@
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 295fd21597795590bea0e6fb5c2cff1a0aee0f88..0089e7b80e72206f5feb7dfffb2288938be68304
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -337,6 +337,8 @@
         return LowRankFactorizedResidualSparseCoder
     if architecture == "lowrank_soft_codebook_residual":
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1908,6 +1910,129 @@
         )
 
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
I’ve made the architecture edit. Next I’m running the exact integration smoke at `K=1, EF=12` for the new string so this round doesn’t hand back an unverified family.
exec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify import SparseCoder
from sparsify.config import SparseCoderConfig

device = 'cuda' if torch.cuda.is_available() else 'cpu'
try:
    import torch_npu
    if torch.npu.is_available():
        device = 'npu'
except ImportError:
    pass

arch = 'lowrank_two_stage_soft_codebook_residual'
k = 1
ef = 12

cfg = SparseCoderConfig(architecture=arch, k=k, expansion_factor=ef)
sae = SparseCoder(1024, cfg, device=device, dtype=torch.float32)
x = torch.randn(4, 1024, device=device)
out = sae(x)
out.fvu.backward()
print('integration_smoke: OK', arch, k, ef, device)
PY" in /root/sparsify-ascendfile update:
diff --git a/sparsify/config.py b/sparsify/config.py
index d55878773d0eb41e716062fbb1437a6d7c7fc585..fa6f16a656221e396586d1d2d35b1e96b7aad59f
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -214,6 +214,7 @@
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 295fd21597795590bea0e6fb5c2cff1a0aee0f88..0089e7b80e72206f5feb7dfffb2288938be68304
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -337,6 +337,8 @@
         return LowRankFactorizedResidualSparseCoder
     if architecture == "lowrank_soft_codebook_residual":
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1908,6 +1910,129 @@
         )
 
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

 succeeded in 4.51s:
Using fused decoder on CUDA.
integration_smoke: OK lowrank_two_stage_soft_codebook_residual 1 12 cuda
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index d55878773d0eb41e716062fbb1437a6d7c7fc585..fa6f16a656221e396586d1d2d35b1e96b7aad59f
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -214,6 +214,7 @@
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 295fd21597795590bea0e6fb5c2cff1a0aee0f88..0089e7b80e72206f5feb7dfffb2288938be68304
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -337,6 +337,8 @@
         return LowRankFactorizedResidualSparseCoder
     if architecture == "lowrank_soft_codebook_residual":
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1908,6 +1910,129 @@
         )
 
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
{"command":"run","hypothesis":"`lowrank_soft_codebook_residual` dominates from `K=2` to `K=16` but loses at `K=1`, while `lowrank_two_stage_residual` still owns the `K=1` endpoint. A hybrid that adds a soft codebook coarse residual projection before the proven two-stage low-rank residual refinement may recover the `K=1` slot without giving up the family’s low-K gains.","summary":"Added and smoke-tested a new architecture, `lowrank_two_stage_soft_codebook_residual`, then propose one architecture experiment at `K=1` with the existing winning low-K recipe otherwise fixed.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"smaller_k","family_name":"lowrank_two_stage_soft_codebook_residual","family_stage":"prototype","self_review":"This is a single-hypothesis architecture step targeted exactly at the remaining uncovered endpoint. It reuses two validated ingredients already showing complementary strengths in this repo and was integration-smoke-tested on the exact launch target `K=1, EF=12`.","needs_sanity":false,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_two_stage_soft_codebook_residual"},{"key":"K","value":"1"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["/root/sparsify-ascend/sparsify/config.py","/root/sparsify-ascend/sparsify/sparse_coder.py"],"notes_to_memory":["Round 70 introduces `lowrank_two_stage_soft_codebook_residual`, a new prototype that combines the soft codebook coarse residual projection with the existing low-rank two-stage residual refinement.","Integration chain completed for `lowrank_two_stage_soft_codebook_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation in `sparsify/sparse_coder.py`.","Exact smoke test passed for `lowrank_two_stage_soft_codebook_residual` at `K=1, EF=12` on the concrete dispatched architecture class (`integration_smoke: OK ... cuda`).","Round 70 should test whether this hybrid can beat the current `K=1` frontier anchor (`lowrank_two_stage_residual`, FVU `0.17282412946224213`) while keeping the stronger `LR=3.2e-3` recipe that benefited the soft-codebook family across `K=2-16`."],"next_hypotheses":["If the hybrid wins at `K=1`, test `K=2` to see whether it can subsume the current `lowrank_soft_codebook_residual` low-K band.","If it is close but slightly behind at `K=1`, consider one confirmatory rerun before deciding whether the deficit is architectural or noise.","If it clearly loses at `K=1`, retain `lowrank_soft_codebook_residual` for `K=2-16`, keep `lowrank_two_stage_residual` at `K=1`, and resume a different new-family search for the endpoint."],"primary_variable":"architecture"}
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index d55878773d0eb41e716062fbb1437a6d7c7fc585..fa6f16a656221e396586d1d2d35b1e96b7aad59f
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -214,6 +214,7 @@
             "lowrank_multi_branch_residual",
             "lowrank_factorized_residual",
             "lowrank_soft_codebook_residual",
+            "lowrank_two_stage_soft_codebook_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 295fd21597795590bea0e6fb5c2cff1a0aee0f88..0089e7b80e72206f5feb7dfffb2288938be68304
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -337,6 +337,8 @@
         return LowRankFactorizedResidualSparseCoder
     if architecture == "lowrank_soft_codebook_residual":
         return LowRankSoftCodebookResidualSparseCoder
+    if architecture == "lowrank_two_stage_soft_codebook_residual":
+        return LowRankTwoStageSoftCodebookResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1908,6 +1910,129 @@
         )
 
 
+class LowRankTwoStageSoftCodebookResidualSparseCoder(
+    LowRankTwoStageResidualSparseCoder
+):
+    """Low-rank trunk with soft codebook projection before two-stage sparse refinement."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
+        self.num_codes = min(256, max(32, cfg.k * 2))
+        self.codebook = nn.Parameter(
+            torch.randn(self.num_codes, d_in, device=device, dtype=dtype) * 0.02
+        )
+        self.code_router = nn.Linear(d_in, self.num_codes, device=device, dtype=dtype)
+        self.code_router.bias.data.zero_()
+
+    def _project_codebook(self, residual: Tensor) -> tuple[Tensor, Tensor]:
+        logits = self.code_router(residual)
+        routing = logits.softmax(dim=-1)
+        coarse = routing @ self.codebook
+        return coarse, logits
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+        combined_pre = torch.maximum(stage1_pre, stage2_pre)
+        return EncoderOutput(combined_acts, combined_indices, combined_pre)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        coarse, _ = self._project_codebook(residual)
+        code_residual = residual - coarse
+
+        stage1_acts, stage1_indices, stage1_pre = fused_encoder(
+            code_residual, self.encoder.weight, self.encoder.bias, self.stage1_k
+        )
+        stage1_out = self.decode_residual(stage1_acts, stage1_indices)
+
+        stage2_input = code_residual - stage1_out
+        stage2_acts, stage2_indices, stage2_pre = fused_encoder(
+            stage2_input,
+            self.residual_encoder.weight,
+            self.residual_encoder.bias,
+            self.stage2_k,
+        )
+        stage2_out = self.decode_residual(stage2_acts, stage2_indices)
+
+        sae_out = trunk + coarse + stage1_out + stage2_out + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+
+            aux_stage1 = torch.where(dead_mask[None], stage1_pre, -torch.inf)
+            aux_stage2 = torch.where(dead_mask[None], stage2_pre, -torch.inf)
+
+            aux1_k = min(self.stage1_k, k_aux)
+            aux2_k = min(self.stage2_k, max(1, k_aux - aux1_k))
+
+            aux1_acts, aux1_indices = aux_stage1.topk(aux1_k, sorted=False)
+            aux1_out = self.decode_residual(aux1_acts, aux1_indices)
+
+            aux2_acts, aux2_indices = aux_stage2.topk(aux2_k, sorted=False)
+            aux2_out = self.decode_residual(aux2_acts, aux2_indices)
+
+            e_hat = trunk + coarse + aux1_out + aux2_out + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        combined_acts = torch.cat((stage1_acts, stage2_acts), dim=-1)
+        combined_indices = torch.cat((stage1_indices, stage2_indices), dim=-1)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            combined_acts,
+            combined_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

tokens used
95,259
