{"change_type":"param_only","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_expert_residual"}],"expected_win":"explore_unknown","family_name":"lowrank_expert_residual","family_stage":"probe","hypothesis":"候选 family 设为 `lowrank_expert_residual`。最终重构公式为 `x_hat = Σ_r β_r(x) u_r + Σ_{i∈S_{e(x)}(x)} α_{e,i}(x) b_{e,i} + Σ_j γ_j(x) c_j`，其中 `U={u_r}` 是静态 low-rank trunk 向量库，`{B_e}` 是 routed expert 静态子库，`C={c_j}` 是全局 residual 静态补偿库。在线导出链路仍是“trunk 查表 + routed expert 子库查表 + residual 补偿查表 + 有限加权和 + 选择性在线补偿”，静态库不随样本动态生成，因此该 family 判定为 `扩展兼容`。`expert_topk`、`shared_routed_expert_topk`、`lowrank_expert_topk` 等仍属兼容 family；`batch_topk`、`adaptive_budget_topk` 等 batch/sample-coupled family 仍属 `不兼容`，不能驱动当前 objective leaderboard。本轮唯一主假设是：Round 220 之后，低成本 selector / factorized / single-library 线已经被连续负信号基本裁尽，但当前高优先级里仍有一个尚未在 `r212` exact recipe 上干净裁决的核心结构问题，即“shared low-rank trunk + routed residual experts”本身是否值得继续。因而相对 r212 仅把 `ARCHITECTURE` 从 `expert_topk` 切到 `lowrank_expert_residual`，保持 `K=32`、`EXPANSION_FACTOR=1`、`OPTIMIZER=adam`、`LR=8e-4` 与其余主 recipe 不变，直接测试极短 sparse path 下加入 low-rank trunk 与 residual cleanup 后，是否会出现足够大的 exceed 崩塌，从而证明该结构线仍值得后续用 `TRUNK_RANK` / `STAGE1_RATIO` / `K` 去压缩。本地精确成本复核显示该点 selection_accesses=1605632、deployment_accesses=245760、combined_accesses=1851392，对当前 1024x4096 fused-QKV proxy 的 exact total_cost_ratio=0.44140625；若要刷新 r212 的 objective=0.35553750753402713，需要 exceed_alpha_0.50 压到约 `≤-0.08586874246597288`，这在数学上已不现实。因此这不是一个近端翻盘点，而是一轮高风险但高信息增量的 architecture-slot verdict：如果它连在当前 anchor recipe 下都不能显著压低 exceed，那么这条 trunk+expert+residual 主张在当前 hookpoint 上应整体降级。","needs_sanity":false,"next_hypotheses":["如果 `lowrank_expert_residual@K32` 的 exceed 仍然没有显著低于 `0.18`，则可把“trunk + routed expert + residual cleanup”这条结构线在当前 target 上整体降级，不再让它主导后续 matched architecture probe 预算。","如果该 probe 虽然 objective 很差，但 exceed 明显打到约 `0.12~0.15`，说明这条结构线仍有质量价值；后续只有在允许回到 param-only 压缩轴时，才值得以它为基线去压 `TRUNK_RANK` 或 `STAGE1_RATIO`，而不是继续换 family。","如果该 probe 意外给出比历史强得多的 exceed 交换率，则说明当前 target 的主要缺口确实更接近 tail-error cleanup 而非纯选择成本；下一轮才值得继续留在该 family 内做单参数 follow-up。"],"notes_to_memory":["Round 226 `jumprelu@K32`、Round 227 `group_topk@K32`、Round 228 `topk@K32` 都停在 `objective≈0.60 / cost=0.289062x / exceed≈0.31`，single-library selector 线已基本完成 closure。","更早的 low-cost factorized 线也已被裁掉：`factorized_expert_topk@K32` 给出 `objective=0.482251 / cost=0.132812x / exceed=0.349439`，`factorized_topk@K32` 给出 `objective=0.491881 / cost=0.164062x / exceed=0.327819`。","当前 remaining high-priority yet unclosed 的结构槽位里，`lowrank_expert_residual` 比 `shared_lowrank_routed_expert_residual` 和 `shared_routed_factorized_expert_residual` 更贴近用户明确点名的 trunk + routed residual expert 方向，且旧历史里也曾给出更低 exceed 的弱先验。","本地 allowlist 校验通过：`validate_env_overrides([{key: ARCHITECTURE, value: lowrank_expert_residual}], fallback_architecture='expert_topk')` 返回空列表。","本地成本复核：`lowrank_expert_residual K=32 EF=1 TRUNK_RANK=16 NUM_EXPERTS=128 ACTIVE_EXPERTS=2 LATENTS_PER_EXPERT=192 STAGE1_RATIO=0.25` 的 exact total_cost_ratio=0.44140625，其中 selection=0.3828125x、deployment=0.05859375x。"],"reference_round":212,"self_review":"这轮严格满足单变量原则：相对 r212 只改 `ARCHITECTURE`，没有新增参数，也没有代码改动。没有继续选 `whitened_topk`、`codebook_topk`、`routed`、`bucketed_topk` 等 remaining 结构，是因为它们虽然未在当前 anchor recipe 上全做过干净 probe，但要么已偏离当前高优先级方向，要么在 objective 上同样没有现实赢面且信息增量更低。选择 `lowrank_expert_residual` 的理由是：在 single-library / factorized / lightweight selector 槽位都已经被 current-target 负信号裁掉后，它是剩余候选里最直接对应用户高优先级假设、且最值得得到一次 matched recipe verdict 的结构线。主要风险很明确：它在当前 total_cost 下不可能刷新 incumbent，所以这一步只能被理解为结构裁决，不是 frontier attack；但在当前阶段，这恰好是更重要的问题。","summary":"建议以 r212 为 reference，只改 `ARCHITECTURE=lowrank_expert_residual`。这轮不再补低信息 selector 变体，而是用一次高风险 matched architecture probe，直接裁决当前仍最值得回答的结构问题：`low-rank trunk + routed expert + residual cleanup` 这条线在当前 hookpoint 上究竟是否还值得继续。","touched_files":[]}