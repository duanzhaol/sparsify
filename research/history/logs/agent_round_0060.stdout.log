{"command":"run","hypothesis":"A conditional mixture of residual dictionaries may recover the mid-K lane where fixed residual mechanisms have stalled: `lowrank_multi_branch_residual` keeps the strong low-rank trunk, then lets a learned sample-level router mix several residual scoring branches before final top-k selection, potentially matching heterogeneous residual regimes better than a single encoder.","summary":"Added a new prototype family, `lowrank_multi_branch_residual`, in `sparsify/` and registered it in config/factory dispatch. The architecture applies the existing low-rank trunk first, then mixes three residual encoder branches with a learned sample-level routing head before sparse selection. Exact smoke test passed for `architecture=lowrank_multi_branch_residual, K=24, EF=12`, and a quick encode comparison confirmed behavior differs from `lowrank_residual`. This is a fundamentally different conditional-dictionary architecture for severe-stagnation recovery, not a scalar or K sweep.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"lower_fvu","family_name":"lowrank_multi_branch_residual","family_stage":"prototype","self_review":"This follows the severe-stagnation policy by introducing a new mechanism family rather than tuning previous losers. The experiment remains architecture-only at the pinned `K=24, EF=12, signum, LR=3.2e-3, AUXK_ALPHA=0.0` recipe, so attribution stays clean. Main risk: the earlier plain `multi_branch_gated` baseline failed badly, so the gain depends on the low-rank trunk making branch specialization operate on an easier residual space rather than repeating that failure pattern.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_multi_branch_residual"},{"key":"K","value":"24"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["Added new prototype architecture `lowrank_multi_branch_residual` that combines the existing low-rank trunk with a learned sample-level mixture of residual encoder branches before top-k selection.","Integration chain completed for `lowrank_multi_branch_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation.","Exact smoke test passed for `lowrank_multi_branch_residual` at `K=24, EF=12` with forward/backward.","Quick behavioral diff check showed `lowrank_multi_branch_residual` encode output is observably different from `lowrank_residual` on the same random input."],"next_hypotheses":["If `lowrank_multi_branch_residual` is competitive but slightly worse at `K=24`, test `K=16` to see whether conditional branch specialization helps more under tighter sparsity.","If `lowrank_multi_branch_residual` wins at `K=24`, follow with `K=32` to check whether the gain persists across the mid-K band.","If it clearly loses at `K=24`, retire the conditional-dictionary branch and move to a different mechanism family rather than tuning branch count or router details."],"primary_variable":"architecture"}

--- STDERR ---
OpenAI Codex v0.116.0 (research preview)
--------
workdir: /root/sparsify-ascend
model: gpt-5.4
provider: ehein
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: none
session id: 019d17e8-09af-7ab3-8981-5a034238157c
--------
user
Continue the SAE research session. Round 60.
Return one JSON object matching the action schema. No markdown fences.

Policy guidance:
SEVERE STAGNATION: 24 rounds without improvement.
1. If only K=128 tested, MUST try K=64 or K=32
2. Otherwise try fundamentally different architecture
3. Stop minor tweaks — they haven't worked

K coverage: [1, 2, 4, 8, 16, 24, 32].

Structured update:
{
  "round": 60,
  "frontier": [
    "k=32 ef=12 fvu=0.08931536227464676 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @a6b9904",
    "k=24 ef=12 fvu=0.09700598567724228 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @c9ce6fc",
    "k=16 ef=12 fvu=0.10837031900882721 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @b071b4c",
    "k=8 ef=12 fvu=0.1280982345342636 arch=lowrank_two_stage_residual lr=3.2e-3 opt=signum @b840cf0",
    "k=4 ef=12 fvu=0.1484663188457489 arch=lowrank_residual lr=1.6e-3 opt=signum @7752961",
    "k=2 ef=12 fvu=0.16997215151786804 arch=lowrank_residual lr=1.6e-3 opt=signum @6c46e80",
    "k=1 ef=12 fvu=0.17282412946224213 arch=lowrank_two_stage_residual lr=1.6e-3 opt=signum @2d897d7"
  ],
  "latest_results": [
    "r57_1774222575 whitened_topk k24 discard fvu=165315.8125 | Run a pure architecture comparison at `K=24, EF=12` with `wh",
    "r58_1774223115 lowrank_jumprelu_residual k24 discard fvu=0.1026327833533287 | Added a new prototype family, `lowrank_jumprelu_residual`, b",
    "r59_1774223476 lowrank_residual_vq k24 discard fvu=0.10183659940958023 | Added a new prototype family, `lowrank_residual_vq`, in `spa"
  ],
  "latest_round_summary": [
    "r59 lowrank_residual_vq edit_sae_code -> discard fvu=0.10183659940958023 361s | A low-rank trunk plus hard residual codebook may recover a better mid-K trade..."
  ],
  "agent_state": {
    "consecutive_crashes": 0,
    "consecutive_no_improve": 24,
    "rounds_since_new_family": 60
  },
  "pending_hints": [],
  "next_hypotheses": [
    "If `lowrank_residual_vq` is competitive but slightly worse at `K=24`, test `K=16` to see whether coarse residual coding helps more under tighter sparsity.",
    "If `lowrank_residual_vq` wins at `K=24`, follow with `K=32` to see whether the gain persists across the mid-K band.",
    "If it clearly loses at `K=24`, retire the low-rank-plus-discrete branch and move to a different mechanism family rather than tuning codebook size or router details."
  ]
}
warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
mcp startup: no servers
codex
The discrete-residual branch also lost, so another mid-K scalar tweak is off the table. I’m scanning for one more mechanism change that is structurally different from routed/gated/discrete/whitened branches and can still be integrated cleanly under `sparsify/`.
exec
/bin/bash -lc "sed -n '936,1018p' sparsify/sparse_coder.py" in /root/sparsify-ascendexec
/bin/bash -lc "sed -n '1878,2055p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
        top_acts = acts.gather(-1, top_indices)
        return EncoderOutput(top_acts, top_indices, acts)


class GroupTopKSparseCoder(SparseCoder):
    """Select one winner per local group before the global top-k competition."""

    def encode(self, x: Tensor) -> EncoderOutput:
        x = x - self.b_dec
        acts = F.relu(F.linear(x, self.encoder.weight, self.encoder.bias))

        group_size = self.cfg.group_topk_size
        if self.num_latents % group_size != 0:
            raise ValueError(
                "group_topk requires num_latents divisible by group_topk_size, "
                f"got num_latents={self.num_latents} and "
                f"group_topk_size={group_size}"
            )

        num_groups = self.num_latents // group_size
 succeeded in 0ms:
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=decoder)
        self.gate_encoder = nn.Linear(
            d_in, self.num_latents, device=device, dtype=dtype
        )

        mix_rank = min(d_in, max(cfg.k * 2, d_in // 8))
        self.preconditioner_down = nn.Linear(
            d_in, mix_rank, bias=False, device=device, dtype=dtype
        )
        self.preconditioner_up = nn.Linear(
            mix_rank, d_in, bias=False, device=device, dtype=dtype
        )

        # Keep the initial behavior close to a stable normalized identity while
exec
/bin/bash -lc 'rg -n "class .*SparseCoder" sparsify/sparse_coder.py' in /root/sparsify-ascend succeeded in 0ms:
37:class SparseCoder(nn.Module):
349:class WhitenedTopKSparseCoder(SparseCoder):
395:class AdaptiveBudgetTopKSparseCoder(SparseCoder):
442:class BatchTopKSparseCoder(SparseCoder):
468:class BucketedTopKSparseCoder(SparseCoder):
530:class CodebookTopKSparseCoder(SparseCoder):
633:class ResidualVQSparseCoder(SparseCoder):
734:class TwoCodeResidualVQSparseCoder(ResidualVQSparseCoder):
833:class JumpReLUSparseCoder(SparseCoder):
871:class GatedSparseCoder(SparseCoder):
901:class RoutedSparseCoder(SparseCoder):
940:class GroupTopKSparseCoder(SparseCoder):
973:class FactorizedTopKSparseCoder(SparseCoder):
1020:class LowRankResidualSparseCoder(SparseCoder):
1115:class LowRankTwoStageResidualSparseCoder(LowRankResidualSparseCoder):
1236:class RoutedLowRankTwoStageResidualSparseCoder(LowRankTwoStageResidualSparseCoder):
1399:class BucketedLowRankResidualSparseCoder(LowRankResidualSparseCoder):
1516:class LowRankResidualVQSparseCoder(LowRankResidualSparseCoder):
1608:class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
1690:class LowRankJumpReLUResidualSparseCoder(LowRankResidualSparseCoder):
codex
Recent losers covered plain top-k, whitening, JumpReLU-on-residual, and low-rank-plus-discrete. I’m reading the existing multi-branch implementation once before deciding whether a low-rank mixture-of-dictionaries branch is distinct enough to justify one more family.
exec
/bin/bash -lc "sed -n '2170,2285p' sparsify/sparse_coder.py" in /root/sparsify-ascend succeeded in 0ms:
class MultiBranchGatedSparseCoder(SparseCoder):
    """Mix several gated encoder branches before the final top-k selection."""

    def __init__(
        self,
        d_in: int,
        cfg: SparseCoderConfig,
        device: str | torch.device = "cpu",
        dtype: torch.dtype | None = None,
        *,
        decoder: bool = True,
    ):
        nn.Module.__init__(self)
        self.cfg = cfg
        self.d_in = d_in
        self.num_latents = cfg.num_latents or d_in * cfg.expansion_factor
        self.num_branches = 3

        self.branch_encoders = nn.ModuleList(
            [
codex
I’m committing to a mixture-of-residual-dictionaries family instead of another selection tweak. The new branch will keep the proven low-rank trunk, then mix several residual encoders by a learned sample-level router before the final top-k, which is closer to a conditional dictionary than the earlier plain multi-branch baseline.
file update
M /root/sparsify-ascend/sparsify/config.py
@@ -213,2 +213,3 @@
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
M /root/sparsify-ascend/sparsify/sparse_coder.py
@@ -333,2 +333,4 @@
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
@@ -1607,2 +1609,101 @@
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
apply_patch(auto_approved=true) exited 0 in 7ms:
Success. Updated the following files:
M /root/sparsify-ascend/sparsify/config.py
M /root/sparsify-ascend/sparsify/sparse_coder.py
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index e8f7f48cd98279d2ff38cd6887ac2aa046206a60..9e8785f28aee89957645a492f84da8420f28f976
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -211,6 +211,7 @@
             "whitened_lowrank_residual",
             "lowrank_gated_residual",
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 4fb8f5b930961c3aa74487a26bfa21dae0f4bc5b..c0a9fa70401978323b040a172613a2f80e4d2db1
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -331,6 +331,8 @@
         return LowRankGatedResidualSparseCoder
     if architecture == "lowrank_jumprelu_residual":
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1605,6 +1607,105 @@
         )
 
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index e8f7f48cd98279d2ff38cd6887ac2aa046206a60..9e8785f28aee89957645a492f84da8420f28f976
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -211,6 +211,7 @@
             "whitened_lowrank_residual",
             "lowrank_gated_residual",
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 4fb8f5b930961c3aa74487a26bfa21dae0f4bc5b..c0a9fa70401978323b040a172613a2f80e4d2db1
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -331,6 +331,8 @@
         return LowRankGatedResidualSparseCoder
     if architecture == "lowrank_jumprelu_residual":
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1605,6 +1607,105 @@
         )
 
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
The new family is wired in. I’m running the exact `K=24, EF=12` forward/backward smoke and a behavior-diff check now so this round stays on the architecture axis only.
exec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify import SparseCoder
from sparsify.config import SparseCoderConfig

arch = 'lowrank_multi_branch_residual'
k = 24
ef = 12

device = 'cuda' if torch.cuda.is_available() else 'cpu'
try:
    import torch_npu
    if torch.npu.is_available():
        device = 'npu'
except ImportError:
    pass

cfg = SparseCoderConfig(architecture=arch, k=k, expansion_factor=ef)
sae = SparseCoder(128, cfg, device=device, dtype=torch.float32)
x = torch.randn(4, 128, device=device)
out = sae(x)
out.fvu.backward()
print('integration_smoke: OK', arch, k, ef, device, float(out.fvu.detach()))
PY" in /root/sparsify-ascendexec
/bin/bash -lc "python - <<'PY'
import sys
sys.path.insert(0, '.')
import torch
from sparsify import SparseCoder
from sparsify.config import SparseCoderConfig

device = 'cuda' if torch.cuda.is_available() else 'cpu'
x = torch.randn(2, 128, device=device)
base = SparseCoder(128, SparseCoderConfig(architecture='lowrank_residual', k=24, expansion_factor=12), device=device, dtype=torch.float32)
new = SparseCoder(128, SparseCoderConfig(architecture='lowrank_multi_branch_residual', k=24, expansion_factor=12), device=device, dtype=torch.float32)
with torch.no_grad():
    b = base.encode(x)
    n = new.encode(x)
    same_idx = torch.equal(b.top_indices, n.top_indices)
    same_act = torch.allclose(b.top_acts, n.top_acts)
print('behavior_diff', same_idx, same_act)
PY" in /root/sparsify-ascendfile update:
diff --git a/sparsify/config.py b/sparsify/config.py
index e8f7f48cd98279d2ff38cd6887ac2aa046206a60..9e8785f28aee89957645a492f84da8420f28f976
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -211,6 +211,7 @@
             "whitened_lowrank_residual",
             "lowrank_gated_residual",
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 4fb8f5b930961c3aa74487a26bfa21dae0f4bc5b..c0a9fa70401978323b040a172613a2f80e4d2db1
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -331,6 +331,8 @@
         return LowRankGatedResidualSparseCoder
     if architecture == "lowrank_jumprelu_residual":
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1605,6 +1607,105 @@
         )
 
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

 succeeded in 4.54s:
Using fused decoder on CUDA.
behavior_diff False False
 succeeded in 4.81s:
Using fused decoder on CUDA.
integration_smoke: OK lowrank_multi_branch_residual 24 12 cuda 0.6823186874389648
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index e8f7f48cd98279d2ff38cd6887ac2aa046206a60..9e8785f28aee89957645a492f84da8420f28f976
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -211,6 +211,7 @@
             "whitened_lowrank_residual",
             "lowrank_gated_residual",
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 4fb8f5b930961c3aa74487a26bfa21dae0f4bc5b..c0a9fa70401978323b040a172613a2f80e4d2db1
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -331,6 +331,8 @@
         return LowRankGatedResidualSparseCoder
     if architecture == "lowrank_jumprelu_residual":
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1605,6 +1607,105 @@
         )
 
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

codex
{"command":"run","hypothesis":"A conditional mixture of residual dictionaries may recover the mid-K lane where fixed residual mechanisms have stalled: `lowrank_multi_branch_residual` keeps the strong low-rank trunk, then lets a learned sample-level router mix several residual scoring branches before final top-k selection, potentially matching heterogeneous residual regimes better than a single encoder.","summary":"Added a new prototype family, `lowrank_multi_branch_residual`, in `sparsify/` and registered it in config/factory dispatch. The architecture applies the existing low-rank trunk first, then mixes three residual encoder branches with a learned sample-level routing head before sparse selection. Exact smoke test passed for `architecture=lowrank_multi_branch_residual, K=24, EF=12`, and a quick encode comparison confirmed behavior differs from `lowrank_residual`. This is a fundamentally different conditional-dictionary architecture for severe-stagnation recovery, not a scalar or K sweep.","change_type":"edit_sae_code","experiment_tier":"full","expected_win":"lower_fvu","family_name":"lowrank_multi_branch_residual","family_stage":"prototype","self_review":"This follows the severe-stagnation policy by introducing a new mechanism family rather than tuning previous losers. The experiment remains architecture-only at the pinned `K=24, EF=12, signum, LR=3.2e-3, AUXK_ALPHA=0.0` recipe, so attribution stays clean. Main risk: the earlier plain `multi_branch_gated` baseline failed badly, so the gain depends on the low-rank trunk making branch specialization operate on an easier residual space rather than repeating that failure pattern.","needs_sanity":true,"env_overrides":[{"key":"ARCHITECTURE","value":"lowrank_multi_branch_residual"},{"key":"K","value":"24"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"3.2e-3"},{"key":"AUXK_ALPHA","value":"0.0"}],"touched_files":["sparsify/config.py","sparsify/sparse_coder.py"],"notes_to_memory":["Added new prototype architecture `lowrank_multi_branch_residual` that combines the existing low-rank trunk with a learned sample-level mixture of residual encoder branches before top-k selection.","Integration chain completed for `lowrank_multi_branch_residual`: config whitelist + `_get_sae_class()` dispatch + class implementation.","Exact smoke test passed for `lowrank_multi_branch_residual` at `K=24, EF=12` with forward/backward.","Quick behavioral diff check showed `lowrank_multi_branch_residual` encode output is observably different from `lowrank_residual` on the same random input."],"next_hypotheses":["If `lowrank_multi_branch_residual` is competitive but slightly worse at `K=24`, test `K=16` to see whether conditional branch specialization helps more under tighter sparsity.","If `lowrank_multi_branch_residual` wins at `K=24`, follow with `K=32` to check whether the gain persists across the mid-K band.","If it clearly loses at `K=24`, retire the conditional-dictionary branch and move to a different mechanism family rather than tuning branch count or router details."],"primary_variable":"architecture"}
file update:
diff --git a/sparsify/config.py b/sparsify/config.py
index e8f7f48cd98279d2ff38cd6887ac2aa046206a60..9e8785f28aee89957645a492f84da8420f28f976
--- a/sparsify/config.py
+++ b/sparsify/config.py
@@ -211,6 +211,7 @@
             "whitened_lowrank_residual",
             "lowrank_gated_residual",
             "lowrank_jumprelu_residual",
+            "lowrank_multi_branch_residual",
             "whitened_lowrank_gated_residual",
             "lowrank_grouped_residual",
             "whitened_topk",
diff --git a/sparsify/sparse_coder.py b/sparsify/sparse_coder.py
index 4fb8f5b930961c3aa74487a26bfa21dae0f4bc5b..c0a9fa70401978323b040a172613a2f80e4d2db1
--- a/sparsify/sparse_coder.py
+++ b/sparsify/sparse_coder.py
@@ -331,6 +331,8 @@
         return LowRankGatedResidualSparseCoder
     if architecture == "lowrank_jumprelu_residual":
         return LowRankJumpReLUResidualSparseCoder
+    if architecture == "lowrank_multi_branch_residual":
+        return LowRankMultiBranchResidualSparseCoder
     if architecture == "whitened_lowrank_gated_residual":
         return WhitenedLowRankGatedResidualSparseCoder
     if architecture == "lowrank_grouped_residual":
@@ -1605,6 +1607,105 @@
         )
 
 
+class LowRankMultiBranchResidualSparseCoder(LowRankResidualSparseCoder):
+    """Low-rank trunk with a learned mixture of residual scoring branches."""
+
+    def __init__(
+        self,
+        d_in: int,
+        cfg: SparseCoderConfig,
+        device: str | torch.device = "cpu",
+        dtype: torch.dtype | None = None,
+        *,
+        decoder: bool = True,
+    ):
+        super().__init__(d_in, cfg, device=device, dtype=dtype, decoder=False)
+        self.num_branches = 3
+        self.branch_encoders = nn.ModuleList(
+            [
+                nn.Linear(d_in, self.num_latents, device=device, dtype=dtype)
+                for _ in range(self.num_branches)
+            ]
+        )
+        self.branch_mix_logits = nn.Linear(
+            d_in, self.num_branches, device=device, dtype=dtype
+        )
+
+        for encoder in self.branch_encoders:
+            encoder.bias.data.zero_()
+        self.branch_mix_logits.weight.data.zero_()
+        self.branch_mix_logits.bias.data.zero_()
+
+        if decoder:
+            decoder_init = torch.stack(
+                [branch.weight.data for branch in self.branch_encoders], dim=0
+            ).mean(dim=0)
+            self.W_dec = nn.Parameter(decoder_init)
+            if self.cfg.normalize_decoder:
+                self.set_decoder_norm_to_unit_norm()
+        else:
+            self.W_dec = None
+
+    def _mixed_acts(self, residual: Tensor) -> Tensor:
+        branch_weights = torch.softmax(self.branch_mix_logits(residual), dim=-1)
+        branch_acts = []
+        for branch_idx, encoder in enumerate(self.branch_encoders):
+            acts = F.relu(F.linear(residual, encoder.weight, encoder.bias))
+            branch_weight = branch_weights[..., branch_idx].unsqueeze(-1)
+            branch_acts.append(acts * branch_weight)
+        return torch.stack(branch_acts, dim=0).sum(dim=0)
+
+    def encode(self, x: Tensor) -> EncoderOutput:
+        x = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x))
+        residual = x - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+        return EncoderOutput(top_acts, top_indices, acts)
+
+    @device_autocast
+    def forward(
+        self, x: Tensor, y: Tensor | None = None, *, dead_mask: Tensor | None = None
+    ) -> ForwardOutput:
+        x_centered = x - self.b_dec
+        trunk = self.trunk_decoder(self.trunk_encoder(x_centered))
+        residual = x_centered - trunk
+        acts = self._mixed_acts(residual)
+        top_acts, top_indices = torch.topk(acts, self.cfg.k, dim=-1, sorted=False)
+
+        sparse_residual = self.decode_residual(top_acts, top_indices)
+        sae_out = trunk + sparse_residual + self.b_dec
+
+        if y is None:
+            y = x
+
+        e = y - sae_out
+        total_variance = (y - y.mean(0)).pow(2).sum()
+
+        if dead_mask is not None and (num_dead := int(dead_mask.sum())) > 0:
+            k_aux = y.shape[-1] // 2
+            scale = min(num_dead / k_aux, 1.0)
+            k_aux = min(k_aux, num_dead)
+            auxk_latents = torch.where(dead_mask[None], acts, -torch.inf)
+            auxk_acts, auxk_indices = auxk_latents.topk(k_aux, sorted=False)
+            e_hat = trunk + self.decode_residual(auxk_acts, auxk_indices) + self.b_dec
+            auxk_loss = (e_hat - e.detach()).pow(2).sum()
+            auxk_loss = scale * auxk_loss / total_variance
+        else:
+            auxk_loss = sae_out.new_tensor(0.0)
+
+        l2_loss = e.pow(2).sum()
+        fvu = l2_loss / total_variance
+
+        return ForwardOutput(
+            sae_out,
+            top_acts,
+            top_indices,
+            fvu,
+            auxk_loss,
+        )
+
+
 class LowRankGatedResidualSparseCoder(LowRankResidualSparseCoder):
     """Low-rank trunk with a gated sparse residual selector."""
 

tokens used
153,468
