{
  "command": "run",
  "hypothesis": "最终重构公式保持为 x_hat = Σ_{i∈S_1(x)} α_i(x) b_i + Σ_{j∈S_{e(r_1)}(r_1)} γ_{e,j}(x) c_{e,j}，其中 r_1 = x - Σ_{i∈S_1(x)} α_i(x) b_i。第一段在静态 coarse library B={b_i} 上做有限加权和，第二段对残差用轻量 router 选择单个静态 expert 子库 C_e={c_{e,j}} 并做局部 sparse 修正，因此可直接导出到 LUTurbo/Lottable 的“查表 + 加权求和 + 选择性补偿”链路，兼容性判定为扩展兼容。此次 repair 不改 family_name 或 env_overrides，只修复注册链与最小实现，使原始 `ARCHITECTURE=two_stage_residual_expert` 实验可实例化、前向、反向并继续真实训练。",
  "summary": "进入 repair 模式后，按 Unknown architecture 注册链故障处理：补上 `two_stage_residual_expert` 的 config whitelist、factory dispatch、concrete class，以及 autoresearch override wiring。保持原始实验定义不变，只修实现，让 Round 40 的原始 run 可以重新启动。",
  "change_type": "edit_sae_code",
  "expected_win": "explore_unknown",
  "family_name": "two_stage_residual_expert",
  "family_stage": "prototype",
  "self_review": "满足 repair 约束：未改 family_name，未改 env_overrides，仍是相对 r27 只切到 `ARCHITECTURE=two_stage_residual_expert`。根因已确认是 `Unknown architecture`，并已完成 checklist 所需的 whitelist、dispatch、class、exact-string smoke。显式 dispatch smoke 在 `(architecture=two_stage_residual_expert, K=192, EF=1)` 上通过，selection_cost≈0.31x、deployment≈0.19x、total≈0.50x，combined_budget_ratio≈0.33，仍在 LUTurbo 成本硬约束内。override 校验也已通过，下一次真实运行不应再卡在 parse/dispatch 阶段。",
  "needs_sanity": true,
  "env_overrides": [
    {
      "key": "ARCHITECTURE",
      "value": "two_stage_residual_expert"
    }
  ],
  "touched_files": [
    "sparsify/config.py",
    "sparsify/sparse_coder.py",
    "research/AutoResearch/override_registry.py"
  ],
  "notes_to_memory": [
    "`two_stage_residual_expert` 属于扩展兼容 family；导出形式是 coarse 静态库与单 expert 静态子库上的有限加权和，不是动态字典。",
    "这次阻塞是纯注册链故障，不是优化器、训练环或 autograd 问题；后续若再失败，应按新的错误类型继续排查，不再重复宣称 registration repair。",
    "当前 `ARCHITECTURE=two_stage_residual_expert` 的 autoresearch override 已接受 `STAGE1_RATIO`，便于后续在 family 跑通后再做单变量调参。"
  ],
  "next_hypotheses": [
    "如果原始训练 run 成功启动并正常收敛，则下一轮才有资格评估 `two_stage_residual_expert` 是否形成新的低到中成本 Pareto 段。",
    "如果真实训练仍失败，新的 blocker 应该已经从 parse/dispatch 转移到实现细节或训练期行为，需要按新错误继续最小修复。"
  ],
  "reference_round": 27
}