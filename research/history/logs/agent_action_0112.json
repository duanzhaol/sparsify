{"change_type":"architecture","command":"run","env_overrides":[{"key":"ARCHITECTURE","value":"routed_lowrank_two_stage_soft_codebook_residual"},{"key":"K","value":"128"},{"key":"EXPANSION_FACTOR","value":"12"},{"key":"OPTIMIZER","value":"signum"},{"key":"LR","value":"2.0e-3"}],"expected_win":"lower_fvu","experiment_tier":"full","family_name":"routed_lowrank_two_stage_soft_codebook_residual","family_stage":"probe","hypothesis":"With the current `lowrank_two_stage_soft_codebook_residual` mainline stalled and the first matched reserve probe (`lowrank_asymmetric_two_stage_soft_codebook_residual`) coming in weaker, the highest-value remaining matched architecture probe is `routed_lowrank_two_stage_soft_codebook_residual`. It keeps the same LUTurbo-compatible static-library decomposition but changes only the selector structure through explicit routing. Holding `K=128`, `EXPANSION_FACTOR=12`, `OPTIMIZER=signum`, and `LR=2.0e-3` fixed isolates one clean question: is routing itself still strong enough on the matched reference recipe to justify future follow-up, or should this family be deprioritized behind the simpler symmetric mainline.","needs_sanity":false,"next_hypotheses":["If the matched `routed_lowrank_two_stage_soft_codebook_residual` probe is clearly worse than the current mainline, return next round to `lowrank_two_stage_soft_codebook_residual` recipe work and deprioritize routed follow-up.","If the matched routed probe is competitive on full-curve shape or endpoint, record it as a viable reserve family but still return next round to the `lowrank_two_stage_soft_codebook_residual` mainline.","Do not change `K`, optimizer, loss, or preprocess in this probe; this round is only about whether the routed architecture itself is worth keeping in consideration."],"notes_to_memory":["Round 112 uses the one allowed matched architecture probe on `routed_lowrank_two_stage_soft_codebook_residual` against the current `lowrank_two_stage_soft_codebook_residual` mainline reference recipe.","Judge this probe by the whole curve versus the archived symmetric mainline at `K=128`, `EF=12`, `OPTIMIZER=signum`, `LR=2.0e-3`, not only the terminal FVU.","Regardless of outcome, next round should return to the `lowrank_two_stage_soft_codebook_residual` mainline rather than extending probe-only branching."],"primary_variable":"architecture","self_review":"This satisfies the round policy: one matched architecture probe, explicit `EXPANSION_FACTOR=12`, fixed `K=128`, fixed optimizer, fixed LR, and only `ARCHITECTURE` changes. `routed_lowrank_two_stage_soft_codebook_residual` is the strongest remaining LUTurbo-compatible nearby family with prior EF=12 evidence, so it provides a high-information probe without entangling recipe changes. The tradeoff is another round away from mainline cleanup, but this is acceptable under the current probe allowance.","summary":"Use the one allowed matched architecture probe on `routed_lowrank_two_stage_soft_codebook_residual`, keeping `K=128`, `EXPANSION_FACTOR=12`, `OPTIMIZER=signum`, and `LR=2.0e-3` fixed and changing only `ARCHITECTURE`.","touched_files":[]}